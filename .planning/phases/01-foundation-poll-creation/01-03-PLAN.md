---
phase: 01-foundation-poll-creation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - .env.production.example
  - vercel.json
  - README.md
autonomous: false
requirements: [PLAT-02, PLAT-03]
user_setup:
  - service: neon
    why: "Production Postgres database on the free tier (same schema as local, selected via the neon-http driver when NODE_ENV=production)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Dashboard (or Vercel Marketplace -> Neon) -> Connection Details -> POOLED connection string (-pooler.neon.tech), sslmode=require"
    dashboard_config:
      - task: "Create a free Neon project (Postgres 17) — via Vercel Marketplace (no credit card) or neon.tech"
        location: "Vercel Dashboard -> Storage -> Neon, or neon.tech"
  - service: vercel
    why: "Free-tier (Hobby) hosting for the Next.js app"
    env_vars:
      - name: DATABASE_URL
        source: "Set in Vercel Project -> Settings -> Environment Variables = Neon POOLED connection string"
      - name: NEXT_PUBLIC_BASE_URL
        source: "Vercel Project -> Settings -> Environment Variables = https://<your-project>.vercel.app"
    dashboard_config:
      - task: "Create/connect a Vercel project (link the repo or use `vercel` CLI) on the Hobby plan"
        location: "Vercel Dashboard -> Add New Project"

must_haves:
  truths:
    - "After the local Docker Desktop skeleton works end-to-end, the deployed Vercel URL creates a poll successfully against Neon Postgres"
    - "A poll creation request after Neon has auto-suspended (cold start) completes within the Vercel function timeout"
    - "The deployed app uses the exact same single Drizzle schema as local — the neon-http driver is selected automatically by NODE_ENV=production, with no code divergence"
    - "All runtime dependencies (Neon Postgres, Vercel hosting) operate entirely within free tiers"
    - "Neon post-idle cold-start resume completes within the Vercel function timeout"
  artifacts:
    - path: ".env.production.example"
      provides: "Documented production env var names (DATABASE_URL pooled, NEXT_PUBLIC_BASE_URL) — placeholders only, no secrets"
      min_lines: 3
    - path: "README.md"
      provides: "Deploy runbook: Neon project + pooled URL, Vercel env vars, drizzle-kit migrate against Neon, cold-start verification"
      contains: "pooler"
      min_lines: 20
  key_links:
    - from: "src/lib/db/index.ts"
      to: "@neondatabase/serverless (neon-http)"
      via: "NODE_ENV === 'production' branch selecting the Neon driver"
      pattern: "neon"
    - from: "Vercel env DATABASE_URL"
      to: "Neon pooled endpoint"
      via: "-pooler.neon.tech pooled connection string"
      pattern: "pooler"
  prohibitions:
    - "MUST NOT commit the Neon DATABASE_URL or any secret to the repo — production secrets live only in Vercel env vars and .env*.local (gitignored)"
    - "MUST NOT use the direct (non-pooled) Neon connection string for app queries — use the -pooler.neon.tech pooled string (reserve direct only for drizzle-kit migrate)"
    - "MUST NOT diverge the production code path from local — the same single Drizzle schema is used; only the driver switches by NODE_ENV"
    - "MUST NOT deploy the diagnostic /health skeleton route to production — it performs an UNAUTHENTICATED DB write on button click; remove the src/app/health route (or hard-guard it to return notFound() when NODE_ENV=production) BEFORE `vercel --prod`. verification: deployed https://<project>.vercel.app/health returns 404 (PLAN-boundary prohibition-probe finding)"
---

<objective>
Deploy the working Phase 1 app (poll creation -> admin page with two share links) to Vercel against Neon Postgres on free tiers. This is the FINAL step of Phase 1 and runs ONLY after the local Docker Desktop Walking Skeleton and the full create -> admin flow (01-01, 01-02) work end-to-end (D-13). The same single Drizzle schema is reused; the neon-http driver is selected automatically by NODE_ENV=production — no code divergence.

Purpose: Satisfy PLAT-02 (Vercel/Neon free-tier deployment) and confirm the Postgres-everywhere strategy holds in production, including the Neon cold-start path that real sporadic D&D-group usage will hit.
Output: A live deployed URL that creates polls against Neon, a migrated production schema, a cold-start-verified create path, and a committed deploy runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-poll-creation/01-SPEC.md
@.planning/phases/01-foundation-poll-creation/01-CONTEXT.md
@.planning/phases/01-foundation-poll-creation/01-RESEARCH.md
@.planning/phases/01-foundation-poll-creation/01-01-SUMMARY.md
@.planning/phases/01-foundation-poll-creation/01-02-SUMMARY.md
@.planning/research/PITFALLS.md

# Key references:
# - RESEARCH.md Vercel deployment checklist (lines 786-803), Pitfall 2 Neon cold-start (lines 658-672),
#   dual-driver client (lines 230-262), pooled-vs-direct anti-pattern (lines 622-623).
# - PITFALLS.md Pitfall 6 Neon auto-suspend (lines 133-155), Integration Gotchas Neon (lines 249-251).
# - Assumption A1: Vercel Hobby function timeout ~60s [ASSUMED] — verify against current Vercel docs (RESEARCH.md lines 826-832).
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision Neon + Vercel (free tier) — human account setup</name>
  <action>Pause for the human to complete the external free-tier account setup Claude cannot perform: create a free Neon Postgres (17) project and obtain its POOLED connection string, and create/connect a Vercel Hobby project (or authenticate the `vercel` CLI). No code is written in this task — it gates Task 2's migration + deploy.</action>
  <what-built>Local Docker Desktop skeleton + full create->admin flow already proven (01-01, 01-02). This task gates on the only steps Claude cannot perform: creating the external free-tier accounts/resources.</what-built>
  <how-to-verify>
    Complete these one-time setup steps, then provide the values back:
    1. Create a free Neon project (Postgres 17) — easiest via Vercel Dashboard -> Storage -> Neon (no credit card), or at neon.tech.
    2. Copy the POOLED connection string (host contains `-pooler.neon.tech`, `sslmode=require`). Do NOT use the direct (non-pooled) string for the app.
    3. Create/connect a Vercel project on the Hobby plan (link this repo, or run `vercel login` so the CLI can deploy).
    4. Confirm you have: the Neon pooled DATABASE_URL, and Vercel access (logged-in CLI or linked project).
  </how-to-verify>
  <resume-signal>Reply "done" with confirmation that the Neon pooled DATABASE_URL and Vercel access are ready (paste nothing secret into chat — set DATABASE_URL via `vercel env` / dashboard or a local untracked file).</resume-signal>
  <verify>
    <human-check>User confirms a free Neon project exists with a pooled connection string and Vercel access is available.</human-check>
  </verify>
  <acceptance_criteria>
    - A free Neon Postgres project exists; pooled (-pooler.neon.tech) connection string available
    - A Vercel Hobby project is connected or the `vercel` CLI is authenticated
    - No credit card / paid tier engaged (PLAT-03)
  </acceptance_criteria>
  <done>A free Neon project (pooled connection string) and Vercel Hobby access are confirmed ready; no paid tier engaged.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migrate schema to Neon + configure Vercel env + deploy</name>
  <read_first>
    - .planning/phases/01-foundation-poll-creation/01-RESEARCH.md (Vercel checklist lines 786-803, drizzle-kit migrate lines 494-507, pooled vs direct lines 622-623)
    - .planning/research/PITFALLS.md (Pitfall 6 lines 133-155; Integration Gotchas lines 249-251)
  </read_first>
  <files>.env.production.example, vercel.json, README.md, src/app/health/</files>
  <action>
    **[PLAN-boundary prohibition-probe finding] Remove or guard the diagnostic /health skeleton BEFORE deploying.** The /health route (created in 01-01) performs an unauthenticated DB write on button click — it must NOT be live in production. Either delete the `src/app/health/` directory now that the real create→admin flow (01-02) proves the stack, OR hard-guard it to `notFound()` when `process.env.NODE_ENV === "production"`. Deleting is preferred (it was always a temporary skeleton probe). Verify the deployed `/health` returns 404.

    Add .env.production.example documenting the production env var NAMES only (DATABASE_URL = Neon POOLED string, NEXT_PUBLIC_BASE_URL = https://<project>.vercel.app) — placeholders, never real secrets (do not commit secrets; mitigates T-01-09). Optionally add a minimal vercel.json only if needed (framework auto-detect usually suffices; do NOT add any sub-daily cron — Hobby disallows it per PITFALLS Pitfall 7).

    [BLOCKING] Apply the EXISTING migrations (from drizzle/, created in 01-01) to Neon BEFORE the app serves traffic: run `npx drizzle-kit migrate` with DATABASE_URL set to the Neon connection string (use the direct connection for migration if available, else the pooled string), so the live Neon database has the `polls` + `options` tables and the options_dedup constraint. Build/types passing is NOT sufficient — the production schema must physically exist.

    Set Vercel project env vars: DATABASE_URL = Neon POOLED string (-pooler.neon.tech) and NEXT_PUBLIC_BASE_URL = the Vercel URL, via `vercel env add` (or the dashboard). Confirm src/lib/db/index.ts already selects neon-http for NODE_ENV=production (from 01-01) — no code change; same single schema (no divergence; mitigates the divergence prohibition). Deploy with `vercel --prod` (or push to the connected branch). Write the deploy runbook into README.md: Neon project + pooled URL, Vercel env vars, `drizzle-kit migrate` to Neon, deploy command, and the cold-start verification procedure.
  </action>
  <verify>
    <automated>test -f .env.production.example &amp;&amp; ! git ls-files --error-unmatch .env.production 2>/dev/null &amp;&amp; ! grep -RniE "neon\\.tech|postgres(ql)?://[^ ]*:[^ @]*@" .env.production.example README.md 2>/dev/null; echo "no real secret strings committed"; grep -qi "pooler" README.md</automated>
  </verify>
  <acceptance_criteria>
    - The diagnostic /health skeleton route is removed (or guarded to 404 in production) before deploy — deployed /health returns 404 (no unauthenticated prod DB write)
    - Neon has the migrated `polls` + `options` schema (verified before the app reads/writes in prod)
    - Vercel env vars set: DATABASE_URL (pooled), NEXT_PUBLIC_BASE_URL
    - Production uses neon-http via NODE_ENV=production with the same schema (no divergence)
    - Deploy succeeds; README documents the full runbook; no secrets committed
  </acceptance_criteria>
  <done>Neon schema migrated; Vercel env vars set (pooled DATABASE_URL + base URL); app deployed via neon-http with no code divergence; runbook in README; no secrets committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify deployed poll creation + Neon cold-start path</name>
  <action>Pause for the human to verify the deployed app on the live Vercel URL: create a poll, confirm both share links with the admin link marked "Keep private", confirm the participant view never exposes the admin token, confirm altered tokens return 404, and confirm a post-idle Neon cold-start poll creation succeeds within the function timeout. Claude resumes on approval.</action>
  <what-built>The app is deployed to Vercel against Neon with the migrated schema and pooled connection. This checkpoint confirms the live happy path and the cold-start path that real sporadic usage hits.</what-built>
  <how-to-verify>
    1. Visit the deployed URL (https://<your-project>.vercel.app/) and create a poll (title + at least one date). Confirm you land on /a/[adminUrlId] with both share links and the admin link marked "Keep private".
    2. Open the participant link in a private window — confirm it resolves and never exposes the admin link.
    3. Alter a character in either link's token — confirm it returns "Poll not found" (404).
    4. Cold-start: leave the app idle 10+ minutes (Neon free tier auto-suspends after ~5 min), then create another poll. Confirm it succeeds without a timeout/504 (pooled connection + neon-http should resume within the Vercel function timeout).
    5. Visit https://<your-project>.vercel.app/health — confirm it returns 404 (the diagnostic skeleton must not be live in production).
  </how-to-verify>
  <resume-signal>Reply "approved" if all four checks pass, or describe what failed (e.g. 504 on cold start, admin link leaked, wrong date).</resume-signal>
  <verify>
    <human-check>Deployed URL creates a poll (incl. a post-idle cold-start request) within free tiers; tokens 404 when altered; admin link never leaks to /p.</human-check>
  </verify>
  <acceptance_criteria>
    - Deployed app creates a poll end-to-end against Neon (PLAT-02)
    - Post-idle cold-start poll creation completes within the Vercel function timeout (no 504)
    - Altered tokens return 404; participant view never exposes the admin token
    - Everything runs on free tiers (PLAT-03)
  </acceptance_criteria>
  <done>Human confirms the deployed app creates polls (including a post-idle cold-start request) within free tiers, altered tokens 404, and the admin token never leaks to /p.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel serverless function → Neon Postgres | DB connection over TLS (pooled endpoint); must stay warm enough to beat the function timeout |
| operator → Vercel/Neon dashboards | Production secrets (DATABASE_URL) live only in platform env, never in the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | Neon DATABASE_URL / production secrets | mitigate | secrets only in Vercel env vars + gitignored local files; .env.production.example holds placeholders; verify no secret strings committed |
| T-01-10 | Denial of Service | Neon cold-start vs Vercel function timeout | mitigate | use the pooled (-pooler.neon.tech) connection + neon-http driver; verify the post-idle cold-start create path completes (RESEARCH Pitfall 2, PITFALLS Pitfall 6) |
| T-01-11 | Tampering | production URL tokens | mitigate | inherited from 01-02: notFound() 404 on miss, nanoid(21) non-enumerable IDs — re-verified on the deployed URL |
| T-01-SC | Tampering | npm installs | accept | no new packages; dependency set audited in 01-01 (all [VERIFIED]) |
</threat_model>

<verification>
- Deployed URL creates a poll against Neon, including a post-idle cold-start request (PLAT-02).
- Same single schema; neon-http selected by NODE_ENV=production (no divergence).
- Pooled connection string in use; no secrets committed.
- All within Vercel Hobby + Neon free tiers (PLAT-03).
</verification>

<success_criteria>
- Phase 1 is live: an organizer can create a poll on the deployed app and get two distinct, unguessable share links, on free tiers, with no timezone drift — after the local Docker Desktop stack proved the same flow first (D-13).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-poll-creation/01-03-SUMMARY.md` when done.
</output>

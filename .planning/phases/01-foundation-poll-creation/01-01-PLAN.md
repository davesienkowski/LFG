---
phase: 01-foundation-poll-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - components.json
  - vitest.config.ts
  - drizzle.config.ts
  - docker-compose.yml
  - Dockerfile.dev
  - .dockerignore
  - .env.example
  - .env.local
  - .gitignore
  - src/lib/db/index.ts
  - src/lib/db/schema.ts
  - src/lib/tokens.ts
  - src/lib/format-date.ts
  - src/lib/format-date.test.ts
  - src/lib/env.ts
  - src/app/health/page.tsx
  - src/app/health/skeleton-check.tsx
  - src/app/health/actions.ts
  - drizzle/
autonomous: true
requirements: [PLAT-01, PLAT-03, PLAT-04, LINK-02, LINK-03, POLL-03, POLL-04]
user_setup: []

must_haves:
  truths:
    - "From a clean checkout, `docker compose up` brings up a `db` (Postgres) service and a `web` (Next.js dev) service in Docker Desktop, with the app reachable on http://localhost:3000"
    - "The walking-skeleton route performs one real DB write and one real DB read against the Docker Postgres and renders the live result in the browser"
    - "A YYYY-MM-DD date renders as the same calendar day under TZ Pacific/Kiritimati (UTC+14) and Etc/GMT+12 (UTC-12)"
    - "The token helper produces two independent crypto-random 21-char IDs; the admin ID is never derived from the participant ID"
    - "The Drizzle schema is migrated into the live Docker Postgres before any read/write is asserted (build/types passing is not sufficient)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Two-service dev stack: db (postgres:17) + web (Next.js dev, source bind-mounted, DATABASE_URL pointing at db)"
      contains: "services:"
      min_lines: 25
    - path: "Dockerfile.dev"
      provides: "Dev image for the web service running the Next.js dev server with hot reload"
      min_lines: 8
    - path: "src/lib/db/schema.ts"
      provides: "polls + options tables with DATE storage and NULLS NOT DISTINCT dedupe constraint"
      contains: "nullsNotDistinct"
      min_lines: 35
    - path: "src/lib/db/index.ts"
      provides: "Dual-driver Drizzle client (node-postgres locally, neon-http when NODE_ENV=production)"
      contains: "node-postgres"
      min_lines: 12
    - path: "src/lib/tokens.ts"
      provides: "generateToken() wrapper around nanoid(21)"
      contains: "nanoid"
      min_lines: 4
    - path: "src/lib/format-date.ts"
      provides: "Timezone-safe date-only formatter (no new Date() on date-only strings)"
      min_lines: 6
    - path: "src/lib/format-date.test.ts"
      provides: "TZ test asserting identical calendar day under UTC+14 and UTC-12"
      contains: "Kiritimati"
      min_lines: 10
  key_links:
    - from: "src/lib/db/index.ts"
      to: "process.env.DATABASE_URL"
      via: "environment-switched driver selection"
      pattern: "NODE_ENV"
    - from: "src/app/health/actions.ts"
      to: "src/lib/db/schema.ts"
      via: "real insert + select via the Drizzle client"
      pattern: "db\\.(insert|select)"
  prohibitions:
    - "MUST NOT make the admin token derivable from the participant token (no hash/transform/shared seed) — generated by two independent nanoid(21) calls. verification: node-test"
    - "MUST NOT shift a candidate date's calendar day via timezone conversion (no new Date() on date-only input). verification: node-test"
    - "MUST NOT commit DATABASE_URL or any secret to the repo, and MUST NOT bake secrets into Docker image layers (.env/.env.local gitignored; compose injects env at runtime)"
---

<objective>
Stand up the LFG foundation and prove the full stack works end-to-end inside Docker Desktop (the Walking Skeleton). This plan scaffolds Next.js 16 + Tailwind + shadcn/ui (D-01), installs Drizzle + Postgres drivers + nanoid + Zod (D-02), writes the dual-driver DB client (D-03), the `polls`/`options` schema (D-05/D-06), the token (D-07) and timezone-safe date (D-11) helpers, the Dockerized two-service dev environment (`db` + `web`, D-12), migrates the schema into the live Docker Postgres (D-04), and renders a skeleton health route that performs one real DB write and one real DB read in the browser.

Purpose: Every later slice (createPoll, voting, dashboard, email) builds on the schema, token model, date handling, and dev environment locked here. These are the highest cost-to-change decisions in the project (CONTEXT.md, D-05/D-06/D-07/D-11; D-12 Docker Desktop dev).
Output: A running `docker compose up` dev stack, a migrated Postgres schema, passing TZ + token tests, and a working DB round-trip proven in the browser. Architectural decisions recorded in SKELETON.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-poll-creation/01-SPEC.md
@.planning/phases/01-foundation-poll-creation/01-CONTEXT.md
@.planning/phases/01-foundation-poll-creation/01-RESEARCH.md
@.planning/phases/01-foundation-poll-creation/SKELETON.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- New contracts this plan creates. Downstream plan 01-02 imports these directly — no codebase exploration needed. -->

src/lib/db/schema.ts (to be created — exact shape locked by D-05/D-06, see RESEARCH.md Pattern 2, lines 264-314):
  - export const polls: pgTable with { id uuid PK defaultRandom; participantUrlId text notNull unique; adminUrlId text notNull unique; title text notNull; description text (nullable); location text (nullable); status text notNull default "open"; createdAt timestamptz notNull defaultNow }
  - export const options: pgTable with { id uuid PK defaultRandom; pollId uuid notNull FK->polls.id onDelete cascade; date date({mode:"string"}) notNull; startTime time (nullable); position integer notNull default 0 } plus unique("options_dedup").on(pollId,date,startTime).nullsNotDistinct() and index("options_poll_id_idx").on(pollId)
  - export type Poll = typeof polls.$inferSelect; export type NewPoll = typeof polls.$inferInsert
  - export type Option = typeof options.$inferSelect; export type NewOption = typeof options.$inferInsert

src/lib/db/index.ts (to be created — see RESEARCH.md Pattern 1, lines 230-262):
  - export const db: NodePgDatabase<typeof schema> | NeonHttpDatabase<typeof schema>  (driver chosen by NODE_ENV)

src/lib/tokens.ts (to be created):
  - export function generateToken(): string   // nanoid(21), 126-bit entropy, URL-safe

src/lib/format-date.ts (to be created):
  - export function formatDateOnly(yyyymmdd: string, opts?): string   // never constructs Date() from a date-only string
  - export function formatDateWithTime(yyyymmdd: string, hhmm: string | null): string
</interfaces>

# Locked versions (RESEARCH.md Standard Stack, STACK.md): next@16.2.9, react@19.2.7, typescript@6.0.3,
# tailwindcss@4.3.2, drizzle-orm@0.45.2, drizzle-kit@0.31.10, @neondatabase/serverless@1.1.0,
# pg@8.22.0, @types/pg@8.20.0, nanoid@5.1.16, zod@4.4.3, @t3-oss/env-nextjs@0.13.11.
# Package Legitimacy Audit: all 10 packages [VERIFIED] via npm registry (RESEARCH.md lines 141-159) — no [ASSUMED]/[SUS]/[SLOP].
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js + dependencies + Dockerized dev environment + tooling</name>
  <read_first>
    - .planning/research/STACK.md (Installation lines 56-77, Env strategy lines 81-112)
    - .planning/research/PITFALLS.md (Pitfall 5 SQLite-vs-Postgres lines 109-129, Pitfall 6 Neon pooling lines 133-155)
    - .planning/phases/01-foundation-poll-creation/01-RESEARCH.md (Pattern 1 dual-driver lines 230-262, Pattern 5 drizzle.config lines 476-491, Pattern 6 Docker lines 509-548)
    - .planning/phases/01-foundation-poll-creation/01-UI-SPEC.md (Design System lines 18-50 — shadcn init + components)
  </read_first>
  <files>package.json, next.config.ts, tsconfig.json, components.json, vitest.config.ts, drizzle.config.ts, docker-compose.yml, Dockerfile.dev, .dockerignore, .env.example, .env.local, .gitignore, src/lib/db/index.ts, src/lib/env.ts</files>
  <action>
    Scaffold Next.js 16 in place per D-01 (App Router + TypeScript + Tailwind + ESLint + src/) WITHOUT clobbering existing .planning/.claude/.git/.gitignore: run create-next-app into a sibling temp dir (e.g. `lfg-scaffold`) with flags `--typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-turbopack`, then move all generated files (including dotfiles) into the repo root, merging into the existing .gitignore (do not delete the existing node_modules/.env entries), then delete the temp dir. Pin next@16.2.9 / react@19.2.7 / typescript@6.0.3 / tailwindcss@4.3.2 if create-next-app installs newer majors.

    Install runtime deps at locked versions (D-02): drizzle-orm@0.45.2, @neondatabase/serverless@1.1.0, pg@8.22.0, nanoid@5.1.16, zod@4.4.3, @t3-oss/env-nextjs@0.13.11. Install dev deps: drizzle-kit@0.31.10, @types/pg@8.20.0, vitest, and a vitest config (`vitest.config.ts`) that runs node-environment tests. Add npm scripts: `dev` (next dev), `build`, `test` (vitest run), and the drizzle-kit migration workflow scripts (D-04) `db:generate` (drizzle-kit generate), `db:migrate` (drizzle-kit migrate), `db:push` (drizzle-kit push).

    Initialize shadcn/ui with `npx shadcn@latest init --defaults` (new-york, zinc, CSS variables, src/) then add ONLY: button input label textarea card (per UI-SPEC — no other components in Phase 1; date selection uses native input type=date). Configure Inter via next/font/google.

    Write the dual-driver Drizzle client at src/lib/db/index.ts EXACTLY per D-03 and RESEARCH.md Pattern 1 (lines 240-262): neon-http when `process.env.NODE_ENV === "production"`, node-postgres otherwise; declare the explicit `DB` union type. Write drizzle.config.ts (D-04: dialect postgresql, schema ./src/lib/db/schema.ts, out ./drizzle, url from DATABASE_URL). Add src/lib/env.ts using @t3-oss/env-nextjs validating DATABASE_URL (server) and NEXT_PUBLIC_BASE_URL (client).

    Author the Dockerized dev environment (D-12): `Dockerfile.dev` (node:22-bookworm base, WORKDIR /app, copy package*.json, npm install, expose 3000, CMD npm run dev -- -H 0.0.0.0). `docker-compose.yml` with TWO services — `db`: image postgres:17, env POSTGRES_USER=postgres / POSTGRES_PASSWORD=password / POSTGRES_DB=lfg, ports 5432:5432, named volume for data, healthcheck pg_isready; `web`: build from Dockerfile.dev, source bind-mounted (with an anonymous volume for /app/node_modules so the host does not shadow container deps), `depends_on db: condition service_healthy`, environment DATABASE_URL=postgres://postgres:password@db:5432/lfg and NEXT_PUBLIC_BASE_URL=http://localhost:3000, ports 3000:3000. Add `.dockerignore` (node_modules, .next, .git, .env*). Write `.env.example` (documented placeholders) and `.env.local` (DATABASE_URL=postgres://postgres:password@localhost:5432/lfg for host-run drizzle-kit, NEXT_PUBLIC_BASE_URL=http://localhost:3000). Confirm .gitignore excludes .env and .env*.local and node_modules so NO secret is committed and secrets are NOT baked into the image (mitigates T-01-01).
  </action>
  <verify>
    <automated>test -f docker-compose.yml &amp;&amp; test -f Dockerfile.dev &amp;&amp; test -f src/lib/db/index.ts &amp;&amp; test -f drizzle.config.ts &amp;&amp; npx next --version &amp;&amp; grep -q "node-postgres" src/lib/db/index.ts &amp;&amp; grep -q "neon-http" src/lib/db/index.ts &amp;&amp; grep -Eq "db:[^A-Za-z]" docker-compose.yml &amp;&amp; grep -q "postgres:17" docker-compose.yml &amp;&amp; grep -Eq "(web|next)" docker-compose.yml &amp;&amp; git check-ignore .env.local .env</automated>
  </verify>
  <acceptance_criteria>
    - create-next-app project exists at repo root (not nested); .planning/.claude/.git preserved (D-01)
    - All locked deps installed (D-02); `npx next --version` reports 16.x
    - shadcn initialized (components.json present) with button/input/label/textarea/card only
    - src/lib/db/index.ts selects neon-http for production and node-postgres otherwise (D-03)
    - docker-compose.yml defines both `db` (postgres:17, healthcheck) and `web` (Next.js dev, bind-mounted, DATABASE_URL -> db service, 3000:3000) (D-12)
    - .env / .env*.local are gitignored (no secrets committed; not baked into image)
  </acceptance_criteria>
  <done>Next.js 16 scaffolded at repo root with locked deps + shadcn (5 components), dual-driver DB client, drizzle.config, typed env, and the two-service Docker Desktop dev stack; no secrets committed.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drizzle schema (polls + options) + migrate into live Docker Postgres + TZ-safe date + token helpers</name>
  <read_first>
    - .planning/phases/01-foundation-poll-creation/01-RESEARCH.md (Pattern 2 schema lines 264-314, Pattern 4 timezone lines 434-474, Generating tokens lines 743-757, Pitfall 1 NULLS NOT DISTINCT lines 646-654)
    - .planning/phases/01-foundation-poll-creation/01-CONTEXT.md (D-05 polls, D-06 options, D-07 tokens, D-11 dates)
    - .planning/phases/01-foundation-poll-creation/01-SPEC.md (requirements 3,4,7,11; Prohibitions P1, P3 lines 136-146)
  </read_first>
  <files>src/lib/db/schema.ts, src/lib/tokens.ts, src/lib/format-date.ts, src/lib/format-date.test.ts, drizzle/ (generated migrations)</files>
  <action>
    Write src/lib/db/schema.ts EXACTLY per D-05/D-06 and RESEARCH.md Pattern 2 (lines 270-314): `polls` (uuid PK defaultRandom; participantUrlId text notNull unique; adminUrlId text notNull unique; title text notNull; description text; location text; status text notNull default "open"; createdAt timestamptz notNull defaultNow) and `options` (uuid PK; pollId uuid notNull references polls.id onDelete cascade; date with `date("date", { mode: "string" })` notNull so Postgres returns 'YYYY-MM-DD' strings — never a JS Date (D-11/P3); startTime `time("start_time")` nullable; position integer notNull default 0). Add the composite `unique("options_dedup").on(pollId, date, startTime).nullsNotDistinct()` so two date-only options with the same date collide (Pitfall 1), and `index("options_poll_id_idx").on(pollId)`. Export the four inferred types (Poll, NewPoll, Option, NewOption).

    Write src/lib/tokens.ts: `generateToken()` returning `nanoid(21)` (≥126-bit entropy, crypto.getRandomValues internally — D-07/LINK-03). This helper is called twice independently by createPoll in 01-02; it MUST contain no derivation/transform logic (P1).

    Write src/lib/format-date.ts: `formatDateOnly(yyyymmdd)` and `formatDateWithTime(yyyymmdd, hhmm)` that format by splitting the 'YYYY-MM-DD' (and 'HH:MM') strings — NEVER `new Date("YYYY-MM-DD")` (P3/D-11). Produce human-readable output (e.g. "Saturday, July 12" / "Saturday, July 12 at 2:00 PM") using string-based weekday/month derivation or a date lib only with explicit local/no-UTC parsing. Write src/lib/format-date.test.ts asserting `formatDateOnly("2025-07-12")` yields an identical string when the test process runs under TZ=Pacific/Kiritimati (UTC+14) and TZ=Etc/GMT+12 (UTC-12) — implement the test so both TZ values are exercised (e.g. set process.env.TZ per case or document the dual `TZ=... vitest` invocation and assert pure string equality).

    [BLOCKING] Apply the schema to the LIVE Docker Postgres BEFORE asserting any read/write (D-04): start the db service (`docker compose up -d db`), wait for healthy, then run `npm run db:generate` (drizzle-kit generate -> SQL in ./drizzle) and `npm run db:migrate` (drizzle-kit migrate) with DATABASE_URL=postgres://postgres:password@localhost:5432/lfg. Build/types passing is NOT sufficient — the live `polls` and `options` tables (and the options_dedup constraint) must exist in the running database. Verify by querying the catalog.
  </action>
  <verify>
    <automated>docker compose up -d db &amp;&amp; until docker compose exec -T db pg_isready -U postgres -d lfg; do sleep 2; done &amp;&amp; npm run db:generate &amp;&amp; DATABASE_URL=postgres://postgres:password@localhost:5432/lfg npm run db:migrate &amp;&amp; docker compose exec -T db psql -U postgres -d lfg -c "\dt" | grep -Eq "polls|options" &amp;&amp; docker compose exec -T db psql -U postgres -d lfg -c "\d options" | grep -qi "NULLS NOT DISTINCT" &amp;&amp; TZ=Pacific/Kiritimati npx vitest run src/lib/format-date.test.ts &amp;&amp; TZ=Etc/GMT+12 npx vitest run src/lib/format-date.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `polls` and `options` tables physically exist in the running Docker Postgres (verified via psql \dt), not merely in TypeScript (D-04 schema-push gate)
    - The options_dedup unique constraint is present with NULLS NOT DISTINCT (D-06)
    - generateToken() returns a 21-char URL-safe string; no derivation logic in the file (D-07/P1)
    - format-date test passes identically under TZ=Pacific/Kiritimati and TZ=Etc/GMT+12 (D-11/PLAT-04/P3)
    - drizzle/ contains generated migration SQL committed to the repo
  </acceptance_criteria>
  <done>polls + options tables (with NULLS NOT DISTINCT dedupe + poll_id index) migrated into the live Docker Postgres; token helper and TZ-safe date helper + passing dual-TZ test in place.</done>
</task>

<task type="auto">
  <name>Task 3: Walking-skeleton DB round-trip proven in Docker Desktop</name>
  <read_first>
    - .planning/phases/01-foundation-poll-creation/SKELETON.md (capability proven end-to-end)
    - .planning/phases/01-foundation-poll-creation/01-RESEARCH.md (Pattern 3 server action lines 316-403 — proto reference only; this is a hardcoded skeleton probe, NOT the real createPoll)
    - $HOME/.claude/gsd-core/references/planner-mvp-mode.md (Walking Skeleton section)
  </read_first>
  <files>src/app/health/page.tsx, src/app/health/actions.ts, src/app/health/skeleton-check.tsx</files>
  <action>
    Build the thinnest end-to-end proof that Next.js + Drizzle + the dual-driver client + Postgres all work inside Docker Desktop. This is a deliberately hardcoded skeleton probe (MVP allows hardcoded values, no validation here) — the REAL validated createPoll + form lands in 01-02.

    src/app/health/actions.ts: a `"use server"` action `runSkeletonCheck()` that performs one REAL WRITE (insert a poll using the real `polls` schema with hardcoded title "Skeleton check" + two independent `generateToken()` calls, plus one `options` row with a hardcoded date string) and one REAL READ (select count of polls and the most-recent poll's participantUrlId/adminUrlId), returning the counts/ids. src/app/health/skeleton-check.tsx: a `"use client"` component with a "Run database check" button that invokes the action (via form action or transition) and displays the returned write+read result. src/app/health/page.tsx: an RSC page that also reads `count(polls)` directly on load (proving an independent read path) and renders the client probe.

    This route is a temporary diagnostic; it does not touch the surfaces 01-02 owns (`/`, `/a/...`, `/p/...`). Document in SKELETON.md the exact run sequence: `docker compose up` then visit http://localhost:3000/health and click "Run database check".
  </action>
  <verify>
    <automated>docker compose up -d &amp;&amp; until docker compose exec -T db pg_isready -U postgres -d lfg; do sleep 2; done &amp;&amp; for i in $(seq 1 30); do curl -sf http://localhost:3000/health >/dev/null &amp;&amp; break || sleep 3; done &amp;&amp; curl -sf http://localhost:3000/health | grep -iq "database" &amp;&amp; docker compose exec -T db psql -U postgres -d lfg -c "select count(*) from polls;" | grep -Eq "[0-9]+"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose up` brings up db + web; http://localhost:3000/health serves successfully (HTTP 200) (PLAT-01)
    - Clicking "Run database check" performs a real INSERT and the page reflects a real SELECT against the Docker Postgres
    - The health route does not create or modify any file under src/app/page.tsx, src/app/a/, or src/app/p/
    - SKELETON.md documents the `docker compose up` + /health run sequence
  </acceptance_criteria>
  <done>`docker compose up` serves /health in Docker Desktop; the skeleton performs a real DB write + read in the browser, proving the full stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repository | Secrets (DATABASE_URL) must never enter version control or image layers |
| app (web container) → Docker Postgres (db container) | DB connection over the compose network; parameterized queries only |
| browser → server action (/health skeleton probe) | Untrusted trigger; skeleton uses hardcoded values, no user-supplied input persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env / .env.local / Dockerfile.dev | mitigate | .env and .env*.local gitignored; docker-compose injects DATABASE_URL at runtime; secrets NOT copied into image layers; .dockerignore excludes .env* |
| T-01-02 | Spoofing / Information Disclosure | src/lib/tokens.ts (nanoid) | mitigate | generateToken() = nanoid(21), ≥126-bit entropy via crypto.getRandomValues; non-enumerable foundation for 404-on-miss (LINK-03) |
| T-01-03 | Elevation of Privilege | token helper used by createPoll | mitigate | helper contains no derivation/transform; two independent calls produce participant vs admin IDs (D-07 / P1) |
| T-01-SC | Tampering | npm package installs | accept | RESEARCH.md Package Legitimacy Audit complete: all 10 packages [VERIFIED] via npm registry, zero [ASSUMED]/[SUS]/[SLOP], no postinstall scripts — no blocking-human checkpoint required |
</threat_model>

<verification>
- `docker compose up` from a clean checkout brings up the full dev stack (db + web) in Docker Desktop (PLAT-01).
- Live `polls`/`options` tables exist in the Docker Postgres after migration (schema-push gate satisfied).
- TZ test passes identically under UTC+14 and UTC-12 (PLAT-04 / P3).
- Token helper emits two independent crypto-random 21-char IDs (LINK-02 / LINK-03 / P1).
- Walking-skeleton route performs a real write + read in the browser.
- No secrets committed (`git check-ignore .env .env.local` succeeds for both).
</verification>

<success_criteria>
- The dev environment is the local Docker Desktop stack; the Walking Skeleton is proven there (not on a cloud deploy).
- Schema, dual-driver client, token + date helpers, and the Docker dev stack are in place for 01-02 to build the real createPoll feature.
- SKELETON.md records framework/DB/deploy/auth/directory decisions for all later phases.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-poll-creation/01-01-SUMMARY.md` when done.
</output>

---
phase: 06-your-polls-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/poll-list-item.tsx
  - src/components/poll-list-item.test.tsx
  - src/components/subscribe-card.tsx
autonomous: true
requirements: [MYP-02, MYP-08]
tags: [react, rsc, presentational, badge, pluralization, subscribe]

must_haves:
  truths:
    - "PollListItem renders a next/link to /a/<adminUrlId> showing the title, an Open (neutral) or Booked (emerald) badge from poll.status, a summary, and a response count — MYP-02."
    - "Summaries pluralize exactly: '1 date' vs '{n} dates' and '1 response' vs '{n} responses'; a poll with 0 participants renders '0 responses' (never blank/hidden) — MYP-02 (★ boundary edge)."
    - "A closed poll whose winningDate is null still renders the 'Booked' badge with no date and does not crash (mirrors EP-FEED-EMPTY) — MYP-02 (★ empty edge)."
    - "An open poll renders the '{n} dates' candidate-count summary and the neutral 'Open' badge; a closed poll with a winningDate renders the formatted booked date via formatDateWithTime — MYP-02."
    - "SubscribeCard renders the same-browser guidance line so polls created from the same browser share one calendar — MYP-08."
  artifacts:
    - path: "src/components/poll-list-item.tsx"
      provides: "Presentational poll row + exported PollListRow prop type"
      exports: ["PollListItem", "PollListRow"]
      contains: "export function PollListItem"
      min_lines: 30
    - path: "src/components/poll-list-item.test.tsx"
      provides: "Component tests for badge/summary/pluralization/null-date fallback"
      contains: "PollListItem"
    - path: "src/components/subscribe-card.tsx"
      provides: "Shared subscribe card (feed URL + webcal + copy + same-browser copy)"
      exports: ["SubscribeCard"]
      contains: "export function SubscribeCard"
  key_links:
    - from: "src/components/poll-list-item.tsx"
      to: "/a/<adminUrlId>"
      via: "next/link href"
      pattern: "/a/\\$\\{.*adminUrlId"
    - from: "src/components/subscribe-card.tsx"
      to: "buildOrganizerFeedUrl / buildOrganizerWebcalUrl"
      via: "import from @/lib/urls"
      pattern: "buildOrganizerFeedUrl"
  prohibitions:
    - id: PROH-2
      statement: "PollListItem MUST NOT accept or render any participant name/email, edit_token, or participant_url_id — its prop type is exactly the 7 participant-safe columns."
      status: resolved
      verification: "source — the PollListRow type declares only adminUrlId, title, status, winningDate, winningStartTime, optionCount, responseCount; grep the type for absence of email/editToken/participantUrlId."
      check_kind: source-review
---

<objective>
Build the two presentational pieces the `/polls` dashboard composes: `PollListItem` (a poll row
— title, Open/Booked badge, summary, response count, wrapped in a link to the admin page) and
`SubscribeCard` (the shared booked-dates subscribe card carrying the new same-browser copy,
MYP-08). Both are pure/presentational (no DB, no cookies, no client JS) so they can be built and
tested in isolation, in parallel with the query plan.

Purpose: encode the pluralization + closed-null-date fallback rules (MYP-02 ★ edges) and the
same-browser guidance (MYP-08) in one place with focused component tests, so the page-wiring plan
(06-03) and the admin-swap plan (06-04) just compose tested parts.
Output: `PollListItem` + `PollListRow` type, `SubscribeCard`, and a component test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-your-polls-dashboard/06-SPEC.md
@docs/superpowers/specs/2026-07-06-your-polls-dashboard-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PollListItem component (badge + summary + response count + admin link)</name>
  <files>src/components/poll-list-item.tsx</files>
  <behavior>
    - open poll, 3 options, 2 participants → summary "3 dates", count "2 responses", neutral "Open" badge, link href "/a/<adminUrlId>".
    - open poll, 1 option, 1 participant → "1 date", "1 response" (singular).
    - open poll, 0 participants → "0 responses" (rendered, not blank).
    - closed poll with winningDate "2026-07-19" + winningStartTime "14:00:00" → emerald "Booked" badge + "Sunday, July 19 at 2:00 PM" (via formatDateWithTime), no candidate-count.
    - closed poll with winningDate null → "Booked" badge, NO date text, no crash.
  </behavior>
  <read_first>
    - src/app/a/[adminUrlId]/page.tsx (lines ~127-135 and ~207-224) — the EXACT badge markup to
      mirror: the emerald "Booked" pill (`border-emerald-300 bg-emerald-100 ... text-emerald-800`)
      and the finalized-date rendering via `formatDateWithTime(date, startTime ? startTime.slice(0,5)
      : null)`. Reuse the same Tailwind classes so the badge matches.
    - src/lib/format-date.ts — `formatDateWithTime(yyyymmdd, hhmm | null)` returns
      "Saturday, July 12 at 2:00 PM" (or date-only when hhmm null). DB startTime is 'HH:MM:SS';
      slice to 'HH:MM' before passing (mirrors the admin page).
    - src/components/copy-link-button.tsx — example of an existing small component (import style,
      "use client" NOT needed here — PollListItem is a server component with a next/link).
  </read_first>
  <action>
    Create `src/components/poll-list-item.tsx`. Export a `PollListRow` type with EXACTLY these
    fields (the participant-safe shape returned by getPollsByOrganizerId): `adminUrlId: string`,
    `title: string`, `status: string`, `winningDate: string | null`, `winningStartTime: string |
    null`, `optionCount: number`, `responseCount: number`. Export `function PollListItem({ poll }:
    { poll: PollListRow })` that renders a `next/link` with `href={`/a/${poll.adminUrlId}`}`
    wrapping: the title; a status badge — `poll.status === "closed"` → the emerald "Booked" pill
    (reuse the admin page classes), else the neutral "Open" pill; a summary — when closed AND
    `poll.winningDate` is non-null → `formatDateWithTime(poll.winningDate, poll.winningStartTime ?
    poll.winningStartTime.slice(0,5) : null)`; when closed AND winningDate is null → render the
    "Booked" badge only, NO date string (defensive fallback, no crash); when open → the candidate
    count `` `${poll.optionCount} ${poll.optionCount === 1 ? "date" : "dates"}` ``; and always a
    response count `` `${poll.responseCount} ${poll.responseCount === 1 ? "response" : "responses"}`
    ``. Pluralization compares the NUMERIC value `=== 1` (the counts arrive as JS numbers because
    the query casts COUNT to int). Keep it a server component (no "use client"); reuse the project's
    Tailwind utility styling. Do NOT accept or reference any participant name/email, edit token, or
    participant URL — the prop type is the 7-column shape only.
  </action>
  <acceptance_criteria>
    - Source: `PollListRow` type declares exactly the 7 fields; grep confirms no `email`,
      `editToken`, `participantUrlId`, `name`, or `creatorEmail` in the type.
    - Source: the link href is `/a/${poll.adminUrlId}` and the closed branch guards on
      `poll.winningDate` being non-null before calling `formatDateWithTime`.
    - Source: pluralization uses `optionCount === 1` and `responseCount === 1` numeric compares.
    - Build: `npm run build` type-checks the component.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PollListItem component tests</name>
  <files>src/components/poll-list-item.test.tsx</files>
  <read_first>
    - src/components/copy-link-button.test.tsx — the component-test harness pattern in this repo
      (vitest + render). Prefer `renderToStaticMarkup` from "react-dom/server" (as the page tests
      use) since PollListItem is a server component with no interactivity — no jsdom/user events
      needed. Confirm the import style used by the existing `.test.tsx` files.
    - src/components/poll-list-item.tsx — the component under test (its exact rendered strings).
    - src/lib/format-date.ts — expected formatter output ("Sunday, July 19 at 2:00 PM").
  </read_first>
  <action>
    Create `src/components/poll-list-item.test.tsx`. Render `PollListItem` with
    `renderToStaticMarkup(<PollListItem poll={...} />)` and assert (one case per behavior bullet):
    (a) open, optionCount 3 / responseCount 2 → html contains "3 dates", "2 responses", "Open", and
        `href="/a/<adminUrlId>"`; does NOT contain "Booked".
    (b) open, optionCount 1 / responseCount 1 → html contains "1 date" and "1 response" (assert the
        singular forms, and assert it does NOT contain "1 dates" / "1 responses").
    (c) open, responseCount 0 → html contains "0 responses" (non-blank).
    (d) closed, winningDate "2026-07-19", winningStartTime "14:00:00" → html contains "Booked" and
        "Sunday, July 19 at 2:00 PM"; does NOT contain "dates" (no candidate count when closed).
    (e) closed, winningDate null → html contains "Booked" and the render does NOT throw and contains
        no "null"/"Invalid" text and no candidate-count.
    Use a fixed `adminUrlId` literal per case and assert the exact `/a/<id>` href substring.
  </action>
  <acceptance_criteria>
    - Test-command: `npx vitest run src/components/poll-list-item.test.tsx` passes (no DATABASE_URL
      needed — pure component test).
    - Behavior: the singular case (b) asserts BOTH the presence of "1 date"/"1 response" and the
      ABSENCE of "1 dates"/"1 responses".
    - Behavior: the null-winningDate case (e) proves the render does not throw (the test itself
      completing green is the proof) and asserts "Booked" is present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SubscribeCard shared component with same-browser copy (MYP-08)</name>
  <files>src/components/subscribe-card.tsx</files>
  <read_first>
    - src/app/a/[adminUrlId]/page.tsx (lines ~277-311) — the EXACT subscribe-card markup to
      extract verbatim: the Card, the "Subscribe to your booked-dates calendar" heading, the
      "Add this once ..." copy, the group-shareable-link note, the `buildOrganizerFeedUrl` mono URL,
      the `buildOrganizerWebcalUrl` "Subscribe in calendar" anchor, and the `CopyLinkButton`. It is
      NEUTRAL severity — NO amber border, NO "Keep private" badge. Preserve every asserted string so
      the admin render test (06-04) still passes after the swap.
    - src/lib/urls.ts (lines ~40-57) — `buildOrganizerFeedUrl(base, organizerId)` and
      `buildOrganizerWebcalUrl(base, organizerId)` signatures.
    - src/components/ui/card.tsx — the `Card` export. src/components/copy-link-button.tsx — the
      `CopyLinkButton` export ({ url, label }).
  </read_first>
  <action>
    Create `src/components/subscribe-card.tsx`. Export `function SubscribeCard({ base, organizerId }:
    { base: string; organizerId: string })` that renders the exact neutral subscribe Card currently
    inlined in the admin page (heading "Subscribe to your booked-dates calendar", the "Add this once
    to your phone/desktop calendar; every poll you finalize appears automatically." copy, the
    group-shareable-link note, `buildOrganizerFeedUrl(base, organizerId)` as the mono URL, the
    `buildOrganizerWebcalUrl(base, organizerId)` "Subscribe in calendar" anchor, and a
    `CopyLinkButton` with the feed URL). ADD one new line of guidance (MYP-08) — the same-browser
    copy, e.g. "Create your polls from the same browser to keep them all in one calendar." — as a
    muted text line inside the card. Keep it NEUTRAL: no amber border, no "Keep private" badge. The
    component takes `base` + `organizerId` as props (no cookies/DB read inside — callers supply
    them). Do NOT change the admin page in this plan (06-04 swaps it to use this component).
  </action>
  <acceptance_criteria>
    - Source: `SubscribeCard` renders the strings "Subscribe to your booked-dates calendar" and the
      same-browser guidance sentence, and calls `buildOrganizerFeedUrl` + `buildOrganizerWebcalUrl`
      with `(base, organizerId)`.
    - Source: the component contains NO "Keep private" text and no amber-border class (neutral
      severity preserved).
    - Build: `npm run build` type-checks the component.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → rendered HTML | PollListItem/SubscribeCard render only caller-supplied, participant-safe props; no direct DB/cookie access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | PollListItem prop type widening | mitigate | `PollListRow` is exactly the 7 participant-safe columns; it structurally cannot carry a name/email/edit-token/participant-URL (source-review, PROH-2). |
| T-06-10 | Information Disclosure | SubscribeCard embeds the organizer feed token | accept | The feed link is an intended group-shareable bearer link (Phase 4 / LD-7) exposing only booked dates + titles, no participant data — neutral severity by design. Callers gate its rendering (06-03/06-04). |
| T-06-SC | Tampering | supply chain (npm installs) | accept | N/A this phase — no new package installs. |
</threat_model>

<verification>
- `npx vitest run src/components/poll-list-item.test.tsx` — all cases green (no DB needed).
- `npm run build` — both components type-check; `PollListRow` is the 7-column shape.
- Manual source-review: SubscribeCard preserves every subscribe-card string the admin render test
  asserts, and adds the same-browser line; it carries no "Keep private" badge/amber border.
</verification>

<success_criteria>
- `PollListItem` + `PollListRow` exist; badge, summary, pluralization (singular/plural + 0), and
  the closed-null-winningDate "Booked, no date" fallback all render correctly and are tested.
- `SubscribeCard` exists, reproduces the neutral subscribe card, and adds the same-browser copy
  (MYP-08).
</success_criteria>

<output>
Create `.planning/phases/06-your-polls-dashboard/06-02-SUMMARY.md` when done.
</output>

---
phase: 06-your-polls-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/app/a/[adminUrlId]/page.tsx
  - src/app/a/[adminUrlId]/page.test.ts
  - src/app/page.tsx
  - src/app/page.test.ts
autonomous: true
requirements: [MYP-06, MYP-08]
tags: [nextjs, rsc, cookies, entry-points, subscribe]

must_haves:
  truths:
    - "The admin page /a/[adminUrlId] renders a 'Your polls' link (→ /polls) and a 'Create a poll' link (→ /) — MYP-06."
    - "The landing page / renders a 'Your polls' link (→ /polls) ONLY when the lfg_organizer cookie is present, and omits it when the cookie is absent/empty — MYP-06."
    - "The admin page's subscribe card (rendered only for a poll with an organizerId) shows the same-browser guidance via the shared SubscribeCard — MYP-08."
  artifacts:
    - path: "src/app/a/[adminUrlId]/page.tsx"
      provides: "Entry links (/polls, /) + subscribe card swapped to shared SubscribeCard"
      contains: "SubscribeCard"
    - path: "src/app/page.tsx"
      provides: "Cookie-gated 'Your polls' link on the landing page"
      contains: "lfg_organizer"
    - path: "src/app/a/[adminUrlId]/page.test.ts"
      provides: "Assertions for the two entry links + same-browser subscribe copy"
      contains: "/polls"
    - path: "src/app/page.test.ts"
      provides: "Cookie-present-shows / cookie-absent-hides the 'Your polls' link"
      contains: "/polls"
  key_links:
    - from: "src/app/a/[adminUrlId]/page.tsx"
      to: "/polls and /"
      via: "next/link entry links"
      pattern: "/polls"
    - from: "src/app/page.tsx"
      to: "lfg_organizer cookie"
      via: "cookies().get('lfg_organizer')"
      pattern: "lfg_organizer"
  prohibitions:
    - id: PROH-3
      statement: "MUST NOT serve the cookie-conditional 'Your polls' link on / from a cache shared across organizers — reading the cookie must force per-cookie dynamic render."
      status: resolved
      verification: "source — / reads cookies() (forces dynamic in Next 16) and exports NO `dynamic = 'force-static'` and NO `revalidate`; the link is a static /polls path (no token embedded)."
      check_kind: source-review
---

<objective>
Add the entry points and finalize the copy: a "Your polls" (→ `/polls`) and "Create a poll"
(→ `/`) link row on the admin page (MYP-06), a cookie-gated "Your polls" link on the landing page
(MYP-06), and swap the admin page's inline subscribe card to the shared `SubscribeCard` (06-02) so
the same-browser copy (MYP-08) appears on both `/polls` and admin from a single source.

Purpose: make `/polls` discoverable from a poll's admin page and from the landing page (for a
returning organizer), and guarantee the same-browser guidance can't diverge between surfaces.
Output: entry links + the SubscribeCard swap + test updates on admin and landing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-your-polls-dashboard/06-SPEC.md
@docs/superpowers/specs/2026-07-06-your-polls-dashboard-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin page — entry links + swap inline subscribe card to shared SubscribeCard</name>
  <files>src/app/a/[adminUrlId]/page.tsx</files>
  <read_first>
    - src/app/a/[adminUrlId]/page.tsx — the file being modified. Lines ~122-136 (the header block
      with `<h1>{poll.title}</h1>`) is where a small top link row goes ABOVE the title. Lines
      ~277-311 are the inline subscribe Card to REPLACE with `<SubscribeCard base={base}
      organizerId={poll.organizerId} />` (keep the existing `poll.organizerId ? (...) : null` guard
      so legacy null-organizer polls still hide it). The file is documented as "PURE layout" —
      preserve every query/branch; this change is add-links + component-swap only.
    - src/components/subscribe-card.tsx — the shared `SubscribeCard({ base, organizerId })` from
      06-02 (renders the same neutral card + the new same-browser line).
  </read_first>
  <action>
    (1) Add a small link row ABOVE the `<h1>` title (inside the header `div`, or as its own row at
    the top of `main`): two `next/link`s — "Your polls" → `/polls` and "Create a poll" → `/`.
    Import `Link` from "next/link". Style them as small muted text links (match the app's existing
    link styling); they are navigation, not primary buttons. (2) Import `SubscribeCard` from
    `@/components/subscribe-card` and REPLACE the inline subscribe Card markup (the block starting
    at "Subscribe to your booked-dates calendar") with `<SubscribeCard base={base} organizerId=
    {poll.organizerId} />`, keeping the surrounding `{poll.organizerId ? (...) : null}` guard so a
    legacy null-organizer poll renders NO subscribe card. Remove now-unused imports if the swap
    leaves `buildOrganizerFeedUrl`/`buildOrganizerWebcalUrl` unreferenced elsewhere in the file
    (check before deleting). Do NOT change the Results/Book-it/share-link logic.
  </action>
  <acceptance_criteria>
    - Source: the admin page renders `next/link` to `/polls` and to `/` above the title.
    - Source: the inline subscribe-card markup is replaced by `<SubscribeCard .../>` and the
      `poll.organizerId ? ... : null` guard is preserved (legacy polls still hide it).
    - Source: no duplicate subscribe-card markup remains; unused url-builder imports are cleaned up.
    - Build: `npm run build` type-checks the admin route.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Landing page — cookie-gated 'Your polls' link</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx — the file being modified. Currently a SYNC static component
      (`export default function Home()`). It must become `async` and read the cookie.
    - src/app/p/[participantUrlId]/page.tsx (lines ~50-51) — the `const cookieStore = await
      cookies();` / `cookieStore.get("lfg_organizer")?.value` pattern. Import `cookies` from
      "next/headers".
  </read_first>
  <action>
    Convert `Home` to `export default async function Home()`. Read `const cookieStore = await
    cookies();` and `const hasOrganizer = Boolean(cookieStore.get("lfg_organizer")?.value?.trim());`
    (treat empty/whitespace as absent, mirroring create-poll). When `hasOrganizer`, render a "Your
    polls" `next/link` → `/polls` (small muted link, e.g. above or beside the "Create a poll"
    heading); when not, render nothing there (first-time visitors see no dead link). Import `Link`
    from "next/link" and `cookies` from "next/headers". Do NOT add `export const dynamic`/
    `revalidate` — reading cookies() forces the (tiny) page dynamic, which is intended (PROH-3);
    the link is the static `/polls` path with no token embedded.
  </action>
  <acceptance_criteria>
    - Source: `Home` is `async`, reads `cookies()` and `lfg_organizer`, and conditionally renders a
      `/polls` link on `hasOrganizer` (empty/whitespace treated as absent).
    - Source: the file contains NO `export const dynamic` and NO `export const revalidate`; the
      `/polls` link carries no token.
    - Build: `npm run build` type-checks the landing route (now dynamic).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Test updates — admin entry links + same-browser copy; landing cookie gate</name>
  <files>src/app/a/[adminUrlId]/page.test.ts, src/app/page.test.ts</files>
  <read_first>
    - src/app/a/[adminUrlId]/page.test.ts — the existing admin render harness (lines ~130-278). The
      subscribe-card test at lines ~260-270 asserts the feed URL + "Subscribe to your booked-dates
      calendar" + exactly one "Keep private"; keep those passing. `next/headers` is already mocked
      (headers only, no cookies) — entry links need no cookie.
    - src/app/p/[participantUrlId]/page.test.ts (lines ~24-38) — the controllable-cookie
      `next/headers` mock (cookies + headers) to copy into the NEW landing test.
    - src/app/page.tsx and src/app/a/[adminUrlId]/page.tsx — the strings to assert.
  </read_first>
  <action>
    (A) In `src/app/a/[adminUrlId]/page.test.ts`: add assertions to an existing render case (or a
    new `it`) that the admin HTML contains a link to `/polls` and a link to `/` (the two entry
    links, MYP-06). In the WITH-organizerId subscribe-card test, add an assertion that the HTML now
    contains the same-browser guidance sentence (MYP-08) — while preserving the existing feed-URL,
    "Subscribe to your booked-dates calendar", and single "Keep private" assertions.
    (B) Create `src/app/page.test.ts`: mock `next/headers` with a controllable `mockCookieValue`
    (cookies().get returns `{ value }` when set, else undefined; headers() returns a host map).
    Import the default `Home`, render via `renderToStaticMarkup(await Home())`. Case 1: cookie set
    (`mockCookieValue = "some-token"`) → HTML contains a `/polls` link and "Your polls". Case 2:
    cookie absent (`mockCookieValue = undefined`) → HTML does NOT contain a `/polls` link. Case 3:
    empty/whitespace cookie (`mockCookieValue = "   "`) → treated as absent, no `/polls` link. No
    DB needed for the landing test.
  </action>
  <acceptance_criteria>
    - Test-command: `DATABASE_URL=$DATABASE_URL npx vitest run src/app/a/[adminUrlId]/page.test.ts`
      passes, including the new `/polls` + `/` entry-link and same-browser-copy assertions and all
      pre-existing subscribe-card assertions.
    - Test-command: `npx vitest run src/app/page.test.ts` passes (no DB): cookie-set shows the
      `/polls` link, cookie-absent and whitespace-cookie hide it.
    - Behavior: the landing test asserts the ABSENCE of the `/polls` link in the no-cookie and
      whitespace-cookie cases (not just presence in the cookie-set case).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cookie → / RSC | the landing page reads `lfg_organizer` only to decide whether to show a static `/polls` link; no token is embedded and no data is scoped by it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | landing `/` cookie-conditional link cached across cookies | mitigate | Reading cookies() forces per-cookie dynamic render in Next 16; no `force-static`/`revalidate` export (PROH-3, source-review). |
| T-06-09 | Information Disclosure | entry links leaking a token in the URL | mitigate | Both links are static paths (`/polls`, `/`) — no admin/participant/organizer token in the href (source-review). |
| T-06-12 | Tampering | admin subscribe-card swap silently drops a leak guard | mitigate | SubscribeCard is the neutral card only (no "Keep private"/amber); the `poll.organizerId ?` guard is preserved so legacy polls still hide it — asserted by the existing admin subscribe-card tests. |
| T-06-SC | Tampering | supply chain (npm installs) | accept | N/A this phase — no new package installs. |
</threat_model>

<verification>
- `DATABASE_URL` exported (Docker Postgres `lfg-db-1` :5432) for the admin render test.
- `DATABASE_URL=$DATABASE_URL npx vitest run src/app/a/[adminUrlId]/page.test.ts` — green
  (entry links + same-browser copy + all prior assertions).
- `npx vitest run src/app/page.test.ts` — green (cookie gate both directions).
- `npm run build` — admin + landing routes type-check; landing is now dynamic.
</verification>

<success_criteria>
- Admin page shows "Your polls" (→ /polls) and "Create a poll" (→ /) links and uses the shared
  SubscribeCard (same-browser copy, legacy-null guard preserved).
- Landing page shows the "Your polls" link only when the organizer cookie is present.
- All new and pre-existing admin/landing tests pass.
</success_criteria>

<output>
Create `.planning/phases/06-your-polls-dashboard/06-04-SUMMARY.md` when done.
</output>

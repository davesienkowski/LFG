---
phase: 06-your-polls-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/app/polls/page.tsx
  - src/app/polls/page.test.ts
autonomous: true
requirements: [MYP-01, MYP-02, MYP-03, MYP-04]
tags: [nextjs, rsc, cookies, dashboard, no-oracle, no-leak]

must_haves:
  truths:
    - "/polls reads the lfg_organizer cookie via next/headers cookies() and calls getPollsByOrganizerId; with a cookie + ≥1 owned poll it renders one PollListItem per poll (newest-first), each linking to /a/<adminUrlId> — MYP-01."
    - "With no cookie, an empty/whitespace cookie, or an unknown organizer (zero polls), /polls returns HTTP 200 with an identical empty state (heading + 'Create a poll' link → /) — never a 404/throw, and no status distinguishes 'no cookie' from 'unknown organizer' — MYP-03."
    - "Exactly at the 0↔1 boundary: 0 owned polls → the empty state; 1 owned poll → the list with exactly one row (0↔1 threshold, covered by MYP-01 empty) — MYP-03."
    - "Each rendered row shows the title, an Open/Booked badge, the booked-date-or-'{n} dates' summary, and the '{n} responses' count — MYP-02."
    - "The rendered /polls HTML contains no participant name/email, no edit_token, and no participant_url_id — MYP-04."
  artifacts:
    - path: "src/app/polls/page.tsx"
      provides: "The 'Your polls' dashboard RSC (cookie → query → list/empty state + subscribe card)"
      contains: "getPollsByOrganizerId"
      min_lines: 30
    - path: "src/app/polls/page.test.ts"
      provides: "Render tests: list, empty state (no cookie == unknown), no-leak, no-oracle, dynamic"
      contains: "polls"
  key_links:
    - from: "src/app/polls/page.tsx"
      to: "lfg_organizer cookie"
      via: "cookies().get('lfg_organizer')"
      pattern: "lfg_organizer"
    - from: "src/app/polls/page.tsx"
      to: "getPollsByOrganizerId"
      via: "import from @/lib/db/queries"
      pattern: "getPollsByOrganizerId"
    - from: "src/app/polls/page.tsx"
      to: "PollListItem / SubscribeCard"
      via: "import from @/components"
      pattern: "PollListItem"
  prohibitions:
    - id: PROH-2
      statement: "MUST NOT emit any participant name/email, edit_token, or participant_url_id anywhere on /polls."
      status: resolved
      verification: "node-test — seed a poll with a canary participant name+email; assert the /polls HTML contains none of them, nor any /p/ participant URL or edit token, while the poll DID render (non-vacuous)."
      check_kind: node-test
    - id: PROH-3
      statement: "MUST NOT serve /polls from a cache shared across organizers — the per-cookie output must be dynamic/uncacheable."
      status: resolved
      verification: "source — /polls reads cookies() (forces dynamic render in Next 16) and exports NO `dynamic = 'force-static'` and NO `revalidate` that would cache per-cookie output."
      check_kind: source-review
    - id: PROH-4
      statement: "MUST NOT render the SubscribeCard (or otherwise embed the organizer feed token) in the /polls empty state; the subscribe card renders ONLY when the organizer has ≥1 poll."
      status: resolved
      verification: "node-test — assert the no-cookie empty state and the unknown-organizer (cookie set, zero polls) empty state are identical AND contain no '/feed/' URL and no organizer-token substring; assert SubscribeCard/'Subscribe to your booked-dates calendar' renders only in the ≥1-poll case."
      check_kind: node-test
---

<objective>
Build `/polls` — the "Your polls" dashboard RSC. It reads the `lfg_organizer` cookie, calls
`getPollsByOrganizerId` (06-01), and renders either the list of `PollListItem` rows (06-02) plus
the `SubscribeCard` (06-02) at the top, or an identical no-oracle empty state. This wires the
tested query + tested components into the page and adds the render tests that prove the no-leak
(MYP-04), no-oracle (MYP-03), and dynamic/uncacheable (PROH-3) contracts.

Purpose: give the organizer one place to see every poll they created from this browser and a
clear path to create another — the phase goal — without leaking participant data or caching
per-cookie output.
Output: `src/app/polls/page.tsx` + `src/app/polls/page.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-your-polls-dashboard/06-SPEC.md
@docs/superpowers/specs/2026-07-06-your-polls-dashboard-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /polls dashboard page (cookie → query → list/empty state + subscribe card)</name>
  <files>src/app/polls/page.tsx</files>
  <read_first>
    - src/app/p/[participantUrlId]/page.tsx (lines ~40-63) — the cookie-read pattern:
      `const cookieStore = await cookies();` then `cookieStore.get("<name>")?.value`. Reading
      cookies() forces dynamic rendering in Next 16 (this is how PROH-3 is satisfied — no explicit
      `dynamic` export needed).
    - src/app/a/[adminUrlId]/page.tsx (lines ~103-106, 122-123) — how to build `base` from
      headers() via `resolveBaseUrl(h.get("host"), h.get("x-forwarded-proto"))`, and the
      `main.mx-auto ... max-w-6xl` layout shell to mirror.
    - src/lib/db/queries.ts — the NEW `getPollsByOrganizerId` (from 06-01): returns the 7-column
      participant-safe rows; returns [] for empty/whitespace/unknown organizer.
    - src/components/poll-list-item.tsx — `PollListItem` + `PollListRow` (from 06-02).
    - src/components/subscribe-card.tsx — `SubscribeCard({ base, organizerId })` (from 06-02).
    - src/lib/urls.ts — `resolveBaseUrl`.
  </read_first>
  <action>
    Create `src/app/polls/page.tsx` as an async default-export RSC. Read the cookie:
    `const cookieStore = await cookies();` and `const rawOrganizer = cookieStore.get("lfg_organizer")
    ?.value;`. Normalize like create-poll: `const organizerId = rawOrganizer && rawOrganizer.trim()
    ? rawOrganizer : undefined;`. Fetch rows: `const polls = organizerId ? await
    getPollsByOrganizerId(organizerId) : [];` (getPollsByOrganizerId also returns [] for
    empty/whitespace, but skip the call when absent). Render a `main` shell with an `<h1>Your polls
    </h1>` and a "Create a poll" link (`next/link` → `/`) in the header row. THEN branch on
    `polls.length`: (empty) when 0 → render the empty state ONLY: a heading such as "You haven't
    created any polls yet" + a "Create a poll" link → `/`; do NOT render the SubscribeCard and do
    NOT read/emit the organizer token here (PROH-4 / MYP-03 no-oracle — the empty state must be
    identical whether the cookie is absent or unknown). (populated) when ≥1 → build `base` from
    `headers()` + `resolveBaseUrl`, render `<SubscribeCard base={base} organizerId={organizerId!}
    />` at the top (organizerId is defined in this branch), then map `polls` to `<PollListItem
    key={poll.adminUrlId} poll={poll} />`. Do NOT add `export const dynamic`/`revalidate` — reading
    cookies() already forces dynamic; an explicit force-static/revalidate would violate PROH-3. Do
    NOT call notFound() and do NOT throw for the no-cookie/unknown case (always 200). Do NOT render
    or resolve any participant name/email/edit-token/participant-URL (the query shape already
    excludes them; keep the page free of any other participant query).
  </action>
  <acceptance_criteria>
    - Source: the page calls `cookies()` and reads `lfg_organizer`; it normalizes empty/whitespace
      to absent before/around the query call.
    - Source: the empty (0-poll) branch renders NO `SubscribeCard` and no `buildOrganizerFeedUrl`/
      `/feed/` reference; the populated branch renders `SubscribeCard` + `PollListItem` map.
    - Source: the file contains NO `export const dynamic` and NO `export const revalidate`.
    - Source: the page imports only `getPollsByOrganizerId` from queries (no participant/vote/edit
      query import).
    - Build: `npm run build` type-checks the route.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: /polls render tests (list, empty no-oracle, no-leak, dynamic)</name>
  <files>src/app/polls/page.test.ts</files>
  <read_first>
    - src/app/p/[participantUrlId]/page.test.ts (lines ~24-38) — the `next/headers` mock pattern
      with a controllable cookie: a module-level `let mockCookieValue` and `vi.mock("next/headers",
      () => ({ cookies: async () => ({ get: () => mockCookieValue !== undefined ? { value:
      mockCookieValue } : undefined }), headers: async () => new Map([["host", "lfg.test"]]) }))`.
      Set `mockCookieValue` per test.
    - src/app/a/[adminUrlId]/page.test.ts (lines ~6-27, 105-128) — the DB-backed render harness:
      `renderToStaticMarkup`, DATABASE_URL guard in beforeAll, `createdAdminIds`/`inArray` cleanup
      in afterAll, direct-insert seeding of polls/options/participants/votes with `generateToken()`.
    - src/app/polls/page.tsx — the page under test.
  </read_first>
  <action>
    Create `src/app/polls/page.test.ts`. Mock `next/headers` per the participant-page pattern so the
    cookie is controllable via a module-level `mockCookieValue`. Import the default `PollsPage` and
    render via `renderToStaticMarkup(await PollsPage(...))`. Seed via direct inserts (track ids,
    clean up in afterAll). Cases:
    (a) MYP-01/02 populated — seed an organizer with two polls (one open w/ options + participants,
        one closed w/ a winning option); set mockCookieValue to that organizer; assert the HTML
        contains each poll's title and `href="/a/<adminUrlId>"`, an "Open" and a "Booked" badge, a
        "{n} dates" and a booked-date summary, and "{n} responses"; assert newest-first ordering by
        checking the first-seeded-newest title appears before the older one (indexOf compare).
    (b) MYP-03 no-cookie empty state — set mockCookieValue = undefined; assert status is a normal
        render (no throw), the HTML contains the empty-state heading + a "Create a poll" link to
        "/", and does NOT contain "Subscribe to your booked-dates calendar" or "/feed/".
    (c) MYP-03/PROH-4 no-oracle — render the no-cookie empty state AND the unknown-organizer empty
        state (mockCookieValue = a fresh `generateToken()` with zero polls); assert the two rendered
        HTML strings are byte-identical and neither contains "/feed/" nor the token substring.
    (d) MYP-03 0↔1 boundary — seed exactly ONE poll under an organizer; assert the list renders
        exactly one `/a/<adminUrlId>` row (not the empty state); (the 0 side is covered by case b/c).
    (e) PROH-2 no-leak — seed a poll under the organizer with a canary participant name + email;
        render; assert the HTML contains the poll title (non-vacuous: it DID render) but none of the
        canary name, canary email, the participant's edit token, or a "/p/<participantUrlId>" URL.
    (f) PROH-3 dynamic — a source-level assertion: read the page source and assert it contains
        `cookies(` and does NOT contain `force-static` or `export const revalidate` (a lightweight
        guard that the route stays per-cookie dynamic).
  </action>
  <acceptance_criteria>
    - Test-command: `DATABASE_URL=$DATABASE_URL npx vitest run src/app/polls/page.test.ts` passes.
    - Behavior: case (c) asserts the no-cookie and unknown-organizer empty-state HTML are strictly
      equal AND both omit "/feed/" and the organizer token (no-oracle + PROH-4).
    - Behavior: case (e) is non-vacuous — it asserts the poll title IS present while the canary
      name/email/edit-token/participant-URL are absent.
    - Behavior: case (a) asserts newest-first ordering via an indexOf position comparison of the two
      titles, and asserts both an "Open" and a "Booked" badge render.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser cookie → /polls RSC | `lfg_organizer` scopes the read; a random/absent value must yield an identical empty page (no oracle). |
| /polls RSC → response cache | per-cookie output must never be shared across organizers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | /polls rendered HTML | mitigate | Renders only participant-safe columns via PollListItem; canary no-leak render test (PROH-2). |
| T-06-05 | Information Disclosure | per-cookie output served from a shared cache | mitigate | Reads cookies() (dynamic in Next 16); no `force-static`/`revalidate` export — source-review (PROH-3). |
| T-06-06 | Information Disclosure | 404/error oracle distinguishing real vs unknown organizer | mitigate | Always 200; identical empty state for absent and unknown cookies (MYP-03) — asserted by the byte-identical no-oracle test. |
| T-06-11 | Information Disclosure | subscribe card token embedded in the empty state | mitigate | Empty state renders no SubscribeCard and no feed token (PROH-4) — asserted by the no-oracle test. |
| T-06-SC | Tampering | supply chain (npm installs) | accept | N/A this phase — no new package installs. |
</threat_model>

<verification>
- `DATABASE_URL` exported (Docker Postgres `lfg-db-1` :5432) before running the render test.
- `DATABASE_URL=$DATABASE_URL npx vitest run src/app/polls/page.test.ts` — all cases green.
- `npm run build` — the `/polls` route type-checks and builds; confirm the route is dynamic
  (reads cookies(), no force-static).
</verification>

<success_criteria>
- `/polls` lists the organizer's owned polls newest-first (linking to admin pages) with the
  subscribe card on top, or an identical 200 no-oracle empty state.
- The rendered HTML leaks no participant data; the empty state embeds no organizer token; the route
  is per-cookie dynamic (no shared cache).
</success_criteria>

<output>
Create `.planning/phases/06-your-polls-dashboard/06-03-SUMMARY.md` when done.
</output>

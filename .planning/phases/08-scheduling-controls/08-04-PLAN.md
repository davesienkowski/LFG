---
phase: 08-scheduling-controls
plan: 04
type: execute
wave: 4
depends_on: ["08-02", "08-03"]
files_modified: []
autonomous: false
requirements: [DEAD-01, ORG-01]
user_setup: []
must_haves:
  truths:
    - "Prod Neon is backed up (pg18 client, stored outside the repo) BEFORE the migration runs."
    - "The additive 0006 migration (deadline on polls, is_organizer on participants) is applied to prod Neon and both columns exist in prod."
    - "The current build is deployed to prod via the Vercel CLI (git push does NOT auto-deploy)."
    - "A human confirms on the live prod app that a passed deadline makes the participant form read-only with the 'Voting has closed' copy, and that the organizer's own availability row appears '(you)' in the results grid."
  artifacts:
    - path: "/home/dave/lfg-db-backups/"
      provides: "Timestamped prod Neon dump taken immediately before the 0006 migration"
  key_links:
    - from: "prod Neon"
      to: "polls.deadline + participants.is_organizer"
      via: "npm run db:migrate against the prod DATABASE_URL pulled by vercel env pull"
      pattern: "deadline|is_organizer"
  prohibitions:
    - "MUST NOT run db:migrate against prod before a verified non-empty backup exists in /home/dave/lfg-db-backups/."
    - "MUST NOT deploy before the prod migration succeeds (additive migration first so the old serverless build tolerates the new nullable columns, then ship the new code)."
    - "MUST NOT close the phase until the human confirms the deadline auto-close read-only state and the organizer '(you)' row on the live prod app (the executor cannot fully self-verify the time-dependent auto-close or the visual)."
---

<objective>
Ship Phase 8 to production: back up prod Neon, apply the additive 0006 migration
(deadline + is_organizer), deploy the new build with the Vercel CLI, and get a
human to confirm on the live app that a passed deadline closes voting read-only
and the organizer's own availability row shows "(you)" in the results grid. This
is the established v1.0/Phase-7 prod pattern (STATE blocker "[v1.1 prod
migration]") plus the carried end-of-phase human check for behavior the executor
cannot fully self-verify (time-dependent lazy auto-close + the visual).

Purpose: The features aren't real until prod has both columns and the deadline
auto-close + organizer row work on the deployed app. Isolated as its own plan
because it mixes a self-serve prod operation with a blocking human-verify
checkpoint (kept separate from the autonomous code plans).

Output: prod migrated (0006) + deployed via the Vercel CLI; human sign-off that
deadline auto-close and the organizer "(you)" row behave on prod.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-scheduling-controls/08-CONTEXT.md
@.planning/phases/08-scheduling-controls/08-01-SUMMARY.md
@.planning/phases/08-scheduling-controls/08-02-SUMMARY.md
@.planning/phases/08-scheduling-controls/08-03-SUMMARY.md
@.planning/phases/07-respondent-tracking-nudges/07-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Back up prod Neon, apply the 0006 migration, deploy (self-serve)</name>
  <files>(no repo files — prod operations only: vercel env pull, docker pg18 backup, db:migrate, vercel deploy)</files>
  <read_first>
    - .planning/STATE.md (the "[v1.1 prod migration]" blocker: backup → migrate → deploy sequence, pg18 client caveat, backup dir /home/dave/lfg-db-backups/, deploy command)
    - .planning/phases/07-respondent-tracking-nudges/07-04-PLAN.md (the exact Phase-7 prod-ship procedure this mirrors, step for step)
    - drizzle/0006_*.sql (the additive migration produced in plan 01 — confirm it is ADD COLUMN only: deadline on polls, is_organizer on participants, no ALTER/DROP of existing columns)
  </read_first>
  <action>
    Self-serve prod ship per project MEMORY (prod-migrate-self-serve; NOT gated as a
    human action). Strict ordering — do NOT reorder:

    1. Pull prod creds: `npx vercel env pull` (or `npx vercel env pull .env.prod`) to
       obtain the prod `DATABASE_URL` (CLI is pre-authed per MEMORY). Do NOT print
       secrets to logs.
    2. BACKUP FIRST (hard precondition): dump prod Neon with a pg18 client (`docker
       run` the `postgres:18` image so the client version matches Neon — local
       pg_dump 17 mismatches per MEMORY prod-db-backup) to a timestamped file under
       `/home/dave/lfg-db-backups/` (OUTSIDE the repo). Confirm the dump file is
       non-empty before continuing — a 0-byte dump means the backup FAILED; STOP and
       do not migrate.
    3. Migrate prod: run `npm run db:migrate` with the prod `DATABASE_URL` exported.
       Confirm both new columns now exist in prod (query information_schema.columns
       for polls.deadline and participants.is_organizer). The migration is additive
       (two ADD COLUMN, both nullable/defaulted), so the pre-deploy prod build simply
       ignores the new columns.
    4. Deploy: `npx vercel@latest deploy --prod --yes` (git push does NOT auto-deploy
       per MEMORY deploy-tooling; the MCP deploy tool is a no-op). Capture the
       deployment URL.
    5. Smoke-check the deployed admin page loads and the NEW controls render: the
       "Voting deadline" card and the "Your availability" card appear on an open
       poll's `/a/{adminUrlId}` (empty/unset states acceptable).

    If any step fails, STOP and surface the failure — never deploy on a failed
    migration; never migrate on a failed backup.
  </action>
  <acceptance_criteria>
    - source: STATE "[v1.1 prod migration]" blocker; MEMORY prod-migrate-self-serve, prod-db-backup, deploy-tooling; Phase-7 07-04 precedent.
    - behavior: a non-empty timestamped backup exists before migration; prod has both polls.deadline and participants.is_organizer after migration; the new build is live via the Vercel CLI and renders both new admin cards.
    - test: the backup file exists and is non-empty in /home/dave/lfg-db-backups/; prod information_schema shows deadline + is_organizer; `vercel deploy --prod` reports a successful production deployment; the deployed admin page returns 200 and renders the Voting deadline + Your availability cards.
  </acceptance_criteria>
  <verify>
    <human-check>Confirm: (1) a non-empty prod backup file was written under /home/dave/lfg-db-backups/ before migrating; (2) polls.deadline and participants.is_organizer exist in prod Neon; (3) the Vercel production deploy succeeded and the admin page renders the Voting deadline + Your availability cards.</human-check>
  </verify>
  <done>Prod Neon backed up, the additive 0006 migration applied to prod, and the new build deployed via the Vercel CLI — in that order, with each step verified before the next.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify deadline auto-close + organizer "(you)" row on prod</name>
  <files>(no repo files — human verifies the deployed prod app in a real browser)</files>
  <what-built>
    DEAD-01 + ORG-01 in production: on `/a/{adminUrlId}` the organizer can set an
    optional voting deadline (which lazily closes voting once it passes, with no
    cron) and can add/edit their own availability row that appears "(you)" in the
    results grid and best-day tally. The deadline auto-close is time-dependent and
    the read-only transition + the visual "(you)" label are best verified on the
    live app by a human (the executor cannot advance wall-clock time or eyeball the
    prod render).
  </what-built>
  <how-to-verify>
    On the deployed prod app:
    1. Open (or create) a poll's admin page `/a/{adminUrlId}`. In the "Voting
       deadline" card set a deadline a FEW MINUTES in the future and Save — confirm the
       copy reads "Voting closes {your local date/time}" and NO amber pill yet.
    2. In the "Your availability" card click "Add your availability", mark every
       candidate date, and Save my availability — confirm the card collapses to
       "Added — showing as "You"…" and that a "You (you)" row now appears in the
       Results grid and is counted in the best-day summary.
    3. Open the PARTICIPANT link `/p/{participantUrlId}` in another tab and confirm you
       can still vote (deadline not yet passed).
    4. Wait for the deadline to pass (or set one ~1 minute out). Reload the participant
       link — confirm the form is now READ-ONLY with the heading "Voting has closed"
       and body "The voting deadline has passed. Contact the organizer…" (NOT the
       generic "organizer has closed this poll" copy).
    5. Reload the admin page — confirm the amber "Voting closed — deadline passed" pill
       shows next to the title, the "Your availability" card is now read-only (no
       editable grid), and the poll is NOT shown as "Booked" (no emerald pill).
    6. Click "Book it" and finalize a date — confirm the emerald "Booked" pill now
       shows and the amber deadline pill is GONE (mutual exclusivity), and the
       finalized card + booked date render as before.
  </how-to-verify>
  <action>
    HUMAN-ONLY verification (the executor cannot advance time nor eyeball the prod
    render): the owner performs the steps in <how-to-verify> on the deployed prod app
    and confirms (a) a future deadline shows "Voting closes …" with no pill; (b) the
    organizer's own row appears "(you)" in Results and counts toward best-day; (c)
    after the deadline passes the participant form is read-only with the distinct
    "Voting has closed" copy and the admin shows the amber deadline-passed pill with the
    availability card read-only; (d) booking afterward shows the emerald Booked pill and
    the amber pill disappears (mutual exclusivity). The executor performs NO automation
    here — Task 1 already shipped prod. If any behavior is wrong (e.g. the deadline pill
    and Booked pill both show, or the participant sees the generic closed copy), the
    owner describes the failure and the executor opens a gap rather than closing the
    phase.
  </action>
  <resume-signal>Type "approved" once the deadline auto-close read-only state, the distinct "Voting has closed" copy, the organizer "(you)" row + best-day count, and the Booked-xor-deadline-passed pill exclusivity all behaved as described; otherwise describe what failed.</resume-signal>
  <verify>
    <human-check>On prod: a future deadline shows "Voting closes …"; the organizer's own row shows "(you)" and counts toward best-day; after the deadline passes the participant form is read-only with the "Voting has closed" copy and the admin shows the amber deadline-passed pill (availability card read-only); booking then shows the emerald Booked pill with the amber pill gone.</human-check>
  </verify>
  <done>The owner has confirmed on prod that deadline lazy auto-close, the distinct deadline-passed participant copy, the organizer "(you)" row + best-day count, and the Booked-xor-deadline-passed pill exclusivity all behave — OR a gap is recorded describing the failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → prod Neon | Prod DATABASE_URL is a secret pulled via vercel env pull; must never be logged |
| prod migration → live data | An additive migration must not disturb existing v1.0/v1.1 rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Denial of Service (data loss) | prod migration | mitigate | Mandatory non-empty backup BEFORE migrate; additive-only (nullable/defaulted) 0006; stop-on-failure ordering (backup → migrate → deploy) |
| T-08-14 | Information Disclosure | prod DATABASE_URL / secrets | mitigate | Pull via vercel env pull; never echo secrets to logs; backup stored outside the repo |
| T-08-15 | Spoofing (status confusion) | deadline-passed vs Booked on prod | mitigate | Human-verify confirms the amber deadline pill and emerald Booked pill never co-render and the participant sees the correct distinct copy |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new dependencies; `postgres:18` docker image is the pinned pg18 client for the backup only |
</threat_model>

<verification>
- Non-empty prod backup exists under /home/dave/lfg-db-backups/ dated before the migration.
- Prod Neon information_schema lists polls.deadline and participants.is_organizer.
- Vercel reports a successful production deployment; the admin page renders both new cards.
- Human confirms deadline auto-close read-only + distinct copy, the organizer "(you)" row + best-day count, and Booked-xor-deadline-passed pill exclusivity on prod.
</verification>

<success_criteria>
- Prod is migrated (both columns present) and deployed, backup-first.
- On prod: an optional deadline lazily closes voting read-only after it passes (no cron), the organizer's own row folds into results/best-day as "(you)", and Booked stays distinct from deadline-passed.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-controls/08-04-SUMMARY.md` when done.
</output>

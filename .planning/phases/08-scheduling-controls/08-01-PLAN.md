---
phase: 08-scheduling-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0006_*.sql
  - drizzle/meta/_journal.json
  - src/lib/poll-status.ts
  - src/lib/poll-status.test.ts
autonomous: true
requirements: [DEAD-01, ORG-01]
user_setup: []
must_haves:
  truths:
    - "The polls table has a nullable `deadline timestamptz` column; NULL means no deadline (existing behavior)."
    - "The participants table has a nullable `is_organizer boolean default false` column; legacy rows read as false."
    - "A single pure helper isVotingOpen(poll, now) returns true only when status is 'open' AND (deadline is null OR deadline is strictly after now)."
    - "The local Docker Postgres has migration 0006 applied so every downstream DB-backed test sees both new columns."
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Additive nullable deadline (polls) + is_organizer (participants) columns"
      contains: "deadline"
    - path: "drizzle/0006_*.sql"
      provides: "The additive 0006 migration (ADD COLUMN only, no ALTER/DROP of existing columns)"
      contains: "add column"
    - path: "src/lib/poll-status.ts"
      provides: "The single shared lazy-close rule isVotingOpen(poll, now)"
      exports: ["isVotingOpen"]
    - path: "src/lib/poll-status.test.ts"
      provides: "Unit coverage for every isVotingOpen branch incl. the deadline==now boundary"
  key_links:
    - from: "src/lib/poll-status.ts"
      to: "polls.deadline + polls.status"
      via: "structural poll param { status, deadline }"
      pattern: "isVotingOpen"
  prohibitions:
    - "MUST NOT ALTER or DROP any existing column in migration 0006 — additive ADD COLUMN only (LOCKED constraint 1)."
    - "MUST NOT give the new columns a NOT NULL constraint without a default that keeps legacy rows valid — deadline is nullable, is_organizer defaults false."
    - "isVotingOpen MUST NOT perform any DB read/write or write status — it is a pure comparison (LOCKED constraint 2: no read-triggered writes)."
    - "isVotingOpen MUST NOT treat a deadline exactly equal to now as open — the boundary (deadline > now) closes voting at the instant it passes."
---

<objective>
Lay the shared foundation both Phase 8 features build on: one additive/nullable
migration (0006) adding `deadline` to polls and `is_organizer` to participants,
and one pure helper `isVotingOpen(poll, now)` that is the SINGLE place the
lazy-close rule lives. No feature wiring here — this plan exists so DEAD-01
(plan 02) and ORG-01 (plan 03) both consume a migrated schema and a tested
helper rather than re-deriving either.

Purpose: DEAD-01 needs `deadline` + the helper at every vote gate; ORG-01 needs
`is_organizer` on participants and gates the organizer's own availability with
the SAME helper. Both columns ship in one prod-safe migration (LOCKED
constraint 1) so the later prod-ship plan applies exactly one migration.

Output: migration 0006 applied locally; `src/lib/poll-status.ts` with full unit
coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-scheduling-controls/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the deadline + is_organizer columns and generate/apply migration 0006</name>
  <files>src/lib/db/schema.ts, drizzle/0006_*.sql, drizzle/meta/_journal.json</files>
  <read_first>
    - src/lib/db/schema.ts (the additive-nullable precedent: winningOptionId, organizerId, creatorEmail on polls, the whole invitations table — every v1.0/v1.1 column was added nullable; the `timestamp(..., { withTimezone: true })` idiom used by createdAt/invitedAt is exactly the shape `deadline` needs)
    - drizzle/0005_easy_madripoor.sql (the shape of the last generated migration — confirm the generator emits additive DDL)
    - drizzle.config.ts (out: "./drizzle", schema points at src/lib/db/schema.ts; migrations apply to DATABASE_URL)
    - .claude/CLAUDE.md ("What NOT to Use" + additive-nullable prod-safe migration constraint)
  </read_first>
  <action>
    Per DEAD-01 and ORG-01 (LOCKED constraint 1 — additive/nullable ONLY, no ALTER
    of existing columns):

    1. In src/lib/db/schema.ts add to the `polls` table a NULLABLE
       `deadline` column: a `timestamp("deadline", { withTimezone: true })` with NO
       `.notNull()` and NO default (NULL = no deadline = existing behavior). Place it
       alongside the other additive nullable poll columns (winningOptionId /
       organizerId / creatorEmail); do NOT reorder or alter existing columns. This is
       a timestamptz INSTANT, not a date-only value — it is exempt from the PLAT-04
       `new Date()` prohibition (LOCKED constraint 5).
    2. In the `participants` table add a NULLABLE `is_organizer` boolean defaulting
       false: `boolean("is_organizer").default(false)` with NO `.notNull()`. Import
       `boolean` from `drizzle-orm/pg-core` (add it to the existing pg-core import
       list). Legacy participant rows read as false. AT MOST ONE row per poll will
       ever have is_organizer=true — that single-row invariant is enforced by the
       ORG-01 upsert action in plan 03, NOT by a DB constraint here (matches the
       single-admin model; do NOT add a partial unique index this phase).
    3. Regenerate the inferred types are automatic via `$inferSelect`/`$inferInsert`;
       the exported Poll/Participant types pick up both columns with no manual edit.
    4. Generate the migration: run `npm run db:generate`. Confirm it emits a NEW
       `drizzle/0006_*.sql` containing exactly two additive `ALTER TABLE ... ADD
       COLUMN` statements (deadline on polls, is_organizer on participants) and NO
       ALTER/DROP of any pre-existing column. If the generator tries to alter
       anything else, STOP and reconcile the schema before continuing.
    5. Apply it locally: run `npm run db:migrate` with DATABASE_URL pointed at the
       local Docker Postgres (`postgres://postgres:password@localhost:5432/lfg`, the
       running `lfg-postgres` container). This makes both columns exist in the local
       test DB so every downstream DB-backed action test (plans 02/03) can seed and
       assert them. Do NOT touch prod here — prod migration is plan 04's gated ship.
  </action>
  <acceptance_criteria>
    - source: DEAD-01 / ORG-01; LOCKED constraint 1 (additive/nullable 0006).
    - behavior: schema.ts declares deadline (polls, nullable timestamptz) and is_organizer (participants, nullable boolean default false); 0006 is additive-only; the local DB has both columns.
    - test: `npm run db:generate` produces drizzle/0006_*.sql with two ADD COLUMN statements and no ALTER/DROP of existing columns; `npm run db:migrate` (local DATABASE_URL) applies cleanly; the local DB's polls/participants tables show the new columns.
  </acceptance_criteria>
  <verify>
    <automated>test -f drizzle/0006_*.sql && grep -qiE "add column .*deadline" drizzle/0006_*.sql && grep -qiE "add column .*is_organizer" drizzle/0006_*.sql && ! grep -qiE "drop column|alter column .*(set|drop) not null" drizzle/0006_*.sql && npx tsc --noEmit</automated>
  </verify>
  <done>Migration 0006 exists as additive ADD COLUMN DDL only, is applied to the local Docker Postgres, and the schema typechecks with both new nullable columns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the isVotingOpen pure helper with full branch coverage</name>
  <files>src/lib/poll-status.ts, src/lib/poll-status.test.ts</files>
  <read_first>
    - src/lib/results.ts (the pure-module convention: no DB, no I/O, never throws — mirror this for poll-status.ts)
    - src/lib/results.test.ts (the pure-test convention: node env, no DB, no mocks, one explicit `it` per case — mirror this exactly)
    - src/lib/actions/submit-response.ts:115 and src/lib/actions/update-response.ts:123 (the `poll.status !== "open"` gates plan 02 replaces with `!isVotingOpen(...)` — the helper's contract must satisfy both call sites)
    - .planning/phases/08-scheduling-controls/08-CONTEXT.md (the exact rule definition in <specifics>)
  </read_first>
  <behavior>
    - isVotingOpen({ status: "open", deadline: null }, now) === true  (no deadline = open)
    - isVotingOpen({ status: "open", deadline: <1h future> }, now) === true
    - isVotingOpen({ status: "open", deadline: <1h past> }, now) === false  (lazy close)
    - isVotingOpen({ status: "open", deadline: now }, now) === false  (boundary: deadline == now is CLOSED, rule is deadline > now)
    - isVotingOpen({ status: "closed", deadline: null }, now) === false  (booked always closed)
    - isVotingOpen({ status: "closed", deadline: <future> }, now) === false  (booked wins over a not-yet-passed deadline)
  </behavior>
  <action>
    Create src/lib/poll-status.ts exporting a single pure function
    `isVotingOpen(poll, now)` implementing LOCKED constraint 2 verbatim:
    `poll.status === "open" && (poll.deadline == null || poll.deadline > now)`.

    Type the `poll` param STRUCTURALLY as `{ status: string; deadline: Date | null }`
    (NOT the full Poll row) so it accepts both the admin poll row
    (getPollWithWinningOption) and the participant-safe row
    (getPollByParticipantUrlId) that plan 02 wires in. `now` is a `Date`. Return a
    boolean. NO DB access, NO I/O, NO status write — this is a pure comparison
    evaluated fresh on each server-side access (the lazy-close mechanism; there is
    deliberately no cron and no read-triggered write). `poll.deadline` is the JS
    Date Drizzle returns for a timestamptz; `Date > Date` compares instants
    correctly regardless of process timezone.

    Write src/lib/poll-status.test.ts (node env, no DB, no mocks — mirror
    results.test.ts) with one explicit `it` for each case in <behavior>, including
    the deadline==now boundary and the closed-with-future-deadline case that proves
    Booked wins over an un-passed deadline (LOCKED constraint 3 in miniature).
  </action>
  <acceptance_criteria>
    - source: DEAD-01; LOCKED constraints 2, 3; CONTEXT <specifics>.
    - behavior: isVotingOpen is a pure function matching the six <behavior> rows; it performs no I/O and never writes status.
    - test: `npm run test -- src/lib/poll-status.test.ts` passes all six cases with no DATABASE_URL required.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/poll-status.test.ts</automated>
  </verify>
  <done>poll-status.ts exports the pure isVotingOpen helper and every branch (incl. the deadline==now boundary and closed-beats-future-deadline) is covered by passing unit tests.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
- New columns: `polls.deadline` (nullable timestamptz), `participants.is_organizer` (nullable boolean, default false).
- New migration: `drizzle/0006_*.sql` (additive ADD COLUMN only) applied to the local Docker Postgres.
- New helper: `src/lib/poll-status.ts` exporting `isVotingOpen(poll, now)`.
- New tests: `src/lib/poll-status.test.ts`.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema change → live data (local now, prod in plan 04) | An additive/nullable migration must not disturb existing rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service (data loss) | migration 0006 | mitigate | Additive ADD COLUMN only; both columns nullable (deadline) / defaulted (is_organizer); verify DDL contains no DROP/ALTER-of-existing before applying |
| T-08-02 | Tampering | isVotingOpen correctness | mitigate | Pure function with exhaustive unit coverage incl. the deadline==now boundary; single source of truth so gates cannot drift |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new dependencies this phase (UI-SPEC: zero new packages) |
</threat_model>

<verification>
- drizzle/0006_*.sql exists and is additive-only (no DROP/ALTER of existing columns).
- Local Docker Postgres has polls.deadline and participants.is_organizer.
- `npm run test -- src/lib/poll-status.test.ts` is green (six cases).
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- Both new columns exist (nullable/defaulted, additive) locally.
- isVotingOpen is a tested pure helper ready for plans 02 and 03 to consume.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-controls/08-01-SUMMARY.md` when done.
</output>

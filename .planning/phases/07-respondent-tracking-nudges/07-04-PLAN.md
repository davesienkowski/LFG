---
phase: 07-respondent-tracking-nudges
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified: []
autonomous: false
requirements: [RESP-03, RESP-02]
user_setup: []
must_haves:
  truths:
    - "Prod Neon is backed up (pg18 client, stored outside the repo) BEFORE the migration runs."
    - "The 0005 invitations migration is applied to prod Neon and the invitations table exists in prod."
    - "The current build is deployed to prod via the Vercel CLI (git push does NOT auto-deploy)."
    - "A real nudge email lands in the owner's inbox (not spam) with a working participant link."
  artifacts:
    - path: "/home/dave/lfg-db-backups/"
      provides: "Timestamped prod Neon dump taken immediately before the migration"
  key_links:
    - from: "prod Neon"
      to: "invitations table"
      via: "npm run db:migrate against the prod DATABASE_URL pulled by vercel env pull"
      pattern: "invitations"
  prohibitions:
    - "MUST NOT run db:migrate against prod before a successful backup exists in /home/dave/lfg-db-backups/."
    - "MUST NOT deploy before the prod migration succeeds (additive migration first so old serverless code tolerates the new table, then ship the new code)."
    - "MUST NOT close the phase until the human confirms the real nudge email arrived with a working link (executor has no inbox access)."
---

<objective>
Ship Phase 7 to production: back up prod Neon, apply the additive `invitations`
migration, deploy the new build with the Vercel CLI, and get a human to confirm a
real nudge email actually lands in an inbox with a working link. This is the
established v1.0 prod pattern (STATE blocker "[v1.1 prod migration]") plus the
carried end-of-phase human email check that the executor cannot self-verify.

Purpose: The feature isn't real until prod has the table and the nudge email
delivers. Isolated as its own plan because it mixes a self-serve prod operation
with a blocking human-verify checkpoint (kept separate from autonomous code work).

Output: prod migrated + deployed; human sign-off that the nudge email delivers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-respondent-tracking-nudges/07-CONTEXT.md
@.planning/phases/07-respondent-tracking-nudges/07-01-SUMMARY.md
@.planning/phases/07-respondent-tracking-nudges/07-02-SUMMARY.md
@.planning/phases/07-respondent-tracking-nudges/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Back up prod Neon, apply the 0005 migration, deploy (self-serve)</name>
  <files>(no repo files — prod operations only: vercel env pull, docker pg18 backup, db:migrate, vercel deploy)</files>
  <read_first>
    - .planning/STATE.md (the "[v1.1 prod migration]" blocker: backup -> migrate -> deploy sequence, pg18 client caveat, backup dir /home/dave/lfg-db-backups/, deploy command)
    - drizzle/0005_*.sql (the exact additive migration produced in 07-01 — confirm it is CREATE TABLE + unique index only)
  </read_first>
  <action>
    Self-serve prod ship per project MEMORY (prod-migrate-self-serve; NOT gated as a
    human action). Strict ordering — do NOT reorder:

    1. Pull prod creds: `npx vercel env pull` (or `npx vercel env pull .env.prod`)
       to obtain the prod `DATABASE_URL` (CLI is pre-authed per MEMORY). Do NOT
       print secrets to logs.
    2. BACKUP FIRST (hard precondition): dump prod Neon with a pg18 client
       (`docker run` the `postgres:18` image so client version matches Neon — local
       pg_dump 17 mismatches per MEMORY prod-db-backup) to a timestamped file under
       `/home/dave/lfg-db-backups/` (OUTSIDE the repo). Confirm the dump file is
       non-empty before continuing — a 0-byte dump (see the stray 0B file already in
       that dir) means the backup FAILED; STOP and do not migrate.
    3. Migrate prod: run `npm run db:migrate` with the prod `DATABASE_URL` exported.
       Confirm the `invitations` table now exists in prod (query information_schema
       or `\d invitations`). The migration is additive (CREATE TABLE + unique index),
       so pre-deploy prod code simply ignores the new table.
    4. Deploy: `npx vercel@latest deploy --prod --yes` (git push does NOT auto-deploy
       per MEMORY deploy-tooling; the MCP deploy tool is a no-op). Capture the
       deployment URL.
    5. Smoke-check the deployed admin page loads and the "Who's responded" card
       renders (empty state acceptable if no invites yet on the checked poll).

    If any step fails, STOP and surface the failure — do not proceed to a later
    step (never deploy on a failed migration; never migrate on a failed backup).
  </action>
  <acceptance_criteria>
    - source: STATE "[v1.1 prod migration]" blocker; MEMORY prod-migrate-self-serve, prod-db-backup, deploy-tooling.
    - behavior: a non-empty timestamped backup exists before migration; prod has the invitations table after migration; the new build is live via the Vercel CLI.
    - test: backup file exists and is non-empty in /home/dave/lfg-db-backups/; prod information_schema shows `invitations`; `vercel deploy --prod` reports a successful production deployment; the deployed admin page returns 200 and renders the card.
  </acceptance_criteria>
  <verify>
    <human-check>Confirm: (1) a non-empty prod backup file was written under /home/dave/lfg-db-backups/ before migrating; (2) the invitations table exists in prod Neon; (3) the Vercel production deploy succeeded and the admin page renders the "Who's responded" card.</human-check>
  </verify>
  <done>Prod Neon backed up, the additive invitations migration applied to prod, and the new build deployed via the Vercel CLI — in that order, with each step verified before the next.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify a real nudge email delivers in production</name>
  <files>(no repo files — human verifies the deployed prod app + their own inbox)</files>
  <what-built>
    RESP-02 one-click nudge in production: the admin "Who's responded" card lists
    invited emails with responded/not-responded status, and "Nudge non-respondents"
    re-emails only the non-respondents a reminder carrying their participant link.
    The reminder routes through the same env-switched sendEmail() seam as v1.0
    invites. The executor has NO inbox access, so real deliverability must be
    human-verified (carried v1.0 end-of-phase email check, now for the nudge).
  </what-built>
  <how-to-verify>
    On the deployed prod app:
    1. Open (or create) a poll's admin page `/a/<adminUrlId>` and send an invite by
       email to an address you control that has NOT yet voted.
    2. Refresh the admin page — confirm that address appears under "Who's responded"
       with a "Not yet responded" (amber) badge, and the summary reads "0 of 1
       responded" with the caption "Only counts people invited by email through this
       tool."
    3. Click "Nudge non-respondents". Confirm the per-recipient chip shows "Sent".
    4. Check that inbox: the reminder email ("Reminder: your response is needed")
       ARRIVES (INBOX, not spam) and its link opens the correct participant voting
       page.
    5. Vote from that link, refresh the admin page, and confirm the badge flips to
       "Responded" (emerald) and the nudge button now shows "Everyone's responded —
       nothing to nudge."
  </how-to-verify>
  <action>
    HUMAN-ONLY verification (the executor has no inbox access): the owner performs
    the steps in <how-to-verify> on the deployed prod app and confirms the reminder
    email arrives in-inbox with a working participant link and that responded status
    updates after voting. The executor performs NO automation here — Task 1 already
    shipped prod; this task is solely the deliverability sign-off. If the email
    spam-folders or the link is wrong, the owner describes the failure and the
    executor opens a gap for follow-up rather than closing the phase.
  </action>
  <resume-signal>Type "approved" once the nudge email arrived with a working link and the responded/not-responded status behaved as described; otherwise describe what failed.</resume-signal>
  <verify>
    <human-check>A real nudge reminder email ("Reminder: your response is needed") sent from the production admin page arrives in the owner's inbox (not spam) with a working participant link, and the invited address flips from "Not yet responded" to "Responded" after voting.</human-check>
  </verify>
  <done>The owner has confirmed the production nudge email delivers to an inbox with a working link and the responded status updates after voting — OR a gap is recorded describing the delivery failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell -> prod Neon | Prod DATABASE_URL is a secret pulled via vercel env pull; must never be logged |
| prod migration -> live data | An additive migration must not disturb existing v1.0 rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Denial of Service (data loss) | prod migration | mitigate | Mandatory non-empty backup BEFORE migrate; additive-only migration; stop-on-failure ordering (backup -> migrate -> deploy) |
| T-07-09 | Information Disclosure | prod DATABASE_URL / secrets | mitigate | Pull via vercel env pull; never echo secrets to logs; backup stored outside the repo |
| T-07-04 | Information Disclosure | reminder email deliverability | mitigate | Human-verify confirms the reminder carries the participant link (not the admin URL) and delivers to inbox |
| T-07-SC | Tampering (supply chain) | npm installs | accept | No new dependencies; `postgres:18` docker image is the pinned pg18 client for the backup only |
</threat_model>

<verification>
- Non-empty prod backup exists under /home/dave/lfg-db-backups/ dated before the migration.
- Prod Neon information_schema lists the `invitations` table.
- Vercel reports a successful production deployment; admin page renders the card.
- Human confirms the nudge reminder email arrives in-inbox with a working participant link.
</verification>

<success_criteria>
- Prod is migrated (invitations table present) and deployed, backup-first.
- A real nudge email delivers to an inbox with a working link, and responded status updates after voting.
</success_criteria>

<output>
Create `.planning/phases/07-respondent-tracking-nudges/07-04-SUMMARY.md` when done.
</output>

---
phase: 05-vote-grid-redesign-matrix-1c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/availability-grid.tsx
  - src/components/availability-grid.test.tsx
autonomous: true
requirements: []          # Phase 5 adds NO new backend/data requirements (visual/UX redesign of shipped VOTE-* features); requirements-coverage gate is skipped. Goal-backward is anchored on D-01..D-08 in must_haves.
user_setup: []

must_haves:
  truths:
    # D-01 — the one structural change
    - "D-01: each date row is a role=radiogroup of three role=radio cells (Available / If-need-be / Not available), exactly one aria-checked at a time — no click-to-cycle button remains"
    # D-02 — desktop icon-only cells + labelled column headers
    - "D-02: at >=640px the grid renders `grid-template-columns: 1.6fr 1fr 1fr 1fr` with a persistent header row (empty label cell + three icon+text state headers); desktop radio cells are icon-only and satisfy a11y via the column header"
    # D-03 (grid half) — mobile stacked icon+text segments
    - "D-03: at <640px the grid renders stacked full-width segments (>=48px), each carrying its own icon AND visible text — no icon-only cell exists at mobile width (sticky submit lives in vote-form.tsx, Plan 03)"
    # D-04 — never-blank invariant
    - "D-04: default/untouched row = 'Not available' (no) selected, never blank; the seed `initial?.[o.id] ?? 'no'` is unchanged"
    # D-05 — bulk actions + closed read-only + unchanged labels
    - "D-05: the three bulk buttons (Set all Available / Set all Not available / Clear, h-11) render when interactive and are absent entirely when disabled; a closed poll renders one non-interactive chip (icon+text) per row — no matrix, no segments, no bulk row; labels stay Available / If-need-be / Not available"
    # D-06 — a11y AA
    - "D-06: radio cells expose role=radio / aria-checked inside role=radiogroup with aria-label='{date}: {state}'; icon-or-color-never-alone holds (desktop via column header, mobile via per-segment text); the aria-live=polite announcement region is retained; focus ring is focus-visible:ring-3 focus-visible:ring-ring/50; targets >=44px"
    # D-07 — verbatim tokens/palette
    - "D-07: STATE_META (emerald/amber/muted palette + Check/CircleHelp/X icons + labels) is reused verbatim from @/lib/vote-state; no new token, utility, @layer rule, font, animation, or dark-mode branch is introduced"
    # D-08 — tests
    - "D-08: availability-grid.test.tsx asserts radio semantics (not click-to-cycle) and adds two new a11y tests — desktop column-header association + mobile segmented-fallback icon+text; `npm test -- availability-grid` exits 0"
    # Edge-probe findings (folded 2026-07-02)
    - "EDGE-KBD: every role=radio is a plain keyboard-focusable <button> (Tab-reachable, Enter/Space-activatable) — NO `tabIndex={-1}` and no partial roving-tabindex, so all three states stay keyboard-reachable (WCAG 2.1.1)"
    - "EDGE-A11Y-EXCL: the desktop matrix (`hidden sm:block`) and mobile segments (`sm:hidden`) both live in the DOM but rely on `display:none` so exactly ONE layer is exposed to the accessibility tree at any viewport — AT never announces two radiogroups per date"
    - "EDGE-IDEMPOTENT: re-selecting the already-checked state is a no-op that keeps exactly one radio checked per row — a radio can never be toggled OFF to a blank row (reinforces D-04 never-blank)"
  artifacts:
    - path: "src/components/availability-grid.tsx"
      provides: "Radio-matrix AvailabilityGrid (desktop matrix + mobile segments + read-only chips), selectCell setter, preserved bulk/never-blank/aria-live/onChange, GridOption + VoteState re-export"
      contains: "role=\"radiogroup\""
      min_lines: 140
    - path: "src/components/availability-grid.test.tsx"
      provides: "Rewritten radio-semantics tests + desktop-header-association + mobile-fallback a11y tests"
      contains: "getAllByRole(\"radio\""
  key_links:
    - from: "src/components/availability-grid.tsx"
      to: "@/lib/vote-state"
      via: "import { STATE_META, type VoteState }"
      pattern: "STATE_META"
    - from: "src/components/vote-form.tsx"
      to: "src/components/availability-grid.tsx"
      via: "AvailabilityGrid mount + type re-export (disabled / onChange / GridOption / VoteState) — contract UNCHANGED by this rewrite"
      pattern: "AvailabilityGrid"
---

<objective>
Rewrite `src/components/availability-grid.tsx` from a single click-to-cycle `<button>` per date
into the **Matrix (1c)** radio grid — the one structural change of Phase 5 (D-01..D-07) — and rewrite
`availability-grid.test.tsx` to assert radio semantics plus two new a11y tests (D-08). This is the
load-bearing, a11y-critical slice; every other Phase 5 plan is visual reconciliation.

Purpose: give participants a tap-the-state-you-mean grid that mirrors the admin `ResultsGrid` (one
mental model) while preserving every shipped data-correctness invariant (never-blank default, bulk
actions, closed read-only, unchanged labels, icon-or-color-never-alone).

Output: rewritten `availability-grid.tsx` (matrix + mobile segments + read-only chips) and a rewritten
`availability-grid.test.tsx` (radio semantics + column-header association + mobile-fallback tests).
</objective>

<assumption_delta_decision>
Primary noun = the participant **vote/response** (one selection per candidate date). Decision =
**no-change**: the Matrix redesign changes only the *input mechanic* (click-to-cycle → radio), not
the data identity — still exactly one of three states per date, `no` as the never-blank default. The
detector fired only on "fallback" (a responsive layout fallback, not a second entity) and "optional"
(the optional email tweak); neither introduces a competing identity. No promote/add-alongside needed.
(Carried verbatim from 05-CONTEXT.md so the executor/verifier keep it in view.)
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-CONTEXT.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md
@src/lib/vote-state.ts
@src/components/results-grid.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite AvailabilityGrid to the radio matrix (desktop matrix + mobile segments + read-only chips)</name>
  <files>src/components/availability-grid.tsx</files>
  <read_first>
    - src/components/availability-grid.tsx (the rewrite target — current click-to-cycle version; preserve every commented invariant)
    - src/lib/vote-state.ts (STATE_META palette/labels/icons — reuse verbatim, do NOT restate values)
    - src/components/results-grid.tsx (layout analog: cn() usage, column-header icon+text pattern, tint composition)
    - design_handoff_vote_grid_redesign/designs/Vote Screen.dc.html (states 2a desktop-filled lines 43-116, 2b desktop-empty lines 119-182, 2c desktop-closed lines 188-228, 2d mobile-filled lines 230-303, 2e mobile-closed lines 305-355 — HTML is source of truth)
    - design_handoff_vote_grid_redesign/DESIGN.md §4 (Matrix vote cell) + §6 (a11y)
    - .planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md (Pattern Assignments for availability-grid.tsx — behavior to PRESERVE + new radio markup)
  </read_first>
  <behavior>
    - Untouched grid: every row's `no` radio has aria-checked="true", the `yes`/`ifneedbe` radios aria-checked="false" (D-04 never-blank).
    - selectCell(opt, "yes") sets only that row to yes (aria-checked flips), leaving other rows untouched.
    - Re-selecting the already-checked state is a no-op — exactly one radio stays checked (never toggles to blank; EDGE-IDEMPOTENT).
    - setAll("yes") sets every row to yes; Clear (setAll("no")) resets every row to no.
    - onChange emits `[{optionId, state}]` for every option on mount and after each change (verbatim effect).
    - disabled=true: zero role=radio nodes, zero bulk buttons; one icon+text chip per row remains.
  </behavior>
  <action>
    Replace the single click-to-cycle `<button>`/`cycleCell` (and the `CYCLE` array) with a direct
    `selectCell(opt: GridOption, next: VoteState)` setter that does `setCellState((prev) => ({ ...prev, [opt.id]: next }))`
    and then calls `setAnnouncement(...)` OUTSIDE the updater (mirrors the shipped `setAll` — avoids the
    React 19 Strict/concurrent double-invocation desync documented in results-grid.tsx). PRESERVE verbatim:
    the `cellState` seed `initial?.[o.id] ?? "no"` (D-04), the `onChange` emission `useEffect`, the
    `aria-live="polite" sr-only` announcement region, the three `Button` bulk actions (`type="button"`,
    `h-11`, Check/X/RotateCcw icons, rendered only when `!disabled`), the `optionLabel` helper
    (`formatDateWithTime`, never `new Date`), the `disabled` prop name, and the `export type { VoteState }`
    + `GridOption` re-export (vote-form.tsx and both participant pages import these from here — D-01 keeps
    the public contract stable).

    When NOT disabled, render TWO sibling layouts (Tailwind responsive; `display:none` removes the hidden
    layer from the a11y tree so only one is exposed at a time — EDGE-A11Y-EXCL):
    (a) DESKTOP matrix — wrapper `hidden sm:block`. A header row `grid grid-cols-[1.6fr_1fr_1fr_1fr] items-end gap-2 border-b px-1 pb-2.5`: first cell empty, then three state headers in state order `yes, ifneedbe, no`, each `flex flex-col items-center gap-1.5 text-sm font-semibold` with the state color (`text-emerald-700` / `text-amber-700` / `text-muted-foreground`), a `meta.Icon` at ~18px `aria-hidden`, and the `meta.label` text. Then one `role="radiogroup" aria-label={optionLabel(opt)}` per option: `grid grid-cols-[1.6fr_1fr_1fr_1fr] items-center gap-2 border-b px-1 py-3`, first cell = date label (`text-base`), then three centered cells each holding a `role="radio"` button — `type="button"`, `aria-checked={state===s}`, `aria-label={`${optionLabel(opt)}: ${STATE_META[s].label}`}`, `onClick={() => selectCell(opt, s)}`, `size-11` (44px) `rounded-lg border outline-none transition-colors focus-visible:ring-3 focus-visible:ring-ring/50`, classes composed with `cn()` so SELECTED = `STATE_META[s].className` (tint+border) + the icon (`meta.Icon`, ~size-5, aria-hidden) and UNSELECTED = `bg-white border-border` with NO icon child (empty white box, per mock 2a).
    (b) MOBILE segments — wrapper `sm:hidden`, one `role="radiogroup" aria-label={optionLabel(opt)}` per option stacking a date label (`text-base font-semibold`) above three full-width `role="radio"` buttons, each `min-h-12` (>=48px) `w-full flex items-center justify-center gap-2 rounded-lg border`, ALWAYS carrying its own `meta.Icon` (aria-hidden) AND visible `meta.label` text (D-03/D-06 — no icon-only at mobile); SELECTED = `STATE_META[s].className`, UNSELECTED = `bg-white text-muted-foreground border-border`; same aria-checked/aria-label/onClick semantics as desktop.

    Keep every `role="radio"` a plain focusable `<button>` — do NOT add `tabIndex={-1}` or a partial
    roving-tabindex (EDGE-KBD): all three states must stay Tab-reachable and Enter/Space-activatable. Do
    not add arrow-key handlers; plain button focus order satisfies WCAG 2.1.1 for this direction.

    When disabled (closed poll): keep the shipped read-only path — one non-interactive `<span>` chip per
    row (icon `size-4` + `meta.label` text, `meta.className`, >=44px), NO matrix, NO segments, NO bulk row
    (mock 2c/2e). Add `data-testid="matrix-desktop"` to the desktop-matrix wrapper and
    `data-testid="segments-mobile"` to the mobile wrapper so Task 2 can scope queries to one layer.
    Update the file-header comment block to describe the matrix (not click-to-cycle) and note the desktop
    icon-only-cell a11y exception (text lives in the column header). No fenced implementation is dictated
    here — follow the markup shapes above and the mock's exact class values.
  </action>
  <acceptance_criteria>
    - `src/components/availability-grid.tsx` contains `role="radiogroup"`, `role="radio"`, and `aria-checked` (grep hits > 0).
    - The file contains `grid-cols-[1.6fr_1fr_1fr_1fr]` (or the exact `1.6fr 1fr 1fr 1fr` template) for both the header row and each desktop radiogroup.
    - The file contains `data-testid="matrix-desktop"` and `data-testid="segments-mobile"`, and both `hidden sm:block` + `sm:hidden` wrappers (EDGE-A11Y-EXCL: display:none isolates one layer).
    - No `CYCLE` array and no `cycleCell` remain; `selectCell(` is present.
    - No `tabIndex={-1}` / `tabindex="-1"` anywhere in the file (EDGE-KBD — radios stay keyboard-reachable).
    - The seed `initial?.[o.id] ?? "no"`, the `onChange` useEffect, the `aria-live="polite"` region, and `export type { VoteState }` are all still present (grep confirms).
    - `focus-visible:ring-3 focus-visible:ring-ring/50` present on interactive cells.
    - `npm run build` succeeds (TypeScript: `disabled`/`onChange`/`GridOption`/`VoteState` contract intact so vote-form.tsx + participant pages still typecheck).
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -c "role=\"radio\"" src/components/availability-grid.tsx</automated>
    <automated>grep -q "grid-cols-\[1.6fr_1fr_1fr_1fr\]" src/components/availability-grid.tsx && grep -q "data-testid=\"segments-mobile\"" src/components/availability-grid.tsx && ! grep -qiE "tabindex=.?-1|tabIndex=\{-1\}" src/components/availability-grid.tsx && echo MARKERS_OK</automated>
  </verify>
  <done>The grid renders as a radio matrix on desktop (icon-only cells + labelled headers) and stacked icon+text segments on mobile, defaults every row to Not available, keeps all three radios keyboard-reachable, preserves bulk/aria-live/onChange/read-only chips, keeps the public prop + type-export contract, and the project builds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite availability-grid.test.tsx for radio semantics + add column-header-association and mobile-fallback a11y tests (D-08)</name>
  <files>src/components/availability-grid.test.tsx</files>
  <read_first>
    - src/components/availability-grid.test.tsx (current test — reuse the OPTIONS fixture + afterEach(cleanup); the click-to-cycle assertions WILL break and must be rewritten)
    - src/components/availability-grid.tsx (the just-rewritten component — data-testid="matrix-desktop"/"segments-mobile", role=radio, aria-checked, aria-label)
    - src/components/results-grid.test.tsx (within()/aria assertion idioms)
    - .planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md (test rewrite guidance — dual-layout DOM means each state renders twice; scope queries by data-testid to avoid double-counting)
    - design_handoff_vote_grid_redesign/DESIGN.md §6 (a11y expectations the tests encode)
  </read_first>
  <action>
    Keep the exact `OPTIONS` fixture and `afterEach(() => cleanup())`. Because both layouts render into
    the DOM (desktop matrix + mobile segments), scope layer-specific assertions with
    `within(screen.getByTestId("matrix-desktop"))` and `within(screen.getByTestId("segments-mobile"))`.
    Rewrite:
    - "untouched rows default to Not available" → within matrix-desktop, each row's `no` radio (query by
      `getByRole("radio", { name: /Not available/ })` scoped to its radiogroup) has `aria-checked="true"`
      and the `yes`/`ifneedbe` radios `aria-checked="false"` (D-04).
    - "Set all Available then a single override" → click `Set all Available`, assert every desktop `yes`
      radio is aria-checked; then `fireEvent.click` a single row's `yes`→ then its `ifneedbe` radio and
      assert ONLY that row changed (direct selection, not a cycle).
    - "re-selecting a checked state stays selected" → click a row's already-checked radio again and assert
      it remains aria-checked (never blank; EDGE-IDEMPOTENT).
    - "Clear resets to Not available" → click `Clear`, assert every `no` radio aria-checked.
    - "seeds from initial prop" → unchanged behavior, radio role.
    - "emits serialized votes via onChange" → copy verbatim (layer-agnostic; asserts the payload array).
    - "read-only renders non-interactive chips, no bulk actions" → with `disabled`, `queryAllByRole("radio")`
      is empty, `Set all Available` button is null, and both "Not available" chips are present.
    ADD two NEW a11y tests (D-08):
    1. Desktop column-header association: within matrix-desktop, assert the header row renders the three
       state labels (Available / If-need-be / Not available) as text, and that every desktop `role="radio"`
       carries an `aria-label` whose suffix equals one of those column labels — proving the icon-only cell
       inherits its meaning from the labelled column.
    2. Mobile segmented fallback: within segments-mobile, for a chosen row assert each `role="radio"` has
       BOTH an `svg` child AND a visible text node matching its `meta.label` (`within(radio).getByText(...)`),
       confirming "no icon-only cells exist at mobile width" (the key contrast with the desktop cell, which
       has no visible text child).
  </action>
  <acceptance_criteria>
    - `availability-grid.test.tsx` no longer references `role: "button"` for grid cells nor `/currently/` cycle labels; it queries `getAllByRole("radio", ...)`.
    - It contains a test asserting desktop-header/`aria-label` association, a test asserting mobile segments carry icon + visible text, and a test asserting re-selection stays checked (never-blank).
    - `npm test -- availability-grid` exits 0 with all cases passing.
    - `npm run lint` reports no new errors for this file.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- availability-grid 2>&1 | tail -15</automated>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>The test suite asserts the radio matrix (radiogroup/radio/aria-checked), preserves the behavioral guarantees (never-blank, idempotent re-selection, bulk, override, onChange, read-only), adds the two required a11y tests, and passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant browser → `submitResponse`/`updateResponse` server action | Untrusted vote payload crosses here (UNCHANGED by this redesign — grid still emits the same `{optionId, state}` array serialized by vote-form.tsx's hidden `votes` input). |
| poll status (open/closed) → interactivity | Read-only enforcement. This rewrite keeps read-only as a render-time chip list; server-side closed-poll enforcement is UNCHANGED and lives in the actions/pages, not this component. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | never-blank default | mitigate | Seed `initial?.[o.id] ?? "no"` preserved verbatim (D-04) so an unsubmitted date can never be recorded as blank/undefined; radio semantics cannot toggle a row to blank (EDGE-IDEMPOTENT); Task 2 asserts every untouched row is `no`. |
| T-05-02 | Tampering | closed-poll read-only | accept (visual layer) | Grid renders non-interactive `<span>` chips when `disabled` and emits no interactive controls; the authoritative closed-poll block remains server-side in the actions/pages (out of this component's scope) — no new client-trust surface introduced. |
| T-05-03 | Information disclosure | grid markup | accept | No admin `/a/` URL, token, or PII is rendered by this component; it renders only dates + the participant's own in-memory selections. |

No new routes, server actions, tokens, dependencies, or data-model changes are introduced. ASVS L1: no
high-severity threat exists in this frontend-only rewrite — the state model, serialization seam, and
read-only semantics are byte-for-byte the shipped behavior. No package installs in this plan.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `selectCell(opt, next)` setter replacing `cycleCell` in `availability-grid.tsx` (new symbol).
- Desktop matrix markup (`grid-cols-[1.6fr_1fr_1fr_1fr]` header + per-row `role="radiogroup"` of three `role="radio"` cells) and mobile stacked-segment markup — both new sub-structures in `availability-grid.tsx`.
- `data-testid="matrix-desktop"` / `data-testid="segments-mobile"` layer hooks (new).
- Three new/changed test cases in `availability-grid.test.tsx` (desktop column-header association, mobile segmented fallback, idempotent re-selection).
- No new files, dependencies, tokens, utilities, or fonts.
</artifacts_produced>

<verification>
- `npm run build` passes (typecheck confirms the unchanged `disabled`/`onChange`/`GridOption`/`VoteState` contract still satisfies vote-form.tsx and both participant pages).
- `npm test -- availability-grid` passes with the rewritten + new a11y tests.
- Grep confirms `role="radiogroup"`, `role="radio"`, `aria-checked`, `grid-cols-[1.6fr_1fr_1fr_1fr]`, `selectCell(`, the preserved `aria-live="polite"` / `?? "no"` / `export type { VoteState }` markers, and the ABSENCE of `tabIndex={-1}`.
- (end-of-phase human check) Desktop renders the icon-only matrix with labelled headers; mobile renders stacked icon+text segments; a screen reader announces exactly one radiogroup per date (not two); closed poll shows read-only chips — matching mock states 2a–2e.
</verification>

<success_criteria>
- AvailabilityGrid is a radio matrix (D-01) with desktop labelled column headers (D-02) and mobile icon+text segments (D-03).
- Never-blank default (D-04), bulk actions + closed read-only + unchanged labels (D-05), and a11y semantics + retained aria-live + keyboard-reachable radios (D-06 / EDGE-KBD) all hold.
- STATE_META palette/labels/icons reused verbatim; no new token/utility/font/animation (D-07).
- Tests rewritten to radio semantics with the two new a11y tests + idempotent-reselection guard; `npm test -- availability-grid` green (D-08).
</success_criteria>

<output>
Create `.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-01-SUMMARY.md` when done.
</output>

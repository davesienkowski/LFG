---
phase: 05-vote-grid-redesign-matrix-1c
plan: 03
type: execute
wave: 1
depends_on: []          # No file overlap with Plan 01; the AvailabilityGrid public contract (disabled/onChange/GridOption/VoteState) is UNCHANGED by the rewrite (D-01), so vote-form.tsx can be reconciled in parallel.
files_modified:
  - src/components/vote-form.tsx
  - src/app/p/[participantUrlId]/page.tsx
  - src/app/p/[participantUrlId]/thanks/page.tsx
  - src/app/p/[participantUrlId]/edit/[editToken]/page.tsx
autonomous: true
requirements: []          # Visual reconciliation of shipped VOTE-* participant surfaces; no new requirements.
user_setup: []

must_haves:
  truths:
    - "D-03: on the participant vote screen the primary action (Submit availability) / closed banner is a `position: sticky` (or pinned footer) at the bottom on mobile so a long date list never buries it — the content scrolls, the action stays visible; implemented in vote-form.tsx (the grid's mobile segments are Plan 01)"
    - "D-09: vote screen (2a–2e), thanks (3b), and edit (3c) match the mocks — `h2` 'Your availability', name(required)/email(optional)+help, submit `w-full sm:w-auto`; closed → the 'Voting is closed' banner (`rounded-lg border bg-muted p-6`) replaces the submit; thanks shows the amber bearer-credential warning ('Don't share this link…') + mono link + Copy link + 'No email was sent — save this link now'; edit heading is 'Edit your availability' with submit 'Save changes', values prefilled"
    - "The AvailabilityGrid mount is UNCHANGED — `disabled={isPending || readOnly}`, `onChange={setVotes}`, `initial={initialVotes}`, and the hidden `votes` serialization seam stay exactly as shipped (no prop renames); readOnly still omits the submit button entirely (not disabled)"
    - "SEC/D-10: no admin `/a/` URL appears on any participant page (vote/thanks/edit) — participants only ever see the participant link and their own per-participant edit link"
    - "No new component/token/utility/font/animation, no dark-mode branch; no structural rewrite of vote-form beyond the sticky-footer wrapping"
  artifacts:
    - path: "src/components/vote-form.tsx"
      provides: "Vote form reconciled to 2a–2e with mobile sticky/pinned submit + closed banner (D-03)"
    - path: "src/app/p/[participantUrlId]/page.tsx"
      provides: "Participant vote page shell reconciled to 2a–2c"
    - path: "src/app/p/[participantUrlId]/thanks/page.tsx"
      provides: "Thanks page reconciled to 3b (amber bearer-credential warning + copy link)"
    - path: "src/app/p/[participantUrlId]/edit/[editToken]/page.tsx"
      provides: "Edit page reconciled to 3c ('Edit your availability' / 'Save changes')"
  key_links:
    - from: "src/components/vote-form.tsx"
      to: "src/components/availability-grid.tsx"
      via: "AvailabilityGrid mount — contract unchanged (disabled/onChange/initial + hidden votes input)"
      pattern: "AvailabilityGrid"
---

<objective>
Reconcile the participant-facing surfaces to the mocks under D-09 and implement the mobile sticky/pinned
submit half of D-03 — `vote-form.tsx` (2a–2e form shell + sticky submit + closed banner), the participant
vote page (`/p/[participantUrlId]`), the thanks page (3b), and the edit page (3c). Adjust classes/copy
only where they drift; the AvailabilityGrid mount contract is unchanged.

Purpose: the hero vote screen and its return/edit flow match the high-fidelity handoff while preserving
the shipped form behavior (required name, optional email, readOnly omits submit, hidden `votes` seam).
Output: reconciled vote-form + three participant pages; the mobile submit/banner stays visible over a
long date list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-CONTEXT.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md
@src/components/vote-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile vote-form.tsx to 2a–2e + implement the mobile sticky/pinned submit and closed banner (D-03)</name>
  <files>src/components/vote-form.tsx</files>
  <read_first>
    - src/components/vote-form.tsx (shipped form — name/email fields, AvailabilityGrid mount, readOnly branch that omits submit and shows the 'Voting is closed' banner)
    - design_handoff_vote_grid_redesign/designs/Vote Screen.dc.html (2a filled lines 43-116, 2b empty lines 119-182, 2c closed lines 188-228, 2d mobile-filled lines 230-303 incl. the pinned `mSubmitBar` at lines 296-299/402-410, 2e mobile-closed lines 305-355 incl. `mBannerBar`)
    - design_handoff_vote_grid_redesign/DESIGN.md §3 (type/spacing) + §5 (screen anatomy)
  </read_first>
  <action>
    Keep the shipped structure and behavior: `h2` heading (default "Your availability"), required Name +
    optional Email with the help text, `disabled={isPending || readOnly}` on inputs, the AvailabilityGrid
    mount UNCHANGED (`disabled`/`onChange`/`initial` + hidden `votes` input), and the readOnly branch that
    OMITS the submit button (not disabled) and renders the "Voting is closed" banner
    (`rounded-lg border bg-muted p-6 text-center`). The D-03 change: make the primary-action block (the
    submit button when open, the closed banner when readOnly) stay visible on mobile over a long date list
    by wrapping it as a `position: sticky` bottom bar on small viewports (e.g. `sticky bottom-0` with a
    background + top border on mobile, reverting to static flow at `sm:` per the prototype's
    `mSubmitBar`/`mBannerBar`; the `mobileScroll`/`mobileViewportH` knobs are demo-only). Submit stays
    `w-full sm:w-auto`. Reconcile only drifting classes/copy to the mock; introduce no new
    token/utility/font/animation and no dark-mode branch. Do not rename or re-wire the `readOnly`/`action`
    props.
  </action>
  <acceptance_criteria>
    - `vote-form.tsx` still mounts `AvailabilityGrid` with `disabled={isPending || readOnly}` and `onChange={setVotes}` and keeps the hidden `<input ... name="votes">` (grep confirms all three, contract unchanged).
    - The readOnly branch still OMITS the submit `<Button type="submit">` and renders the `bg-muted p-6` "Voting is closed" banner.
    - A `sticky` mobile positioning class is applied to the submit/banner block (grep confirms `sticky`), reverting to static at `sm:`.
    - `npm run build` and `npm run lint` pass with no new errors.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -Eq "name=\"votes\"" src/components/vote-form.tsx && grep -q "sticky" src/components/vote-form.tsx && grep -q "bg-muted" src/components/vote-form.tsx && echo VOTEFORM_OK</automated>
    <human-check>On a narrow viewport with many candidate dates, the Submit button (open) / "Voting is closed" banner (closed) stays pinned/visible while the date list scrolls — matching mocks 2d/2e; desktop 2a–2c unchanged.</human-check>
  </verify>
  <done>vote-form.tsx matches 2a–2e, the mobile submit/banner stays visible over a long list (D-03), and the AvailabilityGrid + readOnly contract is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the participant vote / thanks / edit pages to boards 2a–2c, 3b, 3c</name>
  <files>src/app/p/[participantUrlId]/page.tsx, src/app/p/[participantUrlId]/thanks/page.tsx, src/app/p/[participantUrlId]/edit/[editToken]/page.tsx</files>
  <read_first>
    - src/app/p/[participantUrlId]/page.tsx (vote page shell — mounts VoteForm; `readOnly={poll.status !== "open"}`)
    - src/app/p/[participantUrlId]/thanks/page.tsx (confirmation + edit-link card)
    - src/app/p/[participantUrlId]/edit/[editToken]/page.tsx (edit route — same VoteForm, heading/submit differ)
    - design_handoff_vote_grid_redesign/designs/Vote Screen.dc.html (page anatomy 2a–2c) + design_handoff_vote_grid_redesign/designs/App Screens.dc.html (3b thanks, 3c edit)
    - design_handoff_vote_grid_redesign/README.md (Thanks + Edit sections — exact copy)
  </read_first>
  <action>
    Reconcile page shells/copy only. Vote page: `main.mx-auto.max-w-2xl.px-4.py-12` `flex-col gap-8`, `h1`
    poll title (Display 30/600) + PollSummary (description, location), optional returning-participant note,
    then VoteForm — keep `readOnly={poll.status !== "open"}` threading unchanged. Thanks page (3b): heading
    "Thanks for responding!" + "Your availability has been saved.", the personal-edit-link `Card` with the
    AMBER bearer-credential warning ("Don't share this link — anyone who has it can change your answer."),
    the mono link + `Copy link`, and the footer "No email was sent — save this link now." Edit page (3c):
    identical to the vote screen but heading "Edit your availability", values prefilled, submit "Save
    changes" (passed through VoteForm's `heading`/`submitLabel`). Adjust only drifting classes/copy; do not
    surface any admin `/a/` URL on these pages (SEC/D-10); no new primitives/tokens/utilities/fonts.
  </action>
  <acceptance_criteria>
    - Thanks page renders the amber bearer-credential warning copy and a `Copy link` control (grep confirms the warning text + copy affordance).
    - Edit page passes "Edit your availability" heading and "Save changes" submit label to VoteForm (grep confirms both strings).
    - No `/a/` admin URL is constructed or rendered on any of the three pages (grep for admin-link construction returns nothing participant-facing).
    - Existing participant-route tests stay green: `npm test -- p/` (or the shipped page.test.ts suites) exits 0; `npm run build` + `npm run lint` pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -12</automated>
    <automated>grep -rq "Edit your availability" src/app/p/[participantUrlId]/edit && grep -rq "Save changes" src/app/p/[participantUrlId]/edit && echo EDIT_OK</automated>
    <human-check>Compare `/p/[id]` (open + closed), `/p/[id]/thanks`, and `/p/[id]/edit/[token]` against boards 2a–2c, 3b, 3c — headings, the amber warning on thanks, prefilled edit values + "Save changes".</human-check>
  </verify>
  <done>The three participant pages match their mocks; readOnly threading is preserved; no admin URL leaks; shipped participant tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant edit-link (per-participant `editToken`) → edit page | Bearer credential. This plan is visual-only; the thanks page must keep the amber "don't share this link" warning and must NOT expose any admin URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information disclosure | participant pages | mitigate | Grep-verify no admin `/a/` URL is rendered on vote/thanks/edit (SEC/D-10); participants see only the participant link + their own edit link; the amber bearer-credential warning stays on the thanks page. |
| T-05-08 | Tampering | readOnly threading | mitigate | Keep `readOnly={poll.status !== "open"}` → VoteForm → AvailabilityGrid `disabled`; the closed poll still OMITS the submit button (not merely disabled). Preserved verbatim; build typecheck confirms the prop chain. |

ASVS L1: no high-severity threat — visual reconciliation over shipped participant surfaces; token/access
model and readOnly enforcement are untouched. No package installs in this plan.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- Mobile sticky/pinned submit + closed-banner wrapper in `vote-form.tsx` (new responsive positioning; D-03 form half) — no new symbol/export.
- Copy/class adjustments to the three participant pages. Record per-file "drift fixed" vs "no drift" in the SUMMARY. No new files, dependencies, tokens, utilities, or fonts.
</artifacts_produced>

<verification>
- `npm run build`, `npm run lint`, and the full `npm test` (participant-route suites) pass.
- Grep confirms the unchanged AvailabilityGrid mount + hidden `votes` input, the `sticky` mobile submit class, the amber thanks warning, and "Edit your availability"/"Save changes".
- (end-of-phase human check) 2a–2c / 3b / 3c match, and the mobile submit/banner stays visible over a long date list.
</verification>

<success_criteria>
- vote-form + the three participant pages match their mocks (D-09); the mobile submit/banner is sticky/pinned (D-03 form half).
- AvailabilityGrid mount contract, readOnly submit-omission, and the hidden `votes` seam are preserved.
- No admin URL leaks (SEC/D-10); no new token/utility/font/animation; shipped participant tests green.
</success_criteria>

<output>
Create `.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-03-SUMMARY.md` when done.
</output>

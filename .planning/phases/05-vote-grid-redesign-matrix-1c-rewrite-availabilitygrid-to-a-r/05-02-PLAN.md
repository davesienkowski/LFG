---
phase: 05-vote-grid-redesign-matrix-1c
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/results-grid.tsx
  - src/components/invite-by-email-form.tsx
  - src/components/book-it-control.tsx
  - src/app/a/[adminUrlId]/page.tsx
autonomous: true
requirements: []          # Visual reconciliation of shipped DASH-*/MAIL-*/FNL-* surfaces; no new requirements.
user_setup: []

must_haves:
  truths:
    - "D-09: ResultsGrid matches board 3d/3e — participants x dates table, best-day column emerald-100/800 tint + literal 'Best' badge + `N yes · N if-need-be` tallies, Date/Status filter + Clear, horizontal scroll-edge fade on narrow widths; adjusted ONLY where classes drift (no structural rewrite)"
    - "D-09: admin open (3d) shows Share cards (participant link; admin link with amber 'Keep private' badge + do-not-share copy) + Invite-by-email card; admin finalized (3e) adds an emerald 'Booked' pill + 'Poll finalized' emerald card and HIDES the Invite card and Book-it picker; Results stays visible in both"
    - "D-09: InviteByEmailForm per-recipient chips (Sent / Rate limited / Failed) each render icon AND text (never color alone); BookItControl keeps the two-step confirm — `Book this date` (type=button, reveals the amber panel only) then `Confirm and close poll` (type=submit, the only control that closes), spatially separated from the ghost `Keep poll open`"
    - "SEC/D-10: no admin-only secret leaks — the admin 'Keep private' warning stays on the admin-link card; this is the admin surface so admin URLs are expected here, but the Book-it and Invite controls are the ONLY closing/sending controls and remain admin-gated + hidden when finalized"
    - "No structural rewrite, no new component/token/utility/font/animation, no dark-mode branch; shipped tests (results-grid.test.tsx, book-it-control.test.tsx) stay green"
  artifacts:
    - path: "src/components/results-grid.tsx"
      provides: "Results grid reconciled to 3d/3e (best-day tint+badge+tallies, filter+clear, scroll fade)"
    - path: "src/components/book-it-control.tsx"
      provides: "Two-step Book-it confirm reconciled to 3d; hidden when finalized"
    - path: "src/components/invite-by-email-form.tsx"
      provides: "Invite-by-email card + per-recipient icon+text chips reconciled to 3d"
    - path: "src/app/a/[adminUrlId]/page.tsx"
      provides: "Admin page shell — share cards, Booked pill, Poll-finalized card, conditional hide of invite/book-it when closed"
  key_links:
    - from: "src/app/a/[adminUrlId]/page.tsx"
      to: "poll.status"
      via: "closed → hide Invite card + Book-it picker, show Poll-finalized card + Booked pill"
      pattern: "status"
---

<objective>
Reconcile the admin/results surface (mock boards 3d open + 3e finalized) to pixel targets under D-09 —
`ResultsGrid`, `InviteByEmailForm`, `BookItControl`, and the admin page shell `/a/[adminUrlId]`.
These were shipped in Phases 3–4; adjust Tailwind classes / copy ONLY where they drift from the mocks.
No structural rewrite.

Purpose: the organizer's dashboard visually matches the high-fidelity handoff while preserving every
shipped behavior (best-day ranking, filter, two-step finalize, per-recipient send status, closed-poll
hiding of invite/book-it).
Output: reconciled admin components + page shell; all shipped admin tests still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-CONTEXT.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md
@src/lib/vote-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile ResultsGrid + admin page shell to boards 3d/3e (best-day, filter, share cards, Booked/finalized)</name>
  <files>src/components/results-grid.tsx, src/app/a/[adminUrlId]/page.tsx</files>
  <read_first>
    - src/components/results-grid.tsx (current shipped grid — filter, tallies, best-day, scroll fade)
    - src/app/a/[adminUrlId]/page.tsx (admin page shell — share cards, status-conditional sections)
    - design_handoff_vote_grid_redesign/designs/App Screens.dc.html (boards 3d admin-open + 3e admin-finalized; `-m` mobile variant — HTML is source of truth)
    - design_handoff_vote_grid_redesign/README.md (Results/Admin section + fidelity notes)
    - src/lib/vote-state.ts (palette reused verbatim)
  </read_first>
  <action>
    Diff the shipped `ResultsGrid` render and the admin page against boards 3d/3e and change ONLY drifting
    Tailwind classes / copy — do not restructure. Confirm/adjust to the mock: best-day column tint
    `emerald-100`/text `emerald-800` with a literal "Best" text badge; per-date `N yes · N if-need-be`
    tallies; Date + Status filter controls with a `Clear` reset; horizontal scroll-edge fade on narrow
    widths (the shipped `SCROLL_FADE_STYLE`/`overflow-x-auto` — keep as-is, it is the correct results-grid
    pattern and is explicitly NOT the vote-grid mobile pattern). On the admin page: the participant-link
    `Card`, the admin-link `Card` carrying the amber "Keep private" badge + do-not-share copy, and — when
    `poll.status !== "open"` — an emerald "Booked" pill on the title and a "Poll finalized" emerald card
    (copy per 3e), while the Invite card and Book-it picker are omitted. Reuse existing primitives
    (`Card`, `Button`); introduce no new token, utility, font, animation, or dark-mode branch. If a surface
    already matches the mock, leave it unchanged and record "no drift" in the SUMMARY.
  </action>
  <acceptance_criteria>
    - Best-day tint (`emerald-100`/`emerald-800`) + literal "Best" badge + `yes · if-need-be` tallies present in ResultsGrid (grep confirms the emerald classes + "Best").
    - Admin page renders the amber "Keep private" badge on the admin-link card, and gates the "Poll finalized"/Booked pill vs the Invite/Book-it sections on `poll.status`.
    - `npm run build` succeeds; `npm test -- results-grid` exits 0 (shipped grid behavior unbroken).
    - `npm run lint` reports no new errors.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test -- results-grid 2>&1 | tail -8</automated>
    <human-check>Open `/a/[adminUrlId]` for an open poll and a finalized poll; compare against boards 3d/3e — best-day tint/badge/tallies, filter+Clear, scroll fade, "Keep private" admin card, and (finalized) Booked pill + Poll-finalized card with Invite/Book-it hidden.</human-check>
  </verify>
  <done>ResultsGrid and the admin shell match boards 3d/3e; best-day/filter/scroll-fade behavior and closed-poll hiding are intact; results-grid tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile InviteByEmailForm + BookItControl to board 3d (icon+text chips, two-step confirm)</name>
  <files>src/components/invite-by-email-form.tsx, src/components/book-it-control.tsx</files>
  <read_first>
    - src/components/invite-by-email-form.tsx (Textarea + Send invites + per-recipient chips)
    - src/components/book-it-control.tsx (+ src/components/book-it-control.test.tsx — the two-step confirm test that must stay green)
    - design_handoff_vote_grid_redesign/designs/App Screens.dc.html (board 3d — Invite-by-email card + Book-it two-step confirm)
    - design_handoff_vote_grid_redesign/README.md (Results/Admin section — chip states + two-step confirm wording)
  </read_first>
  <action>
    Reconcile visuals only. InviteByEmailForm: confirm the Textarea + `Send invites` layout and that each
    per-recipient status chip (Sent / Rate limited / Failed) carries a lucide icon AND its text label
    (icon-or-color-never-alone), adjusting only drifting classes. BookItControl: preserve the shipped
    two-step confirm exactly — `Book this date` is `type="button"` and ONLY reveals the amber confirm panel
    (it must never submit/close), and `Confirm and close poll` is the sole `type="submit"` control, kept
    spatially separated from the ghost `Keep poll open`; the computed best day stays pre-checked with a
    "Suggested" badge. Adjust only drifting Tailwind classes/copy to match 3d; do not change the control
    types, the submit wiring, or the admin-gating. No new primitives/tokens/utilities.
  </action>
  <acceptance_criteria>
    - InviteByEmailForm chips render icon + text for Sent / Rate limited / Failed (grep confirms lucide icon imports alongside the status labels).
    - BookItControl retains exactly one `type="submit"` (`Confirm and close poll`) and `Book this date` remains `type="button"` (grep confirms both).
    - `npm test -- book-it-control` exits 0 (two-step confirm behavior unbroken).
    - `npm run build` and `npm run lint` pass with no new errors.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- book-it-control 2>&1 | tail -8 && grep -c "type=\"submit\"" src/components/book-it-control.tsx</automated>
    <human-check>On an open poll's `/a/[adminUrlId]`, confirm invite chips show icon+text per state and that `Book this date` only reveals the amber panel (never closes) while `Confirm and close poll` is the only control that finalizes — matching board 3d.</human-check>
  </verify>
  <done>Invite chips and the Book-it two-step confirm match board 3d visually; the first button never submits, the confirm button is the only closer; book-it tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizer browser → `sendInvites` / `closePoll` server actions | Admin-gated actions (send email, finalize poll). This plan is visual-only; it must NOT weaken the admin gate, the two-step confirm, or the closed-poll hiding of these controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation of privilege | BookItControl finalize | mitigate | Preserve the two-step confirm exactly — `Book this date` stays `type="button"` (reveal only); `Confirm and close poll` stays the sole `type="submit"`; admin-gating in the action is untouched. Task 2 greps confirm the control types; book-it-control.test.tsx stays green. |
| T-05-05 | Information disclosure | admin finalized view | mitigate | When `poll.status !== "open"` the Invite card + Book-it picker are hidden (3e); no new leak of tokens/PII beyond the shipped admin surface (admin URLs are expected on the admin page and carry the "Keep private" warning). |
| T-05-06 | Spoofing | invite send | accept | Per-recipient chip states are display-only over the shipped `sendInvites` result; no change to send logic or dedupe. |

ASVS L1: no high-severity threat — visual reconciliation over shipped admin-gated actions; control types,
gating, and status logic are preserved. No package installs in this plan.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- No new symbols/files — visual reconciliation only. Any change is a Tailwind class / copy adjustment within the four listed files. New behavior surface: none. Record per-file "drift fixed" vs "no drift" in the SUMMARY.
</artifacts_produced>

<verification>
- `npm run build`, `npm run lint`, `npm test -- results-grid`, and `npm test -- book-it-control` all pass.
- Grep confirms best-day emerald classes + "Best" badge, single `type="submit"` in book-it-control, and icon+text invite chips.
- (end-of-phase human check) Admin open + finalized views match boards 3d/3e.
</verification>

<success_criteria>
- ResultsGrid, InviteByEmailForm, BookItControl, and the admin shell match boards 3d/3e (D-09).
- Two-step finalize, admin gating, best-day ranking, filter, and closed-poll hiding are all preserved.
- No structural rewrite, no new token/utility/font/animation; shipped admin tests green.
</success_criteria>

<output>
Create `.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-02-SUMMARY.md` when done.
</output>

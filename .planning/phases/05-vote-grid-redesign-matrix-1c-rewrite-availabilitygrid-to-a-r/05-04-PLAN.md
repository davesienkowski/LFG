---
phase: 05-vote-grid-redesign-matrix-1c
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/poll-create-form.tsx
  - src/components/calendar-date-picker.tsx
  - src/app/page.tsx
autonomous: true
requirements: []          # Visual reconciliation of the shipped POLL-* create surface; no new requirements.
user_setup: []

must_haves:
  truths:
    - "D-09: the create screen (board 3a / 3a-m) matches the mock — `Create a poll` Display title, Poll title* (PollCreateForm), Description (Textarea), Location, then 'Candidate dates' (CalendarDatePicker), and a `Create poll` button; adjusted ONLY where classes/copy drift (no structural rewrite of the Server Action form)"
    - "D-09: CalendarDatePicker matches 3a — month calendar `mode=\"multiple\"` with past days disabled; right pane = Default start time + `Apply to all` + a chronologically sorted Selected-dates list with a per-row time input + remove; the shipped client-island + serialize seam is preserved"
    - "D-09: mobile (3a-m) stacks the two panes and pins the `Create poll` action"
    - "The create Server Action wiring (hidden serialized dates field, validation, redirect to `/a/[adminUrlId]`) is UNCHANGED — visual reconciliation only"
    - "No new component/token/utility/font/animation, no dark-mode branch; timezone-safe date handling (DATE strings, never `new Date()` on a date-only value) is preserved"
  artifacts:
    - path: "src/components/poll-create-form.tsx"
      provides: "Create-poll form reconciled to board 3a"
    - path: "src/components/calendar-date-picker.tsx"
      provides: "Multi-select month calendar + selected-dates pane reconciled to board 3a"
    - path: "src/app/page.tsx"
      provides: "Root create page shell reconciled to 3a / 3a-m (stacked + pinned action on mobile)"
  key_links:
    - from: "src/components/poll-create-form.tsx"
      to: "createPoll server action"
      via: "form action + hidden serialized candidate-dates field (unchanged)"
      pattern: "action"
---

<objective>
Reconcile the poll-creation surface (mock board 3a + 3a-m) to pixel targets under D-09 —
`PollCreateForm`, `CalendarDatePicker`, and the root create page `/`. Adjust Tailwind classes / copy
only where they drift; the `createPoll` Server Action wiring and the timezone-safe date serialization
are unchanged.

Purpose: the organizer's create flow visually matches the handoff while preserving the shipped
behavior (multi-select dates, per-date optional start time, validation, redirect to the admin page).
Output: reconciled create form + date picker + root page; the create Server Action still works end to
end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-CONTEXT.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile PollCreateForm + root create page to board 3a / 3a-m</name>
  <files>src/components/poll-create-form.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/poll-create-form.tsx (shipped Server Action create form — title/description/location + candidate dates)
    - src/app/page.tsx (root create page shell)
    - design_handoff_vote_grid_redesign/designs/App Screens.dc.html (board 3a create + 3a-m mobile — HTML is source of truth)
    - design_handoff_vote_grid_redesign/README.md (Create poll section — field list + mobile behavior)
  </read_first>
  <action>
    Diff the shipped create form + root page against board 3a and change ONLY drifting classes / copy —
    do not restructure the Server Action form. Confirm/adjust to the mock: `Create a poll` Display-30/600
    title; Poll title (required) `Input`; Description `Textarea`; Location `Input`; the "Candidate dates"
    section housing `CalendarDatePicker`; and the `Create poll` submit `Button`. On mobile (3a-m) the two
    panes stack and the `Create poll` action is pinned/visible. Reuse existing primitives
    (`Card`/`Input`/`Textarea`/`Label`/`Button`); introduce no new token/utility/font/animation/dark-mode
    branch. Keep the form `action`, hidden serialized-dates input, validation, and redirect wiring exactly
    as shipped. If a surface already matches, leave it and note "no drift" in the SUMMARY.
  </action>
  <acceptance_criteria>
    - The create form keeps its `createPoll` action wiring + the hidden serialized candidate-dates input (grep confirms the action + hidden field unchanged).
    - `Create a poll` title, required Poll title, Description, Location, and `Create poll` button all present.
    - `npm run build` and `npm run lint` pass; the shipped create-flow test(s) stay green (`npm test`).
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
    <human-check>Compare `/` (desktop + narrow) against boards 3a/3a-m — title, field order, and the pinned `Create poll` action on mobile.</human-check>
  </verify>
  <done>PollCreateForm and the root page match board 3a/3a-m; the create Server Action wiring is unchanged; build/lint/tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile CalendarDatePicker to board 3a (multi-select calendar + selected-dates pane)</name>
  <files>src/components/calendar-date-picker.tsx</files>
  <read_first>
    - src/components/calendar-date-picker.tsx (shipped client-island month calendar, `mode="multiple"`, serialize seam)
    - design_handoff_vote_grid_redesign/designs/App Screens.dc.html (board 3a right pane — Default start time + Apply to all + sorted Selected-dates list with per-row time + remove)
    - design_handoff_vote_grid_redesign/README.md (Create poll section)
  </read_first>
  <action>
    Reconcile visuals only. Confirm/adjust to the mock: the month calendar with past days disabled and
    `mode="multiple"` selection; the right pane's `Default start time` input + `Apply to all` control; and
    the chronologically SORTED Selected-dates list, each row carrying an optional per-date time input and a
    remove control. Preserve the shipped client-island + serialize seam (local state → hidden field) and
    the timezone-safe handling (date-only strings; never `new Date()` on a date-only value — the shipped
    P1/P3 invariant). Adjust only drifting Tailwind classes/copy; introduce no new
    token/utility/font/animation/dark-mode branch and no new date library.
  </action>
  <acceptance_criteria>
    - CalendarDatePicker keeps `mode="multiple"`, past-day disabling, the Default-start-time + Apply-to-all controls, and the sorted selected-dates list with per-row time + remove (grep/read confirms the pieces are intact).
    - No `new Date(` is introduced on a date-only value (timezone-safe handling preserved).
    - `npm run build` + `npm run lint` pass; `npm test` stays green.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "multiple" src/components/calendar-date-picker.tsx && echo PICKER_OK</automated>
    <human-check>On `/`, select several dates across month boundaries; confirm the calendar, Default start time + Apply to all, and the sorted selected-list with per-row time + remove match board 3a; dates render on the same calendar day (no off-by-one).</human-check>
  </verify>
  <done>CalendarDatePicker matches board 3a; multi-select, apply-to-all, sorted selected list, and timezone-safe serialization are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizer browser → `createPoll` server action | Untrusted create payload (title/dates) crosses here. This plan is visual-only; validation + serialization are UNCHANGED. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | date serialization | mitigate | Preserve the timezone-safe date-only handling (no `new Date()` on a date-only value) so candidate dates cannot silently drift a day; acceptance greps for no new `new Date(` on the picker. |
| T-05-10 | Input validation | createPoll | accept | Server-side Zod validation at the action boundary is unchanged; this plan touches only presentation. |

ASVS L1: no high-severity threat — visual reconciliation over the shipped, validated create action; no
new trust surface. No package installs in this plan.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- No new symbols/files — visual reconciliation only (Tailwind class / copy adjustments within the three listed files). Record per-file "drift fixed" vs "no drift" in the SUMMARY. No new dependencies, tokens, utilities, or fonts.
</artifacts_produced>

<verification>
- `npm run build`, `npm run lint`, and `npm test` pass.
- Grep confirms the unchanged `createPoll` action + hidden dates field, `mode="multiple"`, and no new `new Date(` on the picker.
- (end-of-phase human check) Create screen matches boards 3a / 3a-m; multi-select + per-date time + sorted list behave; no date off-by-one.
</verification>

<success_criteria>
- PollCreateForm, CalendarDatePicker, and the root create page match board 3a / 3a-m (D-09).
- The create Server Action wiring, validation, and timezone-safe date handling are preserved.
- No new token/utility/font/animation/date-library; shipped create tests green.
</success_criteria>

<output>
Create `.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-04-SUMMARY.md` when done.
</output>

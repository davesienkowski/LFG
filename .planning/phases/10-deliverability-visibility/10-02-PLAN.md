---
phase: 10-deliverability-visibility
plan: 02
type: execute
wave: 2
depends_on: [01]
gated_on: "Phase 9 Task 3 (prod credentials + redeploy) — D-19. SC1/SC2 are meaningless until prod email actually sends."
files_modified:
  - .planning/phases/10-deliverability-visibility/DELIVERABILITY-RUNBOOK.md
  - scripts/check-email-alignment.mjs
requirements: [DLVR-03]
covers_decisions: [D-17, D-18, D-19]
autonomous: false
user_setup:
  - "SC1 (DNS — Dave): publish/confirm SPF, DKIM, and DMARC records for the configured sender identity at Dave's DNS provider, per the free-tier recipe in DELIVERABILITY-RUNBOOK.md. No paid domain/plan (D-17). The agent supplies the record SHAPES; Dave supplies the actual records/tokens (secrets never enter the repo)."
  - "SC2 (real-inbox — Dave): after SC1 is live, trigger ONE real prod send (e.g. email the admin link on poll creation, or a nudge) to an inbox Dave controls, and confirm by eye it lands in the INBOX (not spam) with a WORKING link. Record the outcome to close the v1.0/v1.1 open check."
must_haves:
  truths:
    - "A DELIVERABILITY-RUNBOOK.md documents the FREE-TIER SPF/DKIM/DMARC alignment recipe for the configured provider(s) and the exact SC2 real-inbox verification steps — with NO secrets, keys, or DNS tokens in it (D-17/D-18)."
    - "A non-secret check-email-alignment.mjs script inspects PUBLIC DNS (SPF/DKIM/DMARC TXT records via dig/DNS lookup) for a sender domain passed as an ARGUMENT and reports pass/fail per record — reading nothing from env and printing no secret."
    - "SC1 and SC2 are recorded as HUMAN checks (D-18) in the Deferred Verification table, closed only by Dave — the plan does NOT self-close them and does NOT run a real send, vercel env, vercel deploy, or any DNS change."
    - "If free-tier alignment is found to be impossible for the chosen provider, execution STOPS and reports rather than selecting any paid domain/plan (D-17)."
  artifacts:
    - path: ".planning/phases/10-deliverability-visibility/DELIVERABILITY-RUNBOOK.md"
      provides: "Free-tier SPF/DKIM/DMARC alignment recipe + SC2 real-inbox verification steps (no secrets)"
      contains: "DMARC"
    - path: "scripts/check-email-alignment.mjs"
      provides: "Non-secret public-DNS alignment checker (domain as arg)"
      contains: "dmarc"
  prohibitions:
    - "MUST NOT run any external deploy, `vercel deploy`, `vercel env`, DNS change, or real production send — planning/scaffolding only (guardrail 3)."
    - "MUST NOT put any provider key, DKIM private key, DNS token, or credential into the runbook or the script — env/config seams and public-DNS lookups only (guardrail 2)."
    - "MUST NOT self-close SC1 or SC2 — they are Dave's human checks (D-18); the plan builds scaffolding AROUND them."
    - "MUST NOT select a paid domain or paid plan to satisfy SC1 — if free-tier alignment is not achievable, STOP and report (D-17)."
    - "MUST NOT read EMAIL_* secrets or DATABASE_URL in the alignment script — it takes a domain argument and queries public DNS only."
---

<objective>
DLVR-03 — the mail Phase 9 sends must LAND in the inbox (SC1: SPF/DKIM/DMARC
alignment, free tier) and be HUMAN-VERIFIED to do so (SC2: a real prod send Dave
sees in a real inbox, not spam, with a working link). Both are structurally
NOT agent-closable (D-18): SC1 needs Dave's DNS records; SC2 needs Dave's eyes on
a real send. This plan does the ONLY agent-buildable part — a free-tier alignment
RUNBOOK and a non-secret public-DNS alignment CHECKER — and records SC1/SC2 as
human checks to be closed by Dave, NOT re-deferred indefinitely: they are the
milestone's closing gate.

GATED on Phase 9 Task 3 (D-19): deliverability is meaningless until prod email
sends. No deploy, no vercel env, no DNS change, no real send happen here
(guardrail 3). If free-tier alignment proves impossible for the provider, STOP and
report rather than choosing a paid path (D-17).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-deliverability-visibility/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the free-tier deliverability runbook (no secrets)</name>
  <files>.planning/phases/10-deliverability-visibility/DELIVERABILITY-RUNBOOK.md</files>
  <read_first>
    - src/lib/email/send.ts (the D-03 EMAIL_FROM/DMARC-alignment invariant comment — the runbook operationalizes it)
    - src/lib/env.ts (the email env var NAMES — the runbook references names, never values)
    - .planning/REQUIREMENTS.md (v1.2 Out of Scope: no paid domain/plan — D-17 bounds the recipe)
  </read_first>
  <action>
    Author DELIVERABILITY-RUNBOOK.md documenting, for the provider(s) actually
    configured in Phase 9 (Gmail SMTP and/or the fallback), the FREE-TIER path to
    SPF, DKIM, and DMARC alignment for the EMAIL_FROM (and EMAIL_FALLBACK_FROM)
    sender identity: which TXT records to publish, how EMAIL_FROM must align with the
    authenticated sender (the D-03 trap: never a gmail From on a third-party relay),
    and a DMARC policy suitable for a small sender. Then the SC2 steps: trigger one
    real send, confirm inbox-not-spam + working link, what to check in the received
    headers (SPF=pass, DKIM=pass, DMARC=pass). Record SHAPES and steps ONLY — no
    keys, no DKIM private material, no DNS tokens (guardrail 2). Explicitly state the
    $0 boundary (D-17): if the provider cannot align on the free tier, STOP and
    report — do not buy a domain/plan.
  </action>
  <acceptance_criteria>
    - source: DLVR-03 / SC1 / SC2 / D-17 / D-18.
    - behavior: a secret-free runbook covers free-tier SPF/DKIM/DMARC alignment + the SC2 real-inbox verification, bounded by the $0 constraint.
    - test: `grep -iE "spf|dkim|dmarc" DELIVERABILITY-RUNBOOK.md` matches; a secret-scan (no KEY/TOKEN/PASSWORD values) is clean.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/10-deliverability-visibility/DELIVERABILITY-RUNBOOK.md && grep -qiE "dmarc" .planning/phases/10-deliverability-visibility/DELIVERABILITY-RUNBOOK.md && ! grep -qiE "BEGIN (RSA|PRIVATE)|api[_-]?key\s*[:=]\s*\S" .planning/phases/10-deliverability-visibility/DELIVERABILITY-RUNBOOK.md</automated>
  </verify>
  <done>A secret-free, $0-bounded runbook exists for both alignment (SC1) and the real-inbox check (SC2).</done>
</task>

<task type="auto">
  <name>Task 2: Non-secret public-DNS alignment checker</name>
  <files>scripts/check-email-alignment.mjs</files>
  <read_first>
    - scripts/ (existing repo scripts, if any, for the house style; else a plain node ESM script)
    - DELIVERABILITY-RUNBOOK.md (the records the checker verifies)
  </read_first>
  <action>
    Write scripts/check-email-alignment.mjs: takes a sender DOMAIN as argv[2],
    performs PUBLIC DNS TXT lookups (node dns/promises resolveTxt) for the SPF
    (domain), DMARC (_dmarc.domain), and a DKIM selector (selector._domainkey.domain,
    selector as an optional argv[3]) records, and prints a per-record pass/absent
    report with a non-zero exit if a required record is missing. It reads NOTHING
    from env (no EMAIL_*, no DATABASE_URL), embeds NO secret, and only queries public
    DNS. This lets Dave self-check alignment after publishing records, without any
    credential. Usage line at the top; no network target other than DNS.
  </action>
  <acceptance_criteria>
    - source: DLVR-03 / SC1 (verification scaffolding) / D-18.
    - behavior: `node scripts/check-email-alignment.mjs example.com` reports SPF/DKIM/DMARC presence from public DNS; reads no env; contains no secret.
    - test: the script runs against a domain arg and prints a report; `grep -iE "process.env.(EMAIL|DATABASE|SMTP|RESEND)" scripts/check-email-alignment.mjs` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-email-alignment.mjs example.com >/dev/null 2>&1; test $? -le 1 && ! grep -qiE "process\.env\.(EMAIL|DATABASE|SMTP|RESEND)" scripts/check-email-alignment.mjs</automated>
  </verify>
  <done>A non-secret, env-free public-DNS alignment checker exists for Dave to verify SC1 after publishing records.</done>
</task>

<task type="checkpoint">
  <name>Task 3: Record SC1 + SC2 as Dave's human checks (close the milestone gate)</name>
  <files>.planning/STATE.md (Deferred Verification), .planning/phases/10-deliverability-visibility/10-02-SUMMARY.md</files>
  <read_first>
    - .planning/STATE.md (the Deferred Verification table — the same human-check mechanism v1.0/v1.1 used)
    - .planning/milestones/v1.1-phases/08-scheduling-controls/08-04-SUMMARY.md (the human-check recording shape)
  </read_first>
  <action>
    CHECKPOINT — SC1/SC2 are Dave's (D-18). Record them as human-verification items
    with the runbook + checker as the exact resume steps: SC1 = publish + verify
    SPF/DKIM/DMARC (run check-email-alignment.mjs); SC2 = one real prod send lands in
    a real inbox, not spam, working link. State plainly this CLOSES the long-standing
    v1.0/v1.1 open inbox check — Dave confirms once, it is not re-deferred. The agent
    performs NO real send, NO vercel env/deploy, NO DNS change. If, while writing the
    runbook, free-tier alignment is found impossible for the configured provider,
    STOP and report to Dave instead of recording a paid path (D-17).
  </action>
  <acceptance_criteria>
    - source: DLVR-03 / SC1 / SC2 / D-17 / D-18.
    - behavior: SC1/SC2 recorded as human checks with concrete resume steps; no agent-side deploy/DNS/real-send occurred.
    - test: human — Dave publishes DNS + confirms a real inbox arrival; recorded in 10-02-SUMMARY.md and STATE.md.
  </acceptance_criteria>
  <verify>
    <manual>Dave publishes SPF/DKIM/DMARC (verified via check-email-alignment.mjs), triggers one real prod send, and confirms inbox-not-spam + working link. Record in 10-02-SUMMARY.md; only then are SC1/SC2 closed.</manual>
  </verify>
  <done>SC1/SC2 are documented human checks with a runbook + checker; closed only by Dave's DNS + real-inbox confirmation. No agent deploy/DNS/real-send performed.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
- `.planning/phases/10-deliverability-visibility/DELIVERABILITY-RUNBOOK.md` — free-tier SPF/DKIM/DMARC alignment + SC2 real-inbox steps (no secrets).
- `scripts/check-email-alignment.mjs` — non-secret, env-free public-DNS alignment checker.
- SC1/SC2 recorded as Dave's human checks (STATE.md Deferred Verification + 10-02-SUMMARY.md).
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs/scripts ↔ secrets | DNS tokens, DKIM private keys, provider keys must never enter the runbook or the script |
| agent ↔ production (DNS/email/deploy) | The agent must not change DNS, deploy, set env, or send real mail — those are Dave's |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information Disclosure | runbook / checker | mitigate | Record shapes + public-DNS lookups only; secret-scan gate on the runbook; checker reads no env |
| T-10-06 | Elevation / unsafe action | deploy/DNS/real send | mitigate | Prohibitions forbid any deploy/vercel/DNS/real-send; SC1/SC2 are checkpoint human checks (D-18) |
| T-10-07 | Spoofing (deliverability failure) | free-tier alignment limit | accept + escalate | If free-tier alignment is impossible, STOP and report — no paid path (D-17) |
</threat_model>

<verification>
- DELIVERABILITY-RUNBOOK.md covers SPF/DKIM/DMARC (free tier) + SC2 steps; secret-scan clean.
- scripts/check-email-alignment.mjs runs on a domain arg, reads no env, embeds no secret.
- SC1/SC2 recorded as human checks with resume steps; no agent deploy/DNS/real-send.
</verification>

<success_criteria>
- SC1: a free-tier SPF/DKIM/DMARC alignment recipe + a non-secret checker exist; the actual DNS is Dave's to publish (human check, not re-deferred).
- SC2: the real-inbox verification is a documented human check that Dave closes with one confirmed prod send — closing the v1.0/v1.1 open inbox check.
- $0 constraint held (D-17): no paid domain/plan proposed; STOP-and-report if free-tier alignment is impossible.
</success_criteria>

<output>
Create `.planning/phases/10-deliverability-visibility/10-02-SUMMARY.md` when done.
</output>

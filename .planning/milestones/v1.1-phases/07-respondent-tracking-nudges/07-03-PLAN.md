---
phase: 07-respondent-tracking-nudges
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/components/send-status-meta.ts
  - src/components/invite-by-email-form.tsx
  - src/components/whos-responded-card.tsx
  - src/components/nudge-control.tsx
  - src/app/a/[adminUrlId]/page.tsx
  - src/app/a/[adminUrlId]/page.test.ts
autonomous: true
requirements: [RESP-01, RESP-02]
must_haves:
  truths:
    - "The admin page shows a 'Who's responded' card between Results and Book it, listing every invited email with a Responded (emerald) or Not yet responded (amber) badge."
    - "The card shows 'N of M responded' with the mandatory caption 'Only counts people invited by email through this tool.'"
    - "When there are zero invitations the card shows the empty-state copy and no nudge control."
    - "The nudge control renders the active button only when emailConfigured AND !isClosed AND nonRespondentCount > 0; on a closed poll no nudge control renders at all."
    - "With zero non-respondents the nudge button is disabled and 'Everyone's responded — nothing to nudge.' is shown."
  artifacts:
    - path: "src/components/send-status-meta.ts"
      provides: "Shared SEND_STATUS_META (icon+label+className) imported by InviteByEmailForm and NudgeControl"
      contains: "SEND_STATUS_META"
    - path: "src/components/whos-responded-card.tsx"
      provides: "RSC card: summary + caption + badge list + nudge control slot"
      contains: "Who"
    - path: "src/components/nudge-control.tsx"
      provides: "Client island: useActionState(nudgeNonRespondents) form (adminUrlId only) + result chips"
      contains: "nudgeNonRespondents"
    - path: "src/app/a/[adminUrlId]/page.tsx"
      provides: "getInvitationTrackingForPoll fetch + WhosRespondedCard placement between Results and Book it"
      contains: "WhosRespondedCard"
  key_links:
    - from: "src/app/a/[adminUrlId]/page.tsx"
      to: "getInvitationTrackingForPoll"
      via: "await getInvitationTrackingForPoll(poll.id) then pass to WhosRespondedCard"
      pattern: "getInvitationTrackingForPoll"
    - from: "src/components/nudge-control.tsx"
      to: "nudgeNonRespondents"
      via: "useActionState with a form carrying ONLY a hidden adminUrlId input"
      pattern: "useActionState"
  prohibitions:
    - "Invited emails MUST NOT reach any participant-facing route, the results-grid payload, or any RSC prop that serializes to a participant browser (D-09 / three-token discipline)."
    - "The nudge <form> MUST submit ONLY adminUrlId — never a client-rendered recipient list (Prohibition Probe #1)."
    - "The 'N of M responded' summary MUST always render the disambiguating caption (Prohibition Probe #2) — never the bare stat."
    - "The Responded/Not-yet-responded status MUST always pair its color with a text label (WCAG 1.4.1) — never color alone; 'Not yet responded' MUST use amber, never the destructive/red failure styling."
    - "Extracting SEND_STATUS_META MUST NOT restyle the existing invite chips — InviteByEmailForm output stays visually identical."
---

<objective>
Render the two new admin surfaces from the UI-SPEC: the "Who's responded" card
(RESP-01) and the nested "Nudge non-respondents" control (RESP-02), wired into
the existing `/a/[adminUrlId]` admin page between Results and Book it. Reuse the
existing hand-rolled badge idiom and the invite chip styling verbatim (extracting
`SEND_STATUS_META` to a shared module so both forms share it without restyling).

Purpose: This is the organizer-visible payoff — see who's missing, nudge them in
one click — with every no-leak and Prohibition-Probe guard enforced at the UI
boundary.

Output: `send-status-meta.ts`, `whos-responded-card.tsx`, `nudge-control.tsx`,
admin-page wiring, admin page tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-respondent-tracking-nudges/07-CONTEXT.md
@.planning/phases/07-respondent-tracking-nudges/07-UI-SPEC.md
@.planning/phases/07-respondent-tracking-nudges/07-01-SUMMARY.md
@.planning/phases/07-respondent-tracking-nudges/07-02-SUMMARY.md
</context>

<artifacts_this_plan_produces>
- `src/components/send-status-meta.ts` — shared `SEND_STATUS_META` (moved from invite-by-email-form.tsx)
- `src/components/whos-responded-card.tsx` — `WhosRespondedCard` RSC
- `src/components/nudge-control.tsx` — `NudgeControl` client island
- Admin-page wiring in `src/app/a/[adminUrlId]/page.tsx`
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared SEND_STATUS_META; build WhosRespondedCard (RSC) + NudgeControl (client)</name>
  <read_first>
    - src/components/invite-by-email-form.tsx (SEND_STATUS_META object to extract; the useActionState + hidden-input + aria-live results-list pattern the NudgeControl mirrors; chip markup)
    - .planning/phases/07-respondent-tracking-nudges/07-UI-SPEC.md (New Surfaces contract: card placement/states/markup, badge classes, summary + mandatory caption copy, nudge control gating, aria-live, 44px h-11 button, Copywriting Contract)
    - src/app/a/[adminUrlId]/page.tsx (existing Card idiom `flex flex-col gap-4 p-6`, sibling h2 headings, the hand-rolled Booked/Keep-private badge spans, isClosed + emailConfigured gates)
    - src/lib/actions/nudge-non-respondents.ts (nudgeNonRespondents + SendInvitesState/SendInviteResult from 07-02)
  </read_first>
  <files>src/components/send-status-meta.ts, src/components/invite-by-email-form.tsx, src/components/whos-responded-card.tsx, src/components/nudge-control.tsx</files>
  <action>
    (a) Extract `SEND_STATUS_META` (the `Record<SendInviteStatus, { icon, label,
    className }>`) VERBATIM out of `invite-by-email-form.tsx` into a new
    `src/components/send-status-meta.ts` (import lucide icons + the
    `SendInviteStatus` type there); re-import it into `invite-by-email-form.tsx`
    and delete the local copy — the invite chips must stay visually identical
    (no restyle). UI-SPEC: "extract to a shared module if needed, do not restyle".

    (b) `WhosRespondedCard` (RSC, NO "use client") in `whos-responded-card.tsx`.
    Props: `{ invitations: { email: string; responded: boolean }[]; adminUrlId:
    string; isClosed: boolean; emailConfigured: boolean }`. Render a standalone
    `<Card className="flex flex-col gap-4 p-6">` with `<h2 className="text-2xl
    font-semibold leading-snug">Who's responded</h2>` (same nesting as Results/
    Book it). States (mutually exclusive, per UI-SPEC table):
      - EMPTY (`invitations.length === 0`): render only the empty-state body copy
        "No invitations sent yet. Invite people by email above and we'll track who's
        responded here." (text-base text-muted-foreground). No badge list, no nudge.
      - POPULATED: compute `respondedCount` and `M = invitations.length`; render the
        summary `<p className="text-base font-semibold">{respondedCount} of {M}
        responded</p>` IMMEDIATELY followed by the MANDATORY caption `<p
        className="text-sm text-muted-foreground">Only counts people invited by
        email through this tool.</p>` (Prohibition Probe #2 — never the bare stat).
        Then a `<ul className="flex flex-col gap-2">`, one `<li className="flex
        flex-wrap items-center gap-2 text-sm">` per invitation: `<span
        className="font-semibold">{email}</span>` + a hand-rolled status badge
        `<span className="inline-flex items-center rounded-full border px-2 py-0.5
        text-xs font-semibold ...">`. Responded badge classes:
        `border-emerald-300 bg-emerald-100 text-emerald-800` label "Responded".
        Not-yet-responded classes: `border-amber-200 bg-amber-50 text-amber-700`
        label "Not yet responded" (amber — NEVER destructive/red; it is an expected
        non-error state). No icon inside these two badges (text-on-color satisfies
        WCAG 1.4.1). Match the existing Booked/Best/Keep-private hand-rolled idiom.
      - Nudge slot: render `<NudgeControl>` ONLY when `!isClosed && emailConfigured`
        (POPULATED + open). On a closed poll render NO nudge control at all (hidden,
        not disabled). When `!emailConfigured`, render nothing for the nudge (the
        page's existing "Email isn't set up" fallback already covers this once for
        invites — do NOT duplicate it). Pass `nonRespondentCount = invitations
        filtered to !responded` and `adminUrlId` to NudgeControl.

    (c) `NudgeControl` (client, "use client") in `nudge-control.tsx`. Props:
    `{ adminUrlId: string; nonRespondentCount: number }`. Mirror InviteByEmailForm
    mechanics: `useActionState(nudgeNonRespondents, null)`, `<form action=
    {formAction}>` with a SINGLE `<input type="hidden" name="adminUrlId">` and NO
    recipient field (Prohibition Probe #1 — the server re-queries). A single submit
    `<Button className="h-11">` labelled "Nudge non-respondents", pending label
    "Nudging…", disabled when `isPending || nonRespondentCount === 0`. When
    `nonRespondentCount === 0`, ALSO render the visible copy "Everyone's responded
    — nothing to nudge." (text-muted-foreground) beside/under the disabled button
    (a positive completion state, not hidden). Render `state?.errors?._form?.[0]`
    as `<p role="alert" className="text-destructive text-sm">` (reuse the invite
    formError pattern) so the closed-poll/`_form` notice from the action surfaces.
    Render `state?.results` as an `aria-live="polite"` `<ul>` of chips using the
    SHARED `SEND_STATUS_META` (icon + label + className), replaced entirely on each
    submit — identical to the invite results list. Never color alone.
  </action>
  <acceptance_criteria>
    - source: UI-SPEC "New Surfaces" (card states, badge classes, summary + mandatory caption, nudge gating, h-11, aria-live) + Copywriting Contract; Prohibition Probe #1/#2; CONTEXT nudge/degradation rules.
    - behavior: empty vs populated-open vs populated-closed states render per the UI-SPEC table; caption always present when the stat shows; nudge form posts only adminUrlId; zero-non-respondents disables the button with the completion copy.
    - test: invite-by-email-form.tsx still imports and renders the same chip styling (no visual regression); build/lint pass for the new components.
    - test: `npm run lint` clean and `npm run build` (or `next build` typecheck) passes for the four touched component files.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>Shared SEND_STATUS_META extracted (no restyle); WhosRespondedCard renders all three states with mandatory caption and correct badge colors; NudgeControl posts only adminUrlId, gates on nonRespondentCount, and reuses the invite chip styling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire WhosRespondedCard into the admin page + no-leak + state tests</name>
  <read_first>
    - src/app/a/[adminUrlId]/page.tsx (fetch sequence: getPollWithWinningOption -> getOptionsForPoll -> getResultsForPoll -> computeResults; the isClosed + emailConfigured computations; the Results Card and the Book-it block between which the new card goes)
    - src/app/a/[adminUrlId]/page.test.ts (how the admin page is exercised: mocks, how rendered output/props are asserted, existing no-leak/token assertions)
    - src/lib/db/queries.ts (getInvitationTrackingForPoll from 07-02)
  </read_first>
  <files>src/app/a/[adminUrlId]/page.tsx, src/app/a/[adminUrlId]/page.test.ts</files>
  <action>
    In the admin RSC, after the existing reads, add `const invitations = await
    getInvitationTrackingForPoll(poll.id)` (import from "@/lib/db/queries"). Render
    `<WhosRespondedCard invitations={invitations} adminUrlId={poll.adminUrlId}
    isClosed={isClosed} emailConfigured={emailConfigured} />` in the JSX BETWEEN the
    Results `<Card>` and the Book-it block (UI-SPEC placement: below Results, above
    Book it — the "see grid -> see who's missing -> decide whether to nudge before
    booking" flow). Reuse the EXISTING `isClosed` and `emailConfigured` values (do
    not recompute). Do NOT pass invitation data to ResultsGrid, the participant
    surface, or any other component — it is admin-only.

    Extend `page.test.ts`: (a) EMPTY — a poll with zero invitations renders the
    empty-state copy and no nudge button; (b) POPULATED OPEN — invited emails render
    with Responded/Not-yet-responded badges, the "N of M responded" stat AND the
    mandatory caption "Only counts people invited by email through this tool." both
    appear, and the nudge button renders; (c) POPULATED CLOSED — the badge list +
    summary render but NO nudge control appears; (d) NO-LEAK (non-vacuous, CANARY
    technique): seed an invitation with a distinctive canary email on a poll, render
    the PARTICIPANT page (or assert the participant-facing query/result payload) and
    prove the canary email string is ABSENT from any participant-facing output —
    invited emails never cross to a participant browser (D-09). If the admin page
    test cannot render the participant page, assert instead that no participant
    route module imports getInvitationTrackingForPoll (grep-style) AND that the
    participant page test payload contains no invitations emails.
  </action>
  <acceptance_criteria>
    - source: UI-SPEC card placement (between Results and Book it) + states; CONTEXT no-leak discipline (D-09); Prohibition Probe #2 caption.
    - behavior: card appears in the correct position; empty/open/closed states match the UI-SPEC table; invited emails never appear on a participant surface.
    - test: page.test.ts cases (a)-(d) pass; the CANARY leak assertion is non-vacuous (a real distinctive email is seeded and proven absent from participant output).
    - test: `npm test -- src/app/a/[adminUrlId]/page.test.ts` green (DB-backed; DATABASE_URL exported), plus the participant-page no-leak assertion.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/app/a/[adminUrlId]/page.test.ts src/app/p/[participantUrlId]/page.test.ts</automated>
  </verify>
  <done>The admin page fetches and renders WhosRespondedCard between Results and Book it with correct state behavior; a non-vacuous canary test proves invited emails never reach a participant surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin RSC -> participant browser | The results-grid payload and every participant RSC prop must carry NO invitation email (D-09) |
| nudge client island -> nudgeNonRespondents | The form crosses only adminUrlId; the server recomputes recipients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | admin page / ResultsGrid payload | mitigate | Invitation data flows ONLY into WhosRespondedCard on the admin page; never into ResultsGrid or a participant route; non-vacuous CANARY test proves absence from participant output |
| T-07-02 | Spoofing / Elevation of Privilege | nudge-control.tsx form | mitigate | Form has only a hidden adminUrlId input; no recipient list is rendered into or read from the form (server re-queries) |
| T-07-07 | Information Disclosure (a11y misread) | "N of M responded" stat | mitigate | Mandatory caption always rendered with the stat so it is never misread as total participation (Prohibition Probe #2) |
| T-07-SC | Tampering (supply chain) | npm installs | accept | No new dependencies — reuses installed shadcn Button/Card/Label + lucide-react (no new shadcn Badge block installed) |
</threat_model>

<verification>
- `npm test -- src/app/a/[adminUrlId]/page.test.ts src/app/p/[participantUrlId]/page.test.ts` green.
- `npm run lint && npx tsc --noEmit` clean.
- Grep confirms no participant-facing route imports `getInvitationTrackingForPoll`.
- Manual visual parity: invite chips unchanged after SEND_STATUS_META extraction.
</verification>

<success_criteria>
- The "Who's responded" card renders between Results and Book it with correct empty/open/closed states, the mandatory caption, and correct emerald/amber badges.
- The nudge control renders only when emailConfigured + open + non-respondents exist; posts only adminUrlId; disables with the completion copy at zero non-respondents.
- A non-vacuous canary test proves invited emails never reach a participant surface.
</success_criteria>

<output>
Create `.planning/phases/07-respondent-tracking-nudges/07-03-SUMMARY.md` when done.
</output>

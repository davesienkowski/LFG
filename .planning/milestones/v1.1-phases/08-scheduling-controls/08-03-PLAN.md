---
phase: 08-scheduling-controls
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/lib/actions/save-organizer-availability.ts
  - src/lib/actions/save-organizer-availability.test.ts
  - src/lib/db/queries.ts
  - src/lib/results.ts
  - src/components/results-grid.tsx
  - src/components/organizer-availability-control.tsx
  - src/app/a/[adminUrlId]/page.tsx
autonomous: true
requirements: [ORG-01]
user_setup: []
must_haves:
  truths:
    - "The organizer can add and edit their own availability row from the admin view without using the participant link."
    - "There is at most ONE organizer row (is_organizer=true) per poll — the add/edit action upserts the existing row, never creating a duplicate."
    - "The organizer row is a normal participant: it appears in the results grid and the best-day/computeResults tally with zero changes to computeResults."
    - "The organizer row is labelled '(you)' in the results grid via the is_organizer flag — presentation only, never inferred from the name string."
    - "Saving/editing the organizer's availability is authorized only by re-deriving the poll from the admin token, requires no email, and is rejected when voting is not open."
    - "The single-organizer-row invariant is enforced by the action's find-or-create upsert plus the form's isPending single-submit guard (single-admin token model, mirroring the existing no-DB-constraint participant precedent); no duplicate-creating code path is introduced (edge-probe ORG-01 concurrency resolution)."
    - "The emailless organizer row never affects Phase-7 respondent tracking — invitation matching is email-based, so a null-email participant matches no invitation and never flips a 'responded' count (edge-probe ORG-01 cross-feature resolution)."
  artifacts:
    - path: "src/lib/actions/save-organizer-availability.ts"
      provides: "Admin-token-authorized upsert of the single organizer participant row + its votes"
      exports: ["saveOrganizerAvailability"]
    - path: "src/components/organizer-availability-control.tsx"
      provides: "Admin-only client island wrapping AvailabilityGrid + a name input (add/edit/cancel)"
    - path: "src/lib/actions/save-organizer-availability.test.ts"
      provides: "DB-backed coverage: first add inserts one is_organizer row, edit upserts same row (no duplicate), closed/expired poll rejected, unknown token notFound"
  key_links:
    - from: "src/lib/actions/save-organizer-availability.ts"
      to: "participants.is_organizer + votes"
      via: "getPollByAdminUrlId re-derivation → find-or-create the single organizer row → vote upsert"
      pattern: "is_organizer|isOrganizer"
    - from: "src/lib/db/queries.ts (getResultsForPoll)"
      to: "src/components/results-grid.tsx"
      via: "ResultsParticipant.isOrganizer flag → '(you)' suffix"
      pattern: "isOrganizer"
  prohibitions:
    - "MUST NOT create a second organizer row — find the existing is_organizer=true row and upsert it (LOCKED constraint 6, at most one per poll)."
    - "MUST NOT change computeResults or getResultsForPoll's aggregation semantics — the organizer row folds in as a normal participant (SC5); the only read change is selecting is_organizer."
    - "MUST NOT trust a client-supplied poll id or participant id — re-derive the poll from the admin token (LOCKED constraint 7 / V4 precedent)."
    - "MUST NOT require an email for the organizer row (LOCKED constraint 6) and MUST NOT leak any email through the organizer row (results grid omits emails already)."
    - "MUST NOT render the editable organizer form once voting is closed — hide the card (no row yet) or show read-only (row exists), matching the participant readOnly precedent (UI Probe #1)."
    - "MUST NOT infer the '(you)' suffix from a name equal to 'You' — the suffix is driven ONLY by is_organizer=true (UI Probe #5)."
    - "MUST NOT attach any email hook (confirmation / creator-notify) to saveOrganizerAvailability — it is its own action, not submit/update-response (prohibition-probe: hook bleed)."
---

<objective>
Deliver ORG-01: the organizer adds and edits THEIR OWN availability row straight
from the admin view — no participant link, no email. The row is a normal
participant with `is_organizer=true` (at most one per poll, upserted), so it
appears in the results grid and best-day computation with zero changes to
computeResults; the grid labels it "(you)" from the flag alone.

Purpose: Success Criteria 4–5 for Phase 8. The load-bearing invariants: exactly
one organizer row (upsert, never duplicate — LOCKED constraint 6), admin-token
authorization (LOCKED constraint 7), the SAME isVotingOpen gate as participants
(CONTEXT default), and no aggregation change (SC5).

Output: saveOrganizerAvailability action; getResultsForPoll surfaces is_organizer;
the results grid "(you)" suffix; the admin "Your availability" card.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-scheduling-controls/08-CONTEXT.md
@.planning/phases/08-scheduling-controls/08-UI-SPEC.md
@.planning/phases/08-scheduling-controls/08-01-SUMMARY.md
@.planning/phases/08-scheduling-controls/08-02-SUMMARY.md
@src/lib/poll-status.ts
@src/lib/actions/update-response.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: saveOrganizerAvailability action (single-row upsert) + organizer-row read helper</name>
  <files>src/lib/actions/save-organizer-availability.ts, src/lib/actions/save-organizer-availability.test.ts, src/lib/db/queries.ts</files>
  <read_first>
    - src/lib/actions/update-response.ts (the vote-upsert template: Zod name/votes, build rows from the AUTHORITATIVE getOptionsForPoll list gap-filling untouched to "no", single insert(votes).onConflictDoUpdate on [participantId, optionId]=votes_participant_option_unique, refresh name — mirror this exactly for the organizer's votes)
    - src/lib/actions/submit-response.ts:121-154 (the participant INSERT with a minted editToken + the isUniqueViolation retry loop — the organizer's FIRST-add insert needs the same editToken mint; email is null)
    - src/lib/actions/close-poll.ts (admin-token re-derivation: getPollByAdminUrlId → notFound → redirect to /a/{adminUrlId})
    - src/lib/poll-status.ts (isVotingOpen — the gate for whether the organizer may still edit)
    - src/lib/db/queries.ts:159-190 (getResultsForPoll — the shape you extend in Task 2; here add a focused getOrganizerParticipant(pollId) helper returning the single is_organizer row's id/name or null)
    - .planning/phases/08-scheduling-controls/08-CONTEXT.md ORG-01 decisions (upsert, at-most-one, name defaults "You", no email, isVotingOpen gate)
  </read_first>
  <behavior>
    - saveOrganizerAvailability (no organizer row yet, all dates answered): INSERTS exactly one participants row with is_organizer=true, name = provided name or "You", email null, a freshly minted editToken; INSERTS one vote per authoritative option (untouched → "no"); redirects to /a/{adminUrlId}. Exactly one is_organizer=true row exists afterward.
    - saveOrganizerAvailability (organizer row already exists): UPDATES that row's name and UPSERTS its votes on the SAME participantId — still exactly one is_organizer=true row (no duplicate).
    - saveOrganizerAvailability on a CLOSED poll OR an open poll whose deadline has passed (!isVotingOpen): returns a _form error; NO participant/vote write.
    - saveOrganizerAvailability (unknown adminUrlId): notFound().
    - Votes are built from the server's authoritative option list; a foreign optionId in the payload is ignored; a missing one gap-fills to "no".
  </behavior>
  <action>
    Per ORG-01 (LOCKED constraints 6, 7; CONTEXT ORG-01):

    1. queries.ts — add `getOrganizerParticipant(pollId)` selecting the single
       `is_organizer = true` participant's `{ id, name }` for the poll (limit 1), or
       null. This lets the action decide insert-vs-update and gives the votes upsert a
       participantId. (Participant-safe columns only; no email/editToken projected.)
    2. Create src/lib/actions/save-organizer-availability.ts ("use server"):
         - Zod-validate: `name` optional trimmed, max 100, DEFAULT "You" when blank
           (never require an email — LOCKED 6); `votes` array of { optionId, state ∈
           yes|ifneedbe|no } (same schema as update-response.ts). Parse the JSON "votes"
           hidden field defensively (a bad payload → gap-fill all to "no").
         - Re-derive the poll from `adminUrlId` via getPollByAdminUrlId; notFound() on
           miss (LOCKED 7 — never a client poll/participant id).
         - GATE: `if (!isVotingOpen(poll, new Date())) return { errors: { _form:
           ["Voting is closed — you can no longer change your availability."] } }` with
           NO write (CONTEXT default: same rule as participants; UI Probe #1's
           server-side counterpart).
         - Find the existing organizer row via getOrganizerParticipant(poll.id).
           If NONE: INSERT one participants row `{ pollId, name, email: null,
           isOrganizer: true, editToken: generateToken() }` using the same
           isUniqueViolation retry loop submit-response.ts uses for the editToken; keep
           the new participantId. If EXISTS: `db.update(participants).set({ name }).where(eq(participants.id, existing.id))`
           and keep that participantId. This find-or-create is the at-most-one
           enforcement (LOCKED 6) — do NOT add a DB constraint; the form's isPending
           single-submit guard (Task 3) plus the single-admin token model bound the
           concurrency window, matching the existing no-constraint participant
           precedent (edge-probe ORG-01 concurrency resolution).
         - Build vote rows from getOptionsForPoll(poll.id) (authoritative; gap-fill
           untouched to "no", ignore foreign optionIds) and persist with ONE
           `insert(votes).values(rows).onConflictDoUpdate({ target: [votes.participantId,
           votes.optionId], set: { state: sql\`excluded.state\` } })` — identical to
           update-response.ts (covers both first-add and edit in one atomic statement).
         - redirect to /a/{adminUrlId}. NO email hooks (this is NOT submit/update-response;
           the organizer row sends no confirmation and triggers no creator-notify —
           prohibition-probe: hook bleed). The emailless row is therefore also invisible
           to Phase-7 invitation tracking (edge-probe cross-feature resolution).
         Export `SaveOrganizerAvailabilityState = { errors?: Record<string,string[]> } | null`.
    3. Write save-organizer-availability.test.ts (DB-backed; mirror the
       send-invites/close-poll harness) covering every <behavior> row — in particular
       assert that after a first-add THEN an edit there is EXACTLY ONE is_organizer=true
       row for the poll (query count), and that a closed/expired poll writes nothing.
  </action>
  <acceptance_criteria>
    - source: ORG-01; LOCKED constraints 6, 7; CONTEXT ORG-01.
    - behavior: first save inserts one organizer row + votes; a subsequent save upserts the same row (still exactly one is_organizer=true); a closed/expired poll or unknown token writes nothing; votes come from the authoritative option list; no email hook fires.
    - test: `DATABASE_URL=postgres://postgres:password@localhost:5432/lfg npm run test -- src/lib/actions/save-organizer-availability.test.ts` passes, including the exactly-one-row assertion.
  </acceptance_criteria>
  <verify>
    <automated>DATABASE_URL=postgres://postgres:password@localhost:5432/lfg npm run test -- src/lib/actions/save-organizer-availability.test.ts</automated>
  </verify>
  <done>The organizer's availability upserts a single is_organizer row + its votes under admin-token authorization, gated by isVotingOpen, with no email and no duplicate row.</done>
</task>

<task type="auto">
  <name>Task 2: Surface is_organizer through the results read + "(you)" suffix in the grid</name>
  <files>src/lib/db/queries.ts, src/lib/results.ts, src/components/results-grid.tsx</files>
  <read_first>
    - src/lib/db/queries.ts:159-190 (getResultsForPoll — add is_organizer to the select and to the per-participant reducer object; do NOT change the LEFT JOIN, the ORDER BY createdAt, or the email-omission discipline)
    - src/lib/results.ts:16-27 (the ResultsParticipant type — add an optional isOrganizer flag; computeResults reads only id/name/votes so it is untouched)
    - src/components/results-grid.tsx:282-296 (mobile per-date name cell `<span>{p.name}</span>`) and :510-519 (desktop row-header `{p.name}`) — the two places the "(you)" suffix appends
    - .planning/phases/08-scheduling-controls/08-UI-SPEC.md "New Surfaces → 2. Results-grid integration" (the exact muted " (you)" span, presentation-only, no reorder) and Prohibition Probe #5
  </read_first>
  <action>
    Per ORG-01 (SC5; UI Probes #5):

    1. queries.ts getResultsForPoll: add `isOrganizer: participants.isOrganizer` to the
       select, and in the reducer set `isOrganizer: r.isOrganizer ?? false` on each
       participant object (coalesce the nullable column to a boolean). Leave the LEFT
       JOIN, createdAt ordering, and the deliberate email/token omission exactly as-is —
       the organizer row folds in through the existing all-participants read (no new
       aggregation path; SC5). `participants.createdAt` stays ORDER-BY-only.
    2. results.ts: extend `ResultsParticipant` with `isOrganizer?: boolean`. Do NOT
       touch computeResults — it reads id/name/votes only, so the organizer's votes
       already count toward yes/ifneedbe tallies and best-day with zero code change
       (SC5).
    3. results-grid.tsx: in BOTH name renderings (the mobile per-date list cell and the
       desktop sticky row-header) append, when `p.isOrganizer` is true, a muted suffix
       `<span className="text-muted-foreground"> (you)</span>` directly after the name.
       Presentation only — driven SOLELY by the is_organizer flag, NEVER by the name
       string (a participant literally named "You" gets no suffix; UI Probe #5). No
       badge, no color change, no reordering.
  </action>
  <acceptance_criteria>
    - source: ORG-01; SC5; UI Probe #5; UI-SPEC Surface 2 results integration.
    - behavior: getResultsForPoll returns isOrganizer per participant; the organizer row renders with a muted "(you)" suffix in both grid layouts; computeResults/aggregation is unchanged so the organizer's votes count toward best-day.
    - test: typecheck passes; the "(you)" suffix is gated on p.isOrganizer (not the name); computeResults is unmodified.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "isOrganizer" src/lib/db/queries.ts && grep -q "isOrganizer" src/lib/results.ts && grep -q "(you)" src/components/results-grid.tsx</automated>
  </verify>
  <done>The organizer row carries is_organizer through the results read and renders a flag-driven "(you)" suffix in both grid layouts, with computeResults untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Admin "Your availability" card (organizer-availability island)</name>
  <files>src/components/organizer-availability-control.tsx, src/app/a/[adminUrlId]/page.tsx</files>
  <read_first>
    - src/components/vote-form.tsx (the AvailabilityGrid caller pattern: useActionState, the votes JSON hidden input serialized from grid onChange, the allAnswered gate + "Choose an option for every date to submit" helper text, disabled={isPending} — reuse this composition WITHOUT the email field)
    - src/components/availability-grid.tsx (the component reused verbatim: props options/initial/disabled/onChange; disabled renders the read-only chip list)
    - src/components/book-it-control.tsx (the "Cancel"/"Keep poll open" type="button" collapse precedent for the inline form's Cancel)
    - src/app/a/[adminUrlId]/page.tsx:99-126,216-238 (isClosed; the getResultsForPoll `participants` array already loaded on the page — derive the organizer row from it; the candidate-date <details> and the Results Card between which this card inserts)
    - .planning/phases/08-scheduling-controls/08-UI-SPEC.md "New Surfaces → 2. Your availability Card" (placement, visibility table, states table, Save/Cancel copy) and Prohibition Probe #1
    - src/lib/poll-status.ts (isVotingOpen — the page computes the card's editable-vs-readonly-vs-hidden gate)
  </read_first>
  <action>
    Per ORG-01 (LOCKED constraint 6; UI Probe #1; UI-SPEC Surface 2):

    1. Admin page: the page already loads `participants` (via getResultsForPoll, now
       carrying isOrganizer + votes). Derive the organizer row:
       `const organizerRow = participants.find((p) => p.isOrganizer);` and compute the
       card gate from the SAME isVotingOpen used elsewhere: reuse the plan-02
       server-computed `now`/votingOpen (or compute `const votingOpen = isVotingOpen(poll, new Date())`).
       Insert `<OrganizerAvailabilityControl>` as a standalone Card directly AFTER the
       candidate-date <details> echo and BEFORE the Results Card, passing:
       `adminUrlId={poll.adminUrlId}`, `options` (the chronological getOptionsForPoll
       list), `initialName={organizerRow?.name ?? ""}`, `initialVotes={organizerRow?.votes
       as Record<string,VoteState> | undefined}`, `hasRow={!!organizerRow}`, and
       `votingOpen={votingOpen}`. The organizer's prior votes come from the existing
       results read (its `votes` record is already Record<optionId,state>) — no extra
       query.
    2. Create src/components/organizer-availability-control.tsx ("use client") with
       useActionState(saveOrganizerAvailability). Implement the UI-SPEC visibility table
       (UI Probe #1):
         - votingOpen === false AND hasRow: read-only summary — heading + "Voting is
           closed — you can no longer change your availability." + `<AvailabilityGrid
           disabled initial={initialVotes} ...>` (the existing closed-chip rendering).
           No editable form, no Save.
         - votingOpen === false AND !hasRow: render NOTHING (card hidden entirely).
         - votingOpen === true: the editable branch with a collapsed/expanded inline
           form. Collapsed default: if hasRow → emerald "Added — showing as "{name}" in
           the results below." + outline "Edit your availability" button; else →
           "You haven't added your own availability yet." + "Add your availability"
           button (both type="button", toggle a useState open flag). Expanded form: a
           <Label>"Display name" + <Input defaultValue={initialName || "You"} maxLength
           100> (optional override, NO email field) + `<AvailabilityGrid options
           initial={initialVotes} onChange={setVotes}>` + hidden votes JSON input +
           "Save my availability" (pending "Saving…") + "Cancel" (type="button", collapses
           with zero side effects — BookItControl precedent). Gate Save disabled until
           allAnswered (reuse vote-form's allAnswered logic + the "Choose an option for
           every date to submit" helper) so the organizer row can't be saved
           half-answered. Include the hidden adminUrlId input. Surface `_form` errors via
           the existing role="alert" text-destructive pattern. The isPending disabled
           state on the form is the single-submit guard for the at-most-one invariant.
       Reuse AvailabilityGrid verbatim (no changes to that component — this phase only
       adds a new caller). No shadcn Dialog/date-picker; native controls only.
  </action>
  <acceptance_criteria>
    - source: ORG-01; LOCKED 6; UI Probe #1; UI-SPEC Surface 2.
    - behavior: the admin view shows a "Your availability" card between the candidate-date echo and Results; while voting is open the organizer can add (empty state) or edit (pre-filled) their row and Save is gated on all dates answered; once voting is closed the card is hidden (no row) or read-only (row exists) — the editable grid never renders; the saved row appears "(you)" in Results below.
    - test: typecheck passes; the control reuses AvailabilityGrid; the editable form is gated behind votingOpen; the card is placed before Results.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "OrganizerAvailabilityControl" src/app/a/\[adminUrlId\]/page.tsx && grep -q "AvailabilityGrid" src/components/organizer-availability-control.tsx && grep -q "useActionState" src/components/organizer-availability-control.tsx</automated>
  </verify>
  <done>The admin view renders a "Your availability" card that adds/edits the organizer's row (reusing AvailabilityGrid, no email), hides or read-only-locks once voting closes, and whose saved row shows "(you)" in the results grid.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
- New action: `src/lib/actions/save-organizer-availability.ts` (`saveOrganizerAvailability` — single-row upsert, admin-token, isVotingOpen-gated, no email) + `save-organizer-availability.test.ts`.
- New read helper: `getOrganizerParticipant(pollId)` in `queries.ts`.
- New component: `src/components/organizer-availability-control.tsx` (admin-only client island reusing AvailabilityGrid).
- Changed read: `getResultsForPoll` now selects `is_organizer`; `ResultsParticipant` gains `isOrganizer?: boolean` (computeResults unchanged).
- Changed component: `results-grid.tsx` appends a flag-driven "(you)" suffix in both layouts.
- Changed page: admin page renders the "Your availability" card between the candidate-date echo and Results.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → saveOrganizerAvailability | Untrusted adminUrlId + name + votes cross here; the admin token is the sole authorization |
| organizer row → results grid | The organizer row is a normal participant; the grid must keep omitting emails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Elevation of Privilege | saveOrganizerAvailability | mitigate | Re-derive the poll from the admin token via getPollByAdminUrlId; notFound on miss; never a client poll/participant id (LOCKED 7) |
| T-08-09 | Tampering (data integrity) | duplicate organizer rows | mitigate | find-or-create the single is_organizer row; edit upserts the same row; isPending single-submit guard; test asserts exactly one row after add+edit (LOCKED 6) |
| T-08-10 | Tampering | availability after close | mitigate | Server-side isVotingOpen gate rejects a write on a closed/expired poll independent of the client hiding the form (UI Probe #1) |
| T-08-11 | Information Disclosure | organizer row email | mitigate | Organizer row has no email; getResultsForPoll still omits emails; the "(you)" suffix carries only the display name; invisible to Phase-7 email-keyed invitation tracking |
| T-08-12 | Spoofing | "(you)" via name string | mitigate | Suffix driven solely by is_organizer=true, never by a name equal to "You" (UI Probe #5) |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new dependencies (reuses AvailabilityGrid + native inputs) |
</threat_model>

<verification>
- First save inserts one is_organizer row + votes; edit upserts the same row (exactly one row asserted); a closed/expired poll writes nothing; unknown token notFounds; no email hook fires.
- getResultsForPoll surfaces isOrganizer; the organizer's votes fold into best-day (computeResults unchanged); the grid shows a flag-driven "(you)" suffix.
- The admin "Your availability" card adds/edits while open and hides/read-only-locks once closed.
- `npx tsc --noEmit` passes; DB-backed action test green.
</verification>

<success_criteria>
- SC4: the organizer can add and edit their own availability row from the admin view, without the participant link.
- SC5: the organizer's row appears in the results grid and best-day computation like any other participant.
</success_criteria>

<output>
Create `.planning/phases/08-scheduling-controls/08-03-SUMMARY.md` when done.
</output>

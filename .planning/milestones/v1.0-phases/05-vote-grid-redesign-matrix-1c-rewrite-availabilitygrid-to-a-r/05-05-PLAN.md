---
phase: 05-vote-grid-redesign-matrix-1c
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email/templates.ts
autonomous: true
requirements: []          # Visual reconciliation of the shipped MAIL-*/FNL-* email templates + the one optional D-10 code change; no new requirements.
user_setup: []

must_haves:
  truths:
    - "D-09: the three email templates (3f invite / 3g confirmation / 3h finalization) match the mocks — one shared shell, single outer `<table>`, 600px max width, inline styles only, system font stack, hex approximations of the OKLCH tokens (#171717 FG, #ffffff BG, #737373 muted, #e5e5e5 border, #fafafa card); NO images/`<link>`/webfont/`<script>`; a plain-text fallback link is ALWAYS present"
    - "D-10: `calLink()` gains a per-provider `background` argument — the finalization email's two calendar buttons are colored Google `#1a73e8` vs neutral `#171717` (the existing `FG` constant, reused — no second neutral constant); both buttons are still omitted cleanly (`\"\"`) when their URL is absent / the calendar build fails (MAIL-03 unconfigured path unaffected)"
    - "SEC/D-10: admin `/a/` URLs NEVER appear in any template — the finalization email links only `participantUrl` and `icsUrl` (the hosted `/p/[participantUrlId]/event.ics`), never an `/a/` path"
    - "No structural rewrite of renderShell / the render functions beyond the `calLink` signature; no new dependency; the finalization email keeps NO primary button (event-details block + the two calendar buttons + fallback link only)"
  artifacts:
    - path: "src/lib/email/templates.ts"
      provides: "Reconciled invite/confirmation/finalization templates + per-provider calendar-button color (GOOGLE_BLUE / FG) via calLink(href, label, background)"
      contains: "calLink"
  key_links:
    - from: "src/lib/email/templates.ts (renderFinalizationEmail)"
      to: "calLink(href, label, background)"
      via: "GOOGLE_BLUE for the Google button, FG for the Apple/Outlook button"
      pattern: "GOOGLE_BLUE"
---

<objective>
Reconcile the three transactional email templates (mock boards 3f invite / 3g confirmation / 3h
finalization) to the mocks under D-09, and implement the one optional code change (D-10): give
`calLink()` a per-provider `background` argument so the finalization email's two add-to-calendar buttons
are distinguishable by color (Google `#1a73e8` vs neutral `#171717`) — email clients strip images, so
color is the only reliable per-provider signal.

Purpose: emails match the handoff and the two calendar buttons are visually distinct, while every email
invariant is preserved (600px table, inline styles, system font, plaintext fallback always, clean-omit
when a calendar URL is absent, and NO admin URL anywhere).
Output: reconciled `templates.ts` with the per-provider `calLink` color.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-CONTEXT.md
@.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md
@src/lib/email/templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile invite / confirmation / finalization templates to boards 3f–3h (visual only)</name>
  <files>src/lib/email/templates.ts</files>
  <read_first>
    - src/lib/email/templates.ts (shipped shell + the three render functions + `FG`/`BG`/`MUTED`/`CARD_BORDER`/`CARD_BG` hex constants)
    - design_handoff_vote_grid_redesign/designs/Email Templates.dc.html (boards 3f invite / 3g confirmation / 3h finalization — HTML is source of truth)
    - design_handoff_vote_grid_redesign/README.md (Emails section — shell, 600px, inline styles, system font, plaintext fallback always, admin URLs never)
  </read_first>
  <action>
    Diff the shipped shell + three render functions against boards 3f–3h and adjust ONLY drifting inline
    styles / copy — do not restructure `renderShell`. Confirm/preserve: one shared shell, single outer
    `<table>`, 600px max width, inline styles only, system font stack, the hex token approximations
    (`#171717`/`#ffffff`/`#737373`/`#e5e5e5`/`#fafafa`), no images/`<link>`/webfont/`<script>`, and a
    plain-text fallback link ALWAYS present. 3f invite (`renderInviteEmail`): CTA = participant link,
    "View the poll & vote". 3g confirmation (`renderConfirmationEmail`): CTA = edit link, "View or edit my
    response". 3h finalization (`renderFinalizationEmail`): event-details block, NO primary button, then
    the two calendar buttons + fallback link. Verify NO admin `/a/` URL is constructed anywhere in the
    file. Introduce no new dependency; keep the render-function signatures unchanged (only `calLink` gains
    a param in Task 2).
  </action>
  <acceptance_criteria>
    - Templates keep the 600px single-`<table>` shell, inline styles, system font, and the hex constants; no `<img`, `<link`, `<script`, or webfont is present (grep confirms absence).
    - Every render path still emits a plaintext fallback link (grep confirms the fallback link markup).
    - No `/a/` admin path appears anywhere in `templates.ts` (grep returns nothing).
    - `npm run build` + `npm run lint` pass; the shipped email tests (if any) stay green via `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && ! grep -Eq "<img|<script|<link|/a/" src/lib/email/templates.ts && echo EMAIL_SAFE_OK</automated>
    <human-check>Render the three emails (Mailpit local capture) and compare against boards 3f–3h — shell, CTAs, event-details block, and the always-present plaintext fallback.</human-check>
  </verify>
  <done>The three templates match boards 3f–3h; the 600px/inline/system-font/plaintext-fallback invariants hold; no admin URL or forbidden element appears.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-provider calendar-button color to calLink (D-10)</name>
  <files>src/lib/email/templates.ts</files>
  <read_first>
    - src/lib/email/templates.ts (lines 159-170 — current `calLink(href, label)` hardcoding `FG`; the `calendarLinks` array with the clean-omit ternaries; `FG` defined at line 23)
    - .planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-PATTERNS.md (templates.ts section — the exact minimal-diff signature change)
    - design_handoff_vote_grid_redesign/README.md ("The calendar feature" — Google #1a73e8 vs neutral #171717 rationale)
  </read_first>
  <action>
    Add a third `background: string` parameter to `calLink(href, label, background)` and use it for the
    button's `background-color` (replacing the hardcoded `FG`). Define `const GOOGLE_BLUE = "#1a73e8";`
    and thread it into the Google button call; pass the existing `FG` (`#171717`) for the Apple/Outlook
    button — do NOT introduce a second neutral constant. Preserve every other line/behavior: the two
    `calendarLinks` entries stay guarded by their `googleCalendarUrl ? … : ""` / `icsUrl ? … : ""`
    ternaries so each button is omitted cleanly when its URL is absent, and `calendarBlock` stays `""`
    when both are absent (MAIL-03 unconfigured path unaffected). The `icsUrl` remains the hosted
    `/p/[participantUrlId]/event.ics` — never an `/a/` path. No other signature changes; no new dependency.
  </action>
  <acceptance_criteria>
    - `calLink` has a `background` parameter and the finalization email passes `GOOGLE_BLUE` (`#1a73e8`) for Google and `FG` (`#171717`) for Apple/Outlook (grep confirms `GOOGLE_BLUE` + `#1a73e8` and the `FG` reuse).
    - The clean-omit ternaries (`googleCalendarUrl ? … : ""`, `icsUrl ? … : ""`) and the `calendarBlock` empty-fallback are unchanged (grep confirms both `? ` … `: ""` guards remain).
    - Only ONE new neutral usage — no second `#171717`/neutral constant is introduced beyond the reused `FG`.
    - `npm run build` + `npm run lint` pass; `npm test` stays green.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "GOOGLE_BLUE" src/lib/email/templates.ts && grep -q "#1a73e8" src/lib/email/templates.ts && grep -q ': ""' src/lib/email/templates.ts && echo CALLINK_OK</automated>
    <human-check>In the Mailpit-captured finalization email for a closed poll, the two calendar buttons are visibly distinct (blue Google vs neutral Apple/Outlook) with legible white text; when a calendar URL is absent the corresponding button is cleanly omitted.</human-check>
  </verify>
  <done>calLink takes a per-provider background; the finalization email's two calendar buttons are color-distinguished (Google blue vs neutral) with the clean-omit and no-admin-URL invariants preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered email HTML → recipient mail client | Emails are sent to voters. Must NOT leak admin `/a/` URLs or tokens; the calendar `icsUrl` is the participant-keyed `/p/…/event.ics`, never an admin path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information disclosure | email templates | mitigate | Grep-verify NO `/a/` admin URL appears anywhere in `templates.ts` (SEC/D-10); finalization links only `participantUrl` + the participant-keyed `icsUrl`. |
| T-05-12 | Tampering | calLink clean-omit | mitigate | Preserve the `? … : ""` guards + empty `calendarBlock` so an unbuilt calendar (or unconfigured email) degrades cleanly (MAIL-03); acceptance greps confirm the guards remain. |
| T-05-13 | Injection | calLink background param | accept | `background` is only ever a hardcoded constant (`GOOGLE_BLUE`/`FG`), never user input — no interpolation-injection vector; no HTML/script is introduced (email forbids `<script>`/images). |

ASVS L1: no high-severity threat — visual reconciliation + a tightly-scoped signature change over
hardcoded constants; the no-admin-URL and clean-omit invariants are preserved. No package installs in
this plan.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `GOOGLE_BLUE` constant (`#1a73e8`) in `templates.ts` (new symbol).
- `calLink(href, label, background)` — the `background` parameter is added (signature change; only new API surface).
- No new files or dependencies; no new token/utility/font. Record 3f–3h "drift fixed" vs "no drift" in the SUMMARY.
</artifacts_produced>

<verification>
- `npm run build`, `npm run lint`, and `npm test` pass.
- Grep confirms: no `<img`/`<script`/`<link`/webfont/`/a/` in templates.ts; the plaintext fallback link; `GOOGLE_BLUE`/`#1a73e8`; the reused `FG`; and the intact `? … : ""` clean-omit guards.
- (end-of-phase human check) The three emails match boards 3f–3h and the two calendar buttons are color-distinct with legible text.
</verification>

<success_criteria>
- The three email templates match boards 3f–3h (D-09) with all shipped email invariants preserved.
- `calLink` colors the two calendar buttons per provider (Google blue vs neutral FG), reusing `FG` (D-10).
- No admin `/a/` URL in any template (SEC/D-10); clean-omit + plaintext-fallback preserved; no new dependency.
</success_criteria>

<output>
Create `.planning/phases/05-vote-grid-redesign-matrix-1c-rewrite-availabilitygrid-to-a-r/05-05-SUMMARY.md` when done.
</output>

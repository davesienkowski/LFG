---
phase: quick-260701-il0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/instrumentation.ts
  - src/components/availability-grid.tsx
  - .planning/phases/02-participant-voting/.continue-here.md
autonomous: true
requirements: [REVIEW-1, REVIEW-2]

must_haves:
  truths:
    - "A missing/malformed/empty DATABASE_URL or NEXT_PUBLIC_BASE_URL fails loudly at Node server startup (the env guard actually runs; emptyStringAsUndefined means empty strings count as missing)"
    - "The env guard fires ONLY in the Node.js server runtime — not the edge runtime (NEXT_RUNTIME guard) and not vitest (which never imports instrumentation or @/lib/env); npm test does not require NEXT_PUBLIC_BASE_URL"
    - "AvailabilityGrid.cycleCell computes next state and calls setAnnouncement OUTSIDE the setCellState updater (updater is pure)"
    - "Click-to-cycle behavior is unchanged: a cell still advances no -> yes -> ifneedbe -> no, announces the new state, and a single cell click overrides ONLY that key (prev-merge preserved)"
    - "The stale .continue-here.md checkpoint no longer exists in the Phase 2 directory"
  artifacts:
    - path: "src/instrumentation.ts"
      provides: "Next.js register() hook that imports @/lib/env in the Node.js runtime, activating the env validation guard"
      contains: "register"
    - path: "src/components/availability-grid.tsx"
      provides: "AvailabilityGrid with a pure cycleCell setState updater"
  key_links:
    - from: "src/instrumentation.ts"
      to: "src/lib/env.ts"
      via: "dynamic import guarded by NEXT_RUNTIME === nodejs"
      pattern: "import\\(\"@/lib/env\"\\)"
---

<objective>
Apply the two MEDIUM hardening follow-ups from the Phase 2 code review (`02-REVIEW.md` findings #1 and #2) and delete one stale checkpoint file. Both fixes are already fully designed — implement them exactly; do not re-investigate the design.

Purpose: Activate the "fail loudly on missing env" guard that is currently dead code (finding #1), and eliminate a latent concurrent-render risk in the accessibility announcement path (finding #2). Neither is a live exploit; both harden against future self-host redeploys and React Strict/concurrent rendering.

Output:
- New `src/instrumentation.ts` wiring env validation into an always-executed Node startup entry point.
- Pure `cycleCell` updater in `src/components/availability-grid.tsx`.
- Removal of the stale `.planning/phases/02-participant-voting/.continue-here.md` leftover.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-participant-voting/02-REVIEW.md
@src/lib/env.ts
@src/lib/urls.ts
@src/components/availability-grid.tsx
@src/components/availability-grid.test.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate env validation via instrumentation register hook (REVIEW finding #1)</name>
  <files>src/instrumentation.ts</files>
  <action>
Create a NEW file `src/instrumentation.ts`. Note the path: this project uses a `src/` directory, so Next.js reads instrumentation at `src/instrumentation.ts`, NOT at the repo root — do not create `instrumentation.ts` at the root.

Add a short top-of-file comment explaining WHY this file exists: `src/lib/env.ts` exports `env = createEnv({...})` whose only side effect is to fail loudly when `DATABASE_URL` or `NEXT_PUBLIC_BASE_URL` are missing/malformed, but nothing imports it (`grep -rln "lib/env" src/` returns 0 hits), so that guard never runs. This file is the always-executed entry point that activates it. The Phase 2 `/thanks` edit link is built via `resolveBaseUrl()` in `src/lib/urls.ts`, which silently falls back to request `Host`/`X-Forwarded-Proto` headers when `NEXT_PUBLIC_BASE_URL` is unset — so validating that var at startup is the point.

Export an async function named `register`. Inside it, guard with `if (process.env.NEXT_RUNTIME === "nodejs")` and then `await import("@/lib/env")`. The runtime guard matters because `register()` can also fire in the edge runtime; env validation should run once at Node server startup only. Use the dynamic `import("@/lib/env")` (not a static top-of-file import) so it is scoped inside the guard.

Do NOT modify `next.config.ts`: it is an empty config, and Next.js 16 ships `instrumentation.ts` as stable — no `experimental.instrumentationHook` flag is required. Confirm `next.config.ts` needs no change and leave it untouched.

Do NOT delete `src/lib/env.ts` (the review offered deletion as an alternative; the chosen fix is to wire it in).

INTENDED NEW FAILURE MODE (do not treat as a bug): once wired, `next build` / `next dev` / `next start` will now hard-fail when `DATABASE_URL` or `NEXT_PUBLIC_BASE_URL` is missing OR an empty string (`env.ts` sets `emptyStringAsUndefined: true`). This repo's `.env.local` already carries non-empty values for BOTH (Next auto-loads `.env.local` during build/dev), so the Task 1 `npm run build` verify passes here. If a self-host redeploy omits either var, this guard firing IS the desired outcome — that is the entire point of finding #1.

Commit atomically with message: `fix(quick): activate env validation via instrumentation register hook`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && test -f src/instrumentation.ts && grep -q 'process.env.NEXT_RUNTIME === "nodejs"' src/instrumentation.ts && grep -q 'import("@/lib/env")' src/instrumentation.ts && npm run build</automated>
  </verify>
  <done>`src/instrumentation.ts` exists with an async `register()` that dynamically imports `@/lib/env` only under the `NEXT_RUNTIME === "nodejs"` guard; `next.config.ts` is unchanged; tsc, lint, and build are all clean (build succeeds because `.env.local` supplies both required vars).</done>
</task>

<task type="auto">
  <name>Task 2: Make cycleCell setState updater pure + remove stale checkpoint (REVIEW finding #2)</name>
  <files>src/components/availability-grid.tsx, .planning/phases/02-participant-voting/.continue-here.md</files>
  <action>
In `src/components/availability-grid.tsx`, rewrite `cycleCell` (currently lines 87-94) so the `setCellState` updater is pure — no `setAnnouncement` call inside it.

Compute the next state from the render-time `cellState` snapshot (the component already holds `cellState` state): read `const current = cellState[opt.id] ?? "no";`, then `const next = CYCLE[(CYCLE.indexOf(current) + 1) % CYCLE.length];`. Call `setCellState((prev) => ({ ...prev, [opt.id]: next }))` — KEEP the `prev` merge so concurrent updates to OTHER keys are not clobbered by a stale closure — but use the precomputed `next` for THIS key. Then call `setAnnouncement(\`${optionLabel(opt)} set to ${STATE_META[next].label}\`)` OUTSIDE (after) the `setCellState` call.

Preserve the `?? "no"` fallback exactly (it guards a missing/undefined key for a newly-added option). This mirrors how `setAll` already computes its announcement outside the updater. Behavior is identical (same cycle order, same announcement string, same single-cell override semantics); only the purity of the updater changes, removing the Strict/concurrent-render double-invocation risk on the `aria-live` announcement.

Also in this task, delete the stale leftover file `.planning/phases/02-participant-voting/.continue-here.md`. Phase 2 is complete (both plans have SUMMARYs, `02-VERIFICATION.md` passed, project already transitioned to Phase 3); this checkpoint is a leftover with no consumers. Use `git rm` (or `rm`) to remove it.

Commit atomically with message: `fix(quick): pure setState updater in AvailabilityGrid + remove stale checkpoint`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && ! grep -Pzoq 'setCellState\(\(prev\)[^}]*setAnnouncement' src/components/availability-grid.tsx && test ! -e .planning/phases/02-participant-voting/.continue-here.md && npm run build && DATABASE_URL="postgres://postgres:password@localhost:5432/lfg" npm test</automated>
  </verify>
  <done>`cycleCell` computes `next` from the `cellState` snapshot, the `setCellState` updater only merges (`prev` spread + `next` for this key), `setAnnouncement` is called outside the updater, `?? "no"` fallback preserved; the stale `.continue-here.md` is gone; tsc, lint, build clean; full vitest suite passes (baseline 81/81) with `DATABASE_URL` exported. The existing `availability-grid.test.tsx` "single click overrides only that cell" case stays green (locks the prev-merge).</done>
</task>

</tasks>

<edge_resolutions>
Edge-probe family run against this plan's NEW requirements (REVIEW-1, REVIEW-2). All resolved with source-backed reasons; no unresolved edges remain.

Deterministic edge-probe:
- REVIEW-1 / idempotency ("runs twice on same input?") — RESOLVED. `await import("@/lib/env")` is served from Node's module cache on any second `register()` invocation, so `createEnv` evaluates once. Validation is a pure read of `process.env` with no side effect other than throwing on invalid config. Running it repeatedly yields the same pass/fail. Safe.
- REVIEW-1 / concurrency ("interrupted or parallel?") — RESOLVED. `register()` is a startup hook with no shared mutable state; the `NEXT_RUNTIME === "nodejs"` guard scopes env validation to the Node runtime (edge isolate skips it). No cross-invocation mutation hazard.
- REVIEW-2 / adjacency ("equal/touching — merge, collide, or separate?") — RESOLVED (test-backed). The real concern is one cell click clobbering OTHER keys; the `prev` spread merge is preserved and is locked by `availability-grid.test.tsx:44-63` ("a single click overrides only that cell"). Same-key double-cycle within one render batch is unreachable via discrete user clicks — React commits each click before the next handler reads the `cellState` snapshot. Design is per spec; behavior unchanged.
- REVIEW-2 / empty ("empty/single/null input?") — RESOLVED. `cellState[opt.id] ?? "no"` fallback preserved for undefined keys; empty `options` renders no buttons so `cycleCell` is never called. Locked by the "renders every untouched cell" test.
- REVIEW-2 / ordering ("equal elements — stable output order?") — RESOLVED. `CYCLE` order unchanged; `onChange` emits in original `options` order via the `cellState` `useEffect` map (no re-sort). Locked by the "emits the serialized votes for every option" test.

Prohibition-probe ("what could this silently become that the author would NOT want, but the spec does not forbid?"):
- FINDING (folded into plan): Activating the env guard silently changes the failure surface of `next build`/`dev`/`start` — they now hard-fail on a missing OR empty (`emptyStringAsUndefined`) `DATABASE_URL`/`NEXT_PUBLIC_BASE_URL`. This is the intended behavior of finding #1, but is folded into Task 1's action + must_haves so it is not misread as a code bug. Source-verified: `.env.local` carries both non-empty vars and Next auto-loads it during build/dev, so the plan's own `npm run build` verify passes.
- DISMISSED: guard silently breaking `npm test`. Source-verified NOT reachable — `grep -rln instrumentation src` = 0 test references, and `@/lib/env` is imported only by the new `instrumentation.ts`, which vitest never loads. So `npm test` never triggers the guard and does not need `NEXT_PUBLIC_BASE_URL`; exporting only `DATABASE_URL` (for DB-backed action tests) is correct.
- DISMISSED: deleting `.continue-here.md` silently breaking tooling. It lives under `.planning/` (not code), Phase 2 is complete, and it has no consumers.
</edge_resolutions>

<verification>
Whole-plan gate (both fixes together, DB env exported per project convention — local pg is docker container `lfg-db-1` on :5432, db `lfg`):

```
npx tsc --noEmit
npm run lint
npm run build
DATABASE_URL="postgres://postgres:password@localhost:5432/lfg" npm test
```

Expected: tsc clean, lint clean, build succeeds (the newly-activated env guard passes because `.env.local` supplies non-empty `DATABASE_URL` and `NEXT_PUBLIC_BASE_URL`, auto-loaded by Next during build), and vitest green at the prior baseline of 81/81 tests. `npm test` does NOT trigger the env guard (vitest never imports `instrumentation.ts`/`@/lib/env`), so it needs only `DATABASE_URL` exported. No behavioral change in the availability grid (cycle order, announcements, and single-cell override identical); env guard now runs at Node startup; stale checkpoint removed.
</verification>

<success_criteria>
- `src/instrumentation.ts` exists and dynamically imports `@/lib/env` under a `NEXT_RUNTIME === "nodejs"` guard; `next.config.ts` unchanged.
- `AvailabilityGrid.cycleCell` has a pure `setCellState` updater; `setAnnouncement` and `next` computed outside it; `?? "no"` fallback and prev-merge preserved.
- `.planning/phases/02-participant-voting/.continue-here.md` deleted.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; full vitest suite passes (81/81).
- Two atomic commits with the messages specified in each task.
</success_criteria>

<output>
Create `.planning/quick/260701-il0-apply-phase-2-code-review-follow-ups-1-w/260701-il0-SUMMARY.md` when done.
</output>
